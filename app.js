const topics = [
  {
    title: 'Значения, типы и переменные',
    lead: 'Что такое данные в программе и как дать им имя.',
    sections: [
      {
        title: 'Что такое тип',
        paragraphs: [
          'В программе всё, с чем мы работаем, — это значения: число, текст, список. У каждого значения есть тип. Тип говорит Python, что с объектом можно делать: складывать числа, склеивать строки, менять список.',
          'Коллекции `list`, `tuple`, `dict` и `set` разобраны в отдельных темах. Здесь — базовые типы.',
        ],
        syntax: 'type(значение)',
        example: 'print(type(25))\n# <class \'int\'>',
      },
      {
        title: 'Число',
        paragraphs: [
          'Целые числа имеют тип `int`: `25`, `0`, `-7`. Дробные — тип `float`: `19.99`, `3.0`. Для арифметики оба подходят, но `int` не хранит дробную часть.',
        ],
        syntax: 'целое = 25\nдробное = 19.99',
        example: 'age = 25\nprint(age)          # 25'
      },
      {
        title: 'Строка',
        paragraphs: [
          'Текст имеет тип `str`. Его пишут в кавычках. Даже если внутри цифры, это всё равно текст: `"10"` нельзя сложить как число, пока не превратить в `int`.',
        ],
        syntax: 'имя = "Анна"',
        example: 'name = "Анна"\nprint(name)         # Анна'
      },
      {
        title: 'Логическое значение',
        paragraphs: [
          '`bool` бывает только двух видов: `True` и `False`. Так отмечают «да» и «нет». Их получают из сравнений и пишут в условиях.',
        ],
        syntax: 'ok = True',
        example: 'ok = True\nprint(ok)           # True'
      },
      {
        title: 'None — пусто',
        paragraphs: [
          '`None` означает «значения нет». Это не ноль и не пустая строка, а отдельный объект. Его часто возвращает функция, которая ничего не отдаёт через `return`.',
        ],
        syntax: 'value = None',
        example: 'value = None\nprint(value)        # None',
      },
      {
        title: 'Переменная',
        paragraphs: [
          'Переменная — имя, которое ссылается на значение. Сначала значение создают, затем записывают в имя через `=`. После этого имя можно использовать вместо самого значения.',
          'Имя: буквы, цифры и `_`. Нельзя начинать с цифры: `age1` можно, `1age` нельзя. Регистр важен: `Age` и `age` — разные имена.',
        ],
        syntax: 'имя = значение',
        example: 'age = 25\nprint(age)         # 25\nage = 26\nprint(age)         # 26 — старое значение заменили',
        note: '`=` записывает значение в переменную. `==` сравнивает два значения. Строка `age = 25` — это не проверка, а запись.',
      },
      {
        title: 'Несколько имён сразу',
        paragraphs: [
          'Слева и справа от `=` можно поставить несколько имён и значений через запятую. Количество слева должно совпасть с количеством справа.',
        ],
        syntax: 'a, b = 1, 2',
        example: 'x, y = 10, 20\nprint(x, y)     # 10 20',
      },
      {
        title: 'Обмен значений',
        paragraphs: [
          'Две переменные можно поменять местами без третьей: справа пишут старые значения в новом порядке. Python сначала считает правую часть, потом записывает влево.',
        ],
        syntax: 'a, b = b, a',
        example: 'x, y = 10, 20\nx, y = y, x\nprint(x, y)     # 20 10',
      },
    ],
  },
  {
    title: 'Ввод, вывод и преобразование типов',
    lead: 'Как напечатать ответ, прочитать строку и превратить её в число.',
    sections: [
      {
        title: 'Одно значение',
        paragraphs: [
          '`print()` показывает значение на экране. После печати курсор переходит на новую строку.',
        ],
        syntax: 'print(значение)',
        example: 'print("Привет")\n# Привет',
      },
      {
        title: 'Несколько значений',
        paragraphs: [
          'Несколько аргументов печатаются через пробел. Так удобно вывести подпись и число рядом.',
        ],
        syntax: 'print(a, b)',
        example: 'print(10, 20)\n# 10 20',
      },
      {
        title: 'Разделитель sep',
        paragraphs: [
          'Параметр `sep` меняет то, что ставится между аргументами. По умолчанию это пробел.',
        ],
        syntax: 'print(a, b, sep=";")',
        example: 'print(10, 20, sep=";")\n# 10;20',
      },
      {
        title: 'Конец строки end',
        paragraphs: [
          'Параметр `end` — то, что ставится в конце. По умолчанию это перевод строки. Пустая строка оставляет курсор на той же линии, и следующий `print` допишет туда.',
        ],
        syntax: 'print(значение, end="")',
        example: 'print("A", end="")\nprint("B")\n# AB',
      },
      {
        title: 'Ввод: input',
        paragraphs: [
          '`input()` читает одну строку с клавиатуры. Даже если человек ввёл `10`, программа получит строку `"10"`, а не число.',
        ],
        syntax: 'текст = input("Подсказка: ")',
        example: 'name = input("Имя: ")\nprint("Привет,", name)',
        note: '`input()` всегда возвращает `str`. Пока строку не преобразовали, арифметика с ней будет склейкой, а не сложением.',
      },
      {
        title: 'int — целое число',
        paragraphs: [
          'Чтобы работать с целым числом, строку превращают через `int`. Если в `int()` попадёт не число, например `"abc"`, программа остановится с `ValueError`.',
        ],
        syntax: 'int("10")',
        example: 'n = int(input())\nprint(n + 1)',
      },
      {
        title: 'float — дробь',
        paragraphs: [
          '`float()` превращает строку в дробное число. Точка в записи обязательна в том виде, как её ждёт Python: `"1.5"`, не `"1,5"`.',
        ],
        syntax: 'float("1.5")',
        example: 'price = float("19.99")\nprint(price)        # 19.99',
      },
      {
        title: 'str — текст',
        paragraphs: [
          '`str()` превращает число в текст. Это нужно, когда число склеивают со строкой или пишут в файл как надпись.',
        ],
        syntax: 'str(25)',
        example: 'print("возраст: " + str(25))\n# возраст: 25',
      },
      {
        title: 'Ловушка: сложение строк',
        paragraphs: [
          'Две строки склеиваются знаком `+`. `"10" + "20"` даёт `"1020"`, а не `30`. Сначала каждую строку нужно превратить в число.',
        ],
        syntax: '"10" + "20"',
        example: 'print("10" + "20")             # 1020\nprint(int("10") + int("20"))   # 30',
      },
      {
        title: 'Два числа из одной строки',
        paragraphs: [
          'Частый приём: прочитать `"1 2"`, разрезать по пробелам и превратить каждый кусок в число. Слева должно быть столько переменных, сколько чисел во вводе.',
        ],
        syntax: 'a, b = map(int, input().split())',
        example: '# ввод: 3 7\na, b = map(int, input().split())\nprint(a + b)     # 10',
      },
      {
        title: 'Три числа из одной строки',
        paragraphs: [
          'Тот же приём работает для трёх (и больше) чисел: слева столько имён, сколько значений после `split`.',
        ],
        syntax: 'a, b, c = map(int, input().split())',
        example: '# ввод: 1 2 3\na, b, c = map(int, input().split())\nprint(a + b + c)     # 6',
      },
      {
        title: 'Список чисел из строки',
        paragraphs: [
          'Если чисел заранее неизвестно сколько, результат `map` собирают в список. Тогда длина не обязана совпадать с числом переменных слева.',
        ],
        syntax: 'nums = list(map(int, input().split()))',
        example: '# ввод: 4 8 15\nnums = list(map(int, input().split()))\nprint(nums)     # [4, 8, 15]',
      },
    ],
  },
  {
    title: 'Выражения и операторы',
    lead: 'Сложение, деление, остаток, степень и сравнения.',
    sections: [
      {
        title: 'Сложение',
        paragraphs: [
          'Выражение — то, что Python вычисляет и получает результат. `+` складывает числа. Для строк тот же знак склеивает текст — это другая операция.',
        ],
        syntax: 'a + b',
        example: 'print(10 + 4)     # 14',
      },
      {
        title: 'Умножение',
        paragraphs: [
          '`*` умножает числа. Для строки тот же знак повторяет текст: `"ab" * 3` даёт `"ababab"`.',
        ],
        syntax: 'a * b',
        example: 'print(3 * 5)      # 15',
      },
      {
        title: 'Степень',
        paragraphs: [
          'Степень записывают как `**`, не как `^`. `2 ** 10` — это 1024.',
        ],
        syntax: 'a ** b',
        example: 'print(2 ** 10)    # 1024',
      },
      {
        title: 'Деление /',
        paragraphs: [
          'Обычное деление `/` всегда даёт дробь: `8 / 2` это `4.0`, не `4`.',
        ],
        syntax: 'a / b',
        example: 'print(17 / 5)     # 3.4',
      },
      {
        title: 'Целое деление //',
        paragraphs: [
          '`//` делит нацело и отбрасывает дробную часть вниз. `17 // 5` даёт `3`.',
        ],
        syntax: 'a // b',
        example: 'print(17 // 5)    # 3',
      },
      {
        title: 'Остаток %',
        paragraphs: [
          '`%` — то, что не поместилось в целое число раз. `17 % 5` даёт `2`.',
        ],
        syntax: 'a % b',
        example: 'print(17 % 5)     # 2',
        note: 'Тождество: `a == (a // b) * b + (a % b)`.',
      },
      {
        title: 'Отрицательное //',
        paragraphs: [
          'Для отрицательных чисел `//` тоже идёт вниз, а не к нулю: `-5 // 2` даёт `-3`. Это легко перепутать с «отрезать дробь».',
        ],
        syntax: '-a // b',
        example: 'print(-5 // 2)    # -3',
      },
      {
        title: 'Сравнения',
        paragraphs: [
          'Сравнения дают `True` или `False`. Их пишут в условиях. Знак `=` там нельзя: он не сравнивает, а присваивает.',
        ],
        syntax: 'a == b    a != b\na < b     a <= b\na > b     a >= b',
        example: 'print(7 > 3)       # True',
        note: 'В условии пишут `if x == 0:`.',
      },
      {
        title: 'Приоритет и скобки',
        paragraphs: [
          'Сначала скобки, затем степень, затем умножение и деление, затем сложение, затем сравнения. Если сомневаетесь — ставьте скобки.',
        ],
        syntax: '(a + b) * c',
        example: 'print(2 + 2 * 2)    # 6\nprint((2 + 2) * 2)  # 8',
      },
    ],
  },
  {
    title: 'Условия',
    lead: 'Как выбрать одну из веток кода: if, elif, else.',
    sections: [
      {
        title: 'Конструкция if',
        paragraphs: [
          'Сначала проверяется `if`. Если условие ложно, Python смотрит следующий `elif`. Если ни одна проверка не подошла, выполняется `else`. После `if`, `elif` и `else` нужно двоеточие, тело блока пишут с отступом.',
          '`elif` может быть несколько. `else` — не больше одного, и только в конце. Ветку `else` можно не писать, если запасной код не нужен.',
        ],
        syntax: 'if условие:\n    код\nelif другое_условие:\n    код\nelse:\n    код',
        example: 'age = 20\nif age >= 18:\n    print("Доступ разрешён")\nelif age >= 14:\n    print("Нужен взрослый")\nelse:\n    print("Доступ запрещён")',
      },
      {
        title: 'and',
        paragraphs: [
          '`and` требует, чтобы оба условия были истинны. Если хотя бы одно ложно, вся проверка ложна.',
        ],
        syntax: 'условие1 and условие2',
        example: 'age, city = 20, "Казань"\nif age >= 18 and city == "Казань":\n    print("подойдёт")',
      },
      {
        title: 'or',
        paragraphs: [
          '`or` достаточно одного истинного условия. Оба ложны — тогда вся проверка ложна.',
        ],
        syntax: 'условие1 or условие2',
        example: 'age = 70\nif age < 14 or age > 65:\n    print("другой тариф")',
      },
      {
        title: 'Пустой список в if',
        paragraphs: [
          'В `if` ложными считаются `False`, `0`, пустая строка `""`, пустые коллекции и `None`. Пустой список `[]` ложный: ветка не выполнится.',
        ],
        syntax: 'if список:',
        example: 'if []:\n    print("не напечатается")',
        note: 'Список из нуля `[0]` истинный: в нём есть элемент.',
      },
      {
        title: 'Строка "0" в if',
        paragraphs: [
          'Непустая строка истинна, даже если внутри ноль. `"0"` — это текст из одного символа, не число ноль.',
        ],
        syntax: 'if "0":',
        example: 'if "0":\n    print("напечатается")',
      },
      {
        title: 'Короткий вариант',
        paragraphs: [
          'Если нужно выбрать одно из двух значений, пишут тернарный оператор. Для длинной развилки обычно понятнее обычный `if`. В Python 3.10+ для нескольких фиксированных вариантов есть `match-case`.',
        ],
        syntax: 'результат = a if условие else b',
        example: 'age = 20\nlabel = "взрослый" if age >= 18 else "ребёнок"\nprint(label)     # взрослый',
      },
    ],
  },
  {
    title: 'Строки',
    lead: 'Текст: создание, индексы, срезы и неизменяемость.',
    sections: [
      {
        title: 'Кавычки',
        paragraphs: [
          'Строка — это текст. Её пишут в двойных или одинарных кавычках, это равноправно. Пустая строка — `""`. Тройные кавычки удобны, когда внутри есть переносы.',
        ],
        syntax: 's = "Python"',
        example: 's = "Python"\nprint(s)            # Python',
      },
      {
        title: 'str()',
        paragraphs: [
          'Число превращают в текст через `str()`. Пока число не стало строкой, его нельзя склеить с надписью через `+`.',
        ],
        syntax: 'str(25)',
        example: 'print(str(5) + " яблок")\n# 5 яблок',
      },
      {
        title: 'Склейка +',
        paragraphs: [
          'Две строки склеивают знаком `+`. Получается новая строка, исходные не меняются.',
        ],
        syntax: '"Py" + "thon"',
        example: 'name = "Анна"\nprint("Привет, " + name)\n# Привет, Анна',
      },
      {
        title: 'Повтор *',
        paragraphs: [
          'Знак `*` повторяет строку указанное число раз.',
        ],
        syntax: '"ha" * 3',
        example: 'print("ha" * 3)\n# hahaha',
      },
      {
        title: 'Первый символ',
        paragraphs: [
          'Символы нумеруются с нуля: первый имеет индекс `0`.',
        ],
        syntax: 's[0]',
        example: 'word = "Python"\nprint(word[0])       # P',
      },
      {
        title: 'Последний символ',
        paragraphs: [
          'Индекс `-1` — последний символ. `-2` — предпоследний.',
        ],
        syntax: 's[-1]',
        example: 'word = "Python"\nprint(word[-1])      # n',
      },
      {
        title: 'Срез',
        paragraphs: [
          'Полная конструкция среза — три слота через двоеточие. `start` — откуда, включительно. `stop` — докуда, не включительно. `step` — шаг. Любую часть можно опустить: пустой `start` — с начала, пустой `stop` — до конца, пустой `step` — шаг `1`.',
        ],
        syntax: 's[start:stop:step]',
        example: 'word = "Python"\nprint(word[1:4])     # yth',
        note: 'Правая граница не входит в результат. `"Python"[0:3]` — это `"Pyt"`, без `h`.',
      },
      {
        title: 'Шаг среза',
        paragraphs: [
          'Третий слот — шаг. `s[0:6:2]` берёт символы через один. Все три части на месте — это та же конструкция `s[start:stop:step]`.',
        ],
        syntax: 's[start:stop:step]',
        example: 'word = "Python"\nprint(word[0:6:2])   # Pto',
      },
      {
        title: 'Переворот [::-1]',
        paragraphs: [
          'Если `start` и `stop` пустые, а шаг `-1`, срез идёт с конца: строка переворачивается.',
        ],
        syntax: 's[::-1]',
        example: 'word = "Python"\nprint(word[::-1])    # nohtyP',
      },
      {
        title: 'Строку нельзя изменить на месте',
        paragraphs: [
          'Нельзя написать `s[0] = "A"`. Нужно собрать новую строку: срезом и склейкой. Длина — `len(s)`. В условии пустая строка ложная.',
        ],
        syntax: 's = "К" + s[1:]',
        example: 's = "кот"\n# s[0] = "К"   # ошибка\ns = "К" + s[1:]\nprint(s)          # Кот',
      },
      {
        title: 'replace',
        paragraphs: [
          'Метод `.replace()` не меняет исходную строку: он возвращает новую, где кусок текста заменён.',
        ],
        syntax: 's.replace("о", "а")',
        example: 's = "кот"\nprint(s.replace("о", "а"))  # кат',
      },
      {
        title: 'f-строка',
        paragraphs: [
          'f-строка подставляет значения прямо в текст внутри фигурных скобок.',
        ],
        syntax: 'f"Возраст: {age}"',
        example: 'age = 25\nprint(f"Возраст: {age}")\n# Возраст: 25',
      },
      {
        title: 'Перенос строки',
        paragraphs: [
          'Служебная последовательность `\\n` — новая строка. `\\t` — табуляция, `\\\\` — сам слэш.',
        ],
        syntax: '"раз\\nдва"',
        example: 'print("раз\\nдва")\n# раз\n# два',
      },
    ],
  },
  {
    title: 'Списки',
    lead: 'Как создать список всеми обычными способами и как его менять.',
    sections: [
      {
        title: 'Пустой список',
        paragraphs: [
          'Список — упорядоченная коллекция, которую можно менять: добавлять, удалять и заменять элементы. Пустой список получают как `[]` или `list()`.',
        ],
        syntax: 'lst = []',
        example: 'a = []\nprint(a)           # []',
      },
      {
        title: 'Перечислить элементы',
        paragraphs: [
          'Элементы пишут в квадратных скобках через запятую. В одном списке могут лежать числа, строки и даже другие списки.',
        ],
        syntax: 'lst = [10, 20, 30]',
        example: 'a = [10, 20, 30]\nprint(a[0])        # 10',
      },
      {
        title: 'list из строки',
        paragraphs: [
          'Функция `list()` собирает список из другой последовательности. Из строки получаются символы, не целое слово.',
        ],
        syntax: 'list("abc")',
        example: 'print(list("кот"))\n# ["к", "о", "т"]',
        note: '`list("12")` — это `["1", "2"]`, а не `[12]`.',
      },
      {
        title: 'list из range',
        paragraphs: [
          'Из `range` получается список чисел. Правая граница в `range` не входит.',
        ],
        syntax: 'list(range(1, 5))',
        example: 'print(list(range(1, 4)))\n# [1, 2, 3]',
      },
      {
        title: 'Повтор [0] * n',
        paragraphs: [
          'Умножение `[0] * n` повторяет значение n раз. Так удобно завести список из нулей заданной длины.',
        ],
        syntax: '[0] * 4',
        example: 'print([0] * 4)\n# [0, 0, 0, 0]',
        note: '`[0] * 4` копирует число — так можно. А `[[0]] * 3` даёт три ссылки на один и тот же внутренний список. Для таблицы пишите `[[0] for _ in range(3)]`.',
      },
      {
        title: 'split',
        paragraphs: [
          '`split()` режет строку по пробелам и даёт список кусков. Каждый кусок — всё ещё строка, не число.',
        ],
        syntax: '"1 2 3".split()',
        example: 'print("1 2 3".split())\n# ["1", "2", "3"]',
      },
      {
        title: 'Чтение по индексу',
        paragraphs: [
          'Индекс `0` — первый элемент. `len(lst)` — длина списка.',
        ],
        syntax: 'lst[0]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[0])      # 10',
      },
      {
        title: 'Последний элемент',
        paragraphs: [
          'Индекс `-1` — последний элемент, как у строки.',
        ],
        syntax: 'lst[-1]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[-1])     # 40',
      },
      {
        title: 'Срез списка',
        paragraphs: [
          'У списка та же полная конструкция, что у строки: `start`, `stop` и `step`. Правая граница не входит. Результат — новый список, исходный не меняется.',
        ],
        syntax: 'lst[start:stop:step]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[1:3])    # [20, 30]',
      },
      {
        title: 'Шаг среза списка',
        paragraphs: [
          'Третий слот задаёт шаг. `lst[0:4:2]` берёт элементы через один.',
        ],
        syntax: 'lst[start:stop:step]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[0:4:2])  # [10, 30]',
      },
      {
        title: 'append',
        paragraphs: [
          '`append(x)` добавляет один элемент в конец. Список меняется на месте.',
        ],
        syntax: 'lst.append(x)',
        example: 'nums = [10, 20, 30]\nnums.append(40)\nprint(nums)         # [10, 20, 30, 40]',
      },
      {
        title: 'Замена по индексу',
        paragraphs: [
          '`lst[i] = x` заменяет элемент на месте. Новый список не создаётся.',
        ],
        syntax: 'lst[i] = x',
        example: 'nums = [10, 20, 30]\nnums[1] = 99\nprint(nums)         # [10, 99, 30]',
      },
      {
        title: 'pop',
        paragraphs: [
          '`pop()` удаляет последний элемент и возвращает его. Можно указать индекс: `pop(0)` снимет первый.',
        ],
        syntax: 'lst.pop()',
        example: 'nums = [10, 20, 30]\nlast = nums.pop()\nprint(last)         # 30\nprint(nums)         # [10, 20]',
      },
    ],
  },
  {
    title: 'Кортежи',
    lead: 'Неизменяемая последовательность: зачем нужна и как её создать.',
    sections: [
      {
        title: 'Обычный кортеж',
        paragraphs: [
          'Кортеж похож на список, но его нельзя менять: нет `append`, нельзя написать `t[0] = 1`. Так защищают набор, который не должен измениться: точку на плоскости, пару «имя и возраст».',
          'Обычный кортеж пишут в круглых скобках. Пустой — `()`.',
        ],
        syntax: 't = (10, 20, 30)',
        example: 'point = (3, 5)\nprint(point[0])       # 3',
      },
      {
        title: 'Один элемент: запятая',
        paragraphs: [
          'Кортеж из одного элемента обязательно содержит запятую: `(42,)`. Без запятой `(42)` — это просто число в скобках.',
        ],
        syntax: 't = (42,)',
        example: 'print((42,) == 42)    # False',
        note: 'Запятая важнее скобок. `(42)` — число, `(42,)` — кортеж.',
      },
      {
        title: 'tuple(...)',
        paragraphs: [
          'Из списка или строки кортеж собирают через `tuple(...)`.',
        ],
        syntax: 'tuple([10, 20])',
        example: 'print(tuple([10, 20]))\n# (10, 20)',
      },
      {
        title: 'Распаковка',
        paragraphs: [
          'Кортеж можно разобрать по переменным: слева столько имён, сколько элементов справа.',
        ],
        syntax: 'a, b = (10, 20)',
        example: 'lo, hi = (1, 8)\nprint(lo, hi)     # 1 8',
      },
      {
        title: 'return нескольких значений',
        paragraphs: [
          'Функция возвращает несколько значений через `return a, b`. На самом деле это кортеж. Его сразу распаковывают в переменные.',
        ],
        syntax: 'return a, b',
        example: 'def stats(nums):\n    return min(nums), max(nums)\n\nlo, hi = stats([3, 1, 8])\nprint(lo, hi)     # 1 8',
      },
    ],
  },
  {
    title: 'Словари',
    lead: 'Пары «ключ → значение»: создание, чтение и безопасный доступ.',
    sections: [
      {
        title: 'Пустой словарь',
        paragraphs: [
          'Словарь хранит пары «ключ → значение». К значению обращаются не по номеру, а по ключу. Пустой словарь — `{}` или `dict()`.',
        ],
        syntax: 'd = {}',
        example: 'd = {}\nprint(d)               # {}',
        note: '`{}` — это пустой словарь, не множество. Пустое множество создают только через `set()`.',
      },
      {
        title: 'Литерал',
        paragraphs: [
          'Пары перечисляют в фигурных скобках: ключ, двоеточие, значение. Ключ должен быть неизменяемым: строка, число или кортеж. Список ключом быть не может. Ключи уникальны: повторная запись затирает старое значение.',
        ],
        syntax: 'd = {"name": "Анна", "age": 25}',
        example: 'user = {"name": "Анна", "age": 25}\nprint(user["name"])     # Анна',
      },
      {
        title: 'dict(имя=...)',
        paragraphs: [
          '`dict(имя=значение)` подходит, если ключи — допустимые имена Python. Ключи станут строками.',
        ],
        syntax: 'dict(name="Анна", age=25)',
        example: 'd = dict(x=10, y=20)\nprint(d)               # {\'x\': 10, \'y\': 20}',
      },
      {
        title: 'Чтение d[key]',
        paragraphs: [
          '`d[key]` даёт значение. Если ключа нет, программа остановится с `KeyError`.',
        ],
        syntax: 'd[key]',
        example: 'user = {"name": "Анна"}\nprint(user["name"])     # Анна',
      },
      {
        title: 'Безопасное чтение get',
        paragraphs: [
          '`d.get(key)` возвращает `None`, если ключа нет, и не бросает ошибку. Второй аргумент — значение по умолчанию.',
        ],
        syntax: 'd.get(key, default)',
        example: 'user = {"name": "Анна"}\nprint(user.get("city", "нет"))    # нет',
      },
      {
        title: 'Запись',
        paragraphs: [
          '`d[key] = value` создаёт ключ или заменяет значение.',
        ],
        syntax: 'd[key] = value',
        example: 'user = {"name": "Анна"}\nuser["city"] = "Казань"\nprint(user["city"])     # Казань',
      },
      {
        title: 'Проверка in',
        paragraphs: [
          'Проверка `"name" in user` смотрит ключи, не значения. Обход пар: `for key, value in d.items()`.',
        ],
        syntax: 'key in d',
        example: 'user = {"name": "Анна"}\nprint("age" in user)    # False',
      },
    ],
  },
  {
    title: 'Множества',
    lead: 'Уникальные элементы без порядка и без индексов.',
    sections: [
      {
        title: 'Пустое множество',
        paragraphs: [
          'Множество хранит только уникальные элементы и не обещает порядок. Индексов нет: `s[0]` написать нельзя. Пустое множество — только `set()`.',
        ],
        syntax: 's = set()',
        example: 's = set()\nprint(s)               # set()',
        note: '`{}` создаёт словарь. Пустое множество нельзя записать как `{}`.',
      },
      {
        title: 'Литерал {1, 2, 3}',
        paragraphs: [
          'Литерал `{1, 2, 3}` уже множество, потому что внутри нет пар `ключ: значение`. Проверка `x in s` быстрая, дубликаты пропадают сами.',
        ],
        syntax: 's = {1, 2, 3}',
        example: 'print(len({1, 2, 2, 2}))  # 2',
      },
      {
        title: 'set из списка',
        paragraphs: [
          'Из списка множество собирают через `set(...)`. Повторы исчезают. Так удобно убирать дубликаты.',
        ],
        syntax: 'set([1, 1, 2, 3])',
        example: 'print(set([1, 1, 2]))     # {1, 2}',
      },
      {
        title: 'Объединение |',
        paragraphs: [
          '`|` даёт все элементы, которые есть хотя бы в одном из двух множеств.',
        ],
        syntax: 'a | b',
        example: 'a, b = {1, 2, 3}, {3, 4, 5}\nprint(a | b)     # {1, 2, 3, 4, 5}',
      },
      {
        title: 'Пересечение &',
        paragraphs: [
          '`&` оставляет только общие элементы.',
        ],
        syntax: 'a & b',
        example: 'a, b = {1, 2, 3}, {3, 4, 5}\nprint(a & b)     # {3}',
      },
      {
        title: 'Разность -',
        paragraphs: [
          '`a - b` — то, что есть в `a`, но нет в `b`.',
        ],
        syntax: 'a - b',
        example: 'a, b = {1, 2, 3}, {3, 4, 5}\nprint(a - b)     # {1, 2}',
      },
    ],
  },
  {
    title: 'Цикл while',
    lead: 'Повторять код, пока условие истинно.',
    sections: [
      {
        title: 'Когда берут while',
        paragraphs: [
          '`while` повторяет тело, пока условие истинно. Его используют, когда заранее неизвестно, сколько кругов понадобится: читать ввод до нуля, разбирать цифры числа, ждать условия. Если условие сразу ложно, тело не выполнится ни разу.',
        ],
        syntax: 'while условие:\n    тело_цикла',
        example: 'count = 1\nwhile count <= 5:\n    print(count)\n    count += 1',
        note: 'В теле должен быть шаг, который когда-нибудь сделает условие ложным. Иначе цикл станет бесконечным.',
      },
      {
        title: 'Цифры числа',
        paragraphs: [
          'Типичная задача: пока число больше нуля, брать последнюю цифру через `% 10` и отбрасывать её через `// 10`.',
        ],
        syntax: 'n = 123\nwhile n > 0:\n    digit = n % 10\n    n //= 10',
        example: 'n = 123\nwhile n > 0:\n    print(n % 10)    # 3, затем 2, затем 1\n    n //= 10',
      },
      {
        title: 'Бесконечный цикл и выход',
        paragraphs: [
          'Иногда пишут `while True` и выходят через `break`, когда пора остановиться. `continue` пропускает остаток текущего круга и возвращается к проверке условия.',
        ],
        syntax: 'while True:\n    ...\n    if пора_выйти:\n        break',
      },
    ],
  },
  {
    title: 'Цикл for и range',
    lead: 'Перебор элементов коллекции и последовательности чисел.',
    sections: [
      {
        title: 'for по списку',
        paragraphs: [
          '`for` берёт элементы по одному. Для списка переменная на каждом круге — очередной элемент, не индекс.',
        ],
        syntax: 'for элемент in список:\n    тело_цикла',
        example: 'for name in ["Анна", "Борис"]:\n    print(name)',
      },
      {
        title: 'for по строке',
        paragraphs: [
          'По строке цикл идёт по символам: на каждом круге один символ.',
        ],
        syntax: 'for символ in строка:\n    тело_цикла',
        example: 'for letter in "кот":\n    print(letter)',
      },
      {
        title: 'Повторить n раз',
        paragraphs: [
          'Когда нужно просто повторить код n раз, пишут `for i in range(n)`. Переменная `i` при этом часто не используется.',
        ],
        syntax: 'for i in range(n):',
        example: 'for i in range(3):\n    print("привет")',
      },
      {
        title: 'range(stop)',
        paragraphs: [
          '`range(stop)` даёт числа от `0` до `stop - 1`. Правая граница никогда не входит. Сам `range` — не список; список получают через `list(range(...))`.',
        ],
        syntax: 'range(stop)',
        example: 'print(list(range(5)))          # [0, 1, 2, 3, 4]',
      },
      {
        title: 'range(start, stop)',
        paragraphs: [
          'Два аргумента: начало и конец. Конец по-прежнему не входит.',
        ],
        syntax: 'range(start, stop)',
        example: 'print(list(range(1, 6)))       # [1, 2, 3, 4, 5]',
        note: '`range(1, 6)` — это 1, 2, 3, 4, 5. Шестёрки нет. Нужны числа от 1 до n: `range(1, n + 1)`.',
      },
      {
        title: 'range с шагом',
        paragraphs: [
          'Третий аргумент — шаг. Отрицательный шаг идёт назад: тогда начало должно быть больше конца.',
        ],
        syntax: 'range(start, stop, step)',
        example: 'print(list(range(1, 8, 2)))    # [1, 3, 5, 7]',
      },
      {
        title: 'enumerate',
        paragraphs: [
          'По индексам ходят так: `for i in range(len(items))`. Удобнее `enumerate`: он сразу даёт номер и элемент. Параметр `start` задаёт первый номер, по умолчанию `0`.',
        ],
        syntax: 'for i, item in enumerate(items, start=1):',
        example: 'names = ["Анна", "Борис"]\nfor i, name in enumerate(names, start=1):\n    print(i, name)     # 1 Анна, 2 Борис',
      },
    ],
  },
  {
    title: 'break, continue и else у цикла',
    lead: 'Выйти из цикла, пропустить круг или узнать, что break не случился.',
    sections: [
      {
        title: 'break и continue',
        paragraphs: [
          '`break` полностью прекращает цикл. `continue` пропускает только текущий круг и переходит к следующему элементу или к новой проверке `while`. Оба работают и в `for`, и в `while`.',
        ],
        syntax: 'for item in items:\n    if надо_пропустить:\n        continue\n    if надо_выйти:\n        break',
        example: 'for n in [1, 2, 3]:\n    if n % 2 == 0:\n        continue\n    print(n)     # 1 и 3, двойка пропущена',
        note: '`break` — выход из всего цикла. `continue` — «этот элемент не трогаем, берём следующий».',
      },
      {
        title: 'else у цикла',
        paragraphs: [
          '`else` после цикла выполняется, только если цикл дошёл до конца сам и `break` не срабатывал. Это не `else` у `if`: здесь ветка связана с циклом. Типичный приём: ищем элемент, при находке делаем `break`, в `else` пишем «не нашли».',
        ],
        syntax: 'for item in items:\n    if нашли:\n        break\nelse:\n    print("не нашли")',
        example: 'for n in [1, 3, 5]:\n    if n % 2 == 0:\n        print("нашли чётное")\n        break\nelse:\n    print("Чётных нет")',
      },
    ],
  },
  {
    title: 'Функции и return',
    lead: 'Как объявить функцию, передать аргументы и вернуть результат.',
    sections: [
      {
        title: 'Без аргументов',
        paragraphs: [
          'Функция — именованный блок кода, который можно вызывать много раз. Сначала её определяют через `def`, потом вызывают именем со скобками. Вызов без скобок не запускает код, а даёт сам объект функции.',
        ],
        syntax: 'def имя():\n    тело',
        example: 'def hello():\n    print("Привет")\n\nhello()               # Привет',
      },
      {
        title: 'Один аргумент',
        paragraphs: [
          'В скобках у `def` пишут параметр — имя, через которое внутрь попадёт значение. При вызове в скобках передают аргумент.',
        ],
        syntax: 'def имя(параметр):\n    return результат',
        example: 'def square(x):\n    return x * x\n\nprint(square(4))      # 16',
      },
      {
        title: 'Два аргумента',
        paragraphs: [
          'Параметров может быть несколько: их перечисляют через запятую. При вызове значения идут в том же порядке.',
        ],
        syntax: 'def имя(a, b):\n    return результат',
        example: 'def add(a, b):\n    return a + b\n\nprint(add(2, 3))      # 5',
      },
      {
        title: 'return одного значения',
        paragraphs: [
          '`return` отдаёт результат туда, откуда функцию вызвали, и сразу заканчивает работу функции. Если `return` нет, функция вернёт `None`.',
        ],
        syntax: 'return результат',
        example: 'def add(a, b):\n    return a + b\n\nprint(add(2, 3))      # 5',
        note: '`print()` показывает значение на экране. `return` возвращает его в программу. Это разные вещи.',
      },
      {
        title: 'return двух значений',
        paragraphs: [
          '`return a, b` возвращает кортеж. Его сразу распаковывают в переменные слева от `=`.',
        ],
        syntax: 'return a, b',
        example: 'def pair():\n    return 1, 2\n\nx, y = pair()\nprint(x, y)           # 1 2',
      },
      {
        title: 'Вызов с двумя аргументами',
        paragraphs: [
          'Параметру можно задать значение по умолчанию прямо в `def`. Если передать оба аргумента, значение по умолчанию не используется.',
        ],
        syntax: 'def add(a, b=0):\n    return a + b',
        example: 'def add(a, b=0):\n    return a + b\n\nprint(add(2, 3))     # 5',
        note: 'Не ставьте в значение по умолчанию изменяемый объект вроде `[]`: список будет общим для всех вызовов.',
      },
      {
        title: 'Вызов с одним аргументом',
        paragraphs: [
          'Если второй аргумент не передали, подставится значение из `def`. Здесь `b` станет `0`.',
        ],
        syntax: 'add(2)',
        example: 'def add(a, b=0):\n    return a + b\n\nprint(add(2))        # 2',
      },
    ],
  },
  {
    title: '*args и **kwargs',
    lead: 'Неизвестное число позиционных и именованных аргументов.',
    sections: [
      {
        title: '*args — лишние позиционные',
        paragraphs: [
          'Если функция должна принять сколько угодно значений без имён, пишут `*args`. Все лишние позиционные аргументы собираются в кортеж. Имя `args` — договорённость, работает сама звёздочка: можно написать `*numbers`.',
        ],
        syntax: 'def f(*args):\n    ...              # args — кортеж',
        example: 'def total(*nums):\n    return sum(nums)\n\nprint(total(1, 2, 3))     # 6',
        note: 'Так устроены `print()` и `min()`: они принимают сколько угодно значений.',
      },
      {
        title: '**kwargs — лишние именованные',
        paragraphs: [
          '`**kwargs` собирает лишние именованные аргументы в словарь. Ключи — имена, значения — то, что передали.',
        ],
        syntax: 'def f(**kwargs):\n    ...              # kwargs — словарь',
        example: 'def greet(name, **info):\n    print(name, info)\n\ngreet("Анна", city="Казань", age=25)\n# Анна {\'city\': \'Казань\', \'age\': 25}',
      },
      {
        title: 'Распаковка f(*args)',
        paragraphs: [
          'Звёздочка в вызове распаковывает список в отдельные аргументы. В `def` звёздочка упаковывает, в вызове — распаковывает.',
        ],
        syntax: 'f(*args)',
        example: 'def total(*nums):\n    return sum(nums)\n\nargs = [1, 2, 3]\nprint(total(*args))     # 6',
        note: '`f(args)` передаст один список. `f(*args)` передаст каждый элемент отдельно.',
      },
      {
        title: 'Ошибка: f(args) без звёздочки',
        paragraphs: [
          'Если передать список без `*`, функция получит один аргумент — весь список. Для `sum` это ещё сработает иначе, а для разбора по числам часто будет `TypeError`.',
        ],
        syntax: 'f(args)',
        example: 'def total(*nums):\n    return sum(nums)\n\nprint(total([1, 2, 3]))\n# TypeError: unsupported operand type(s)',
      },
    ],
  },
  {
    title: 'lambda',
    lead: 'Короткая функция в одну строку: слева аргументы, справа то, что вернуть.',
    sections: [
      {
        title: 'Что это такое',
        paragraphs: [
          '`lambda` — это маленькая функция без имени. Её пишут в одну строку. Слева от двоеточия — аргументы, справа — выражение, которое функция вернёт.',
          'Слово `return` здесь не пишут: результат выражения и есть ответ. Если нужен цикл, несколько строк или `print`, берите обычный `def`.',
        ],
        syntax: 'lambda аргументы: выражение',
        example: 'f = lambda x: x * 2\nprint(f(5))     # 10',
        note: '`f(5)` вызывает эту функцию так же, как обычную. Сначала записали `lambda` в имя `f`, потом вызвали со скобками.',
      },
      {
        title: 'Тот же смысл через def',
        paragraphs: [
          'Любую `lambda` можно переписать обычной функцией. Ниже обе делают одно и то же: берут число и умножают его на 2. Разница только в записи.',
        ],
        syntax: 'def double(x):\n    return x * 2',
        example: 'double = lambda x: x * 2\nprint(double(5))     # 10',
        note: 'Если логика длиннее одного выражения, сразу пишите `def`. `lambda` — только для короткого правила.',
      },
      {
        title: 'Один аргумент',
        paragraphs: [
          'Самый частый вид: один параметр слева от двоеточия. Справа с этим параметром считают ответ. Здесь `x` возводится в квадрат.',
        ],
        syntax: 'lambda x: x ** 2',
        example: 'square = lambda x: x ** 2\nprint(square(4))     # 16',
      },
      {
        title: 'Два аргумента',
        paragraphs: [
          'Аргументов может быть несколько — их перечисляют через запятую, как в `def`. Здесь функция складывает два числа.',
        ],
        syntax: 'lambda a, b: a + b',
        example: 'add = lambda a, b: a + b\nprint(add(2, 3))     # 5',
      },
      {
        title: 'Без аргументов',
        paragraphs: [
          'Если вход не нужен, слева от двоеточия ничего не пишут. При вызове скобки всё равно нужны: иначе функция не запустится.',
        ],
        syntax: 'lambda: выражение',
        example: 'hi = lambda: "привет"\nprint(hi())     # привет',
      },
    ],
  },
  {
    title: 'Генераторы коллекций',
    lead: 'Собрать список, словарь или множество в одну строку.',
    sections: [
      {
        title: 'Список без фильтра',
        paragraphs: [
          'Включение читается так: «положи выражение для каждого `x` из коллекции». Это короткий `for` с `append`. Это не генераторная функция с `yield`.',
        ],
        syntax: '[выражение for x in коллекция]',
        example: 'squares = [x ** 2 for x in range(1, 6)]\nprint(squares)     # [1, 4, 9, 16, 25]',
      },
      {
        title: 'Список с if',
        paragraphs: [
          'После коллекции можно написать `if`. В результат попадут только те `x`, для которых условие истинно.',
        ],
        syntax: '[выражение for x in коллекция if условие]',
        example: 'even = [x for x in range(10) if x % 2 == 0]\nprint(even)        # [0, 2, 4, 6, 8]',
      },
      {
        title: 'Словарь',
        paragraphs: [
          'Для словаря в фигурных скобках пишут пару `ключ: значение`.',
        ],
        syntax: '{ключ: значение for x in коллекция}',
        example: 'squares = {x: x ** 2 for x in range(4)}\nprint(squares)     # {0: 0, 1: 1, 2: 4, 3: 9}',
      },
      {
        title: 'Множество',
        paragraphs: [
          'Для множества в фигурных скобках одно выражение без двоеточия. Повторы исчезнут сами.',
        ],
        syntax: '{выражение for x in коллекция}',
        example: 'letters = {ch.lower() for ch in "AbA"}\nprint(letters)     # {"a", "b"}',
        note: '`[]` собирает список, `{ключ: значение}` — словарь, `{значение}` — множество. Круглые скобки `(x for x in ...)` дают генератор, который обходят один раз: это уже не список.',
      },
      {
        title: 'Когда включение не нужно',
        paragraphs: [
          'Если вы накапливаете одно число — сумму, произведение, максимум — новую коллекцию строить незачем. Пишите обычный цикл: `total += x`. Если строка с двумя `for` становится нечитаемой, тоже лучше обычный цикл.',
        ],
        syntax: 'total = 0\nfor x in nums:\n    total += x',
        example: 'total = 0\nfor x in [1, 2, 3]:\n    total += x\nprint(total)       # 6',
      },
    ],
  },
  {
    title: 'Исключения',
    lead: 'Как поймать ошибку и не дать программе упасть.',
    sections: [
      {
        title: 'Полная конструкция',
        paragraphs: [
          'Исключение — ошибка во время работы. Если её не поймать, программа остановится. Полный шаблон идёт строго в таком порядке: `try`, затем один или несколько `except`, затем необязательный `else`, затем необязательный `finally`.',
          '`except Тип as error` кладёт объект ошибки в переменную. Несколько `except` проверяются сверху вниз: более узкий тип пишите раньше.',
        ],
        syntax: 'try:\n    рискованный_код\nexcept ТипОшибки as error:\n    обработка\nexcept ДругойТип:\n    обработка\nelse:\n    если ошибки не было\nfinally:\n    выполнится всегда',
        example: 'try:\n    n = int("10")\nexcept ValueError as error:\n    print("не число", error)\nelse:\n    print("получилось", n)\nfinally:\n    print("конец попытки")',
        note: 'Ловите конкретные ошибки. Голый `except:` перехватит всё подряд, в том числе опечатки в коде.',
      },
      {
        title: 'try и except',
        paragraphs: [
          'Самый частый вид: рискованный ввод в `try` и отдельные обработчики на каждую ожидаемую ошибку. Здесь ловят нечисловой ввод и деление на ноль.',
        ],
        syntax: 'try:\n    рискованный_код\nexcept ТипОшибки:\n    обработка',
        example: 'try:\n    a = int(input())\n    b = int(input())\n    print(a / b)\nexcept ValueError:\n    print("Введите числа")\nexcept ZeroDivisionError:\n    print("На ноль нельзя")',
      },
      {
        title: 'ValueError',
        paragraphs: [
          '`ValueError` — значение не того вида. Часто ловят при `int(...)` и `float(...)`: человек ввёл `"abc"` вместо числа.',
        ],
        syntax: 'except ValueError:',
        example: 'try:\n    int("abc")\nexcept ValueError:\n    print("не число")',
      },
      {
        title: 'ZeroDivisionError',
        paragraphs: [
          '`ZeroDivisionError` — деление на ноль. Проверяйте, если делитель приходит из ввода или из расчёта.',
        ],
        syntax: 'except ZeroDivisionError:',
        example: 'try:\n    print(1 / 0)\nexcept ZeroDivisionError:\n    print("на ноль нельзя")',
      },
      {
        title: 'IndexError',
        paragraphs: [
          '`IndexError` — нет такого индекса у строки, списка или кортежа. Стоит ловить, если индекс считают, а не берут константой.',
        ],
        syntax: 'except IndexError:',
        example: 'try:\n    print([1, 2][5])\nexcept IndexError:\n    print("нет такого индекса")',
      },
      {
        title: 'KeyError',
        paragraphs: [
          '`KeyError` — в словаре нет такого ключа. Если ключ может отсутствовать, либо ловите ошибку, либо берите `.get()`.',
        ],
        syntax: 'except KeyError:',
        example: 'try:\n    print({"a": 1}["b"])\nexcept KeyError:\n    print("нет такого ключа")',
      },
      {
        title: 'TypeError',
        paragraphs: [
          '`TypeError` — операция не для этого типа: сложили число со строкой, вызвали `len` у числа, передали не тот аргумент.',
        ],
        syntax: 'except TypeError:',
        example: 'try:\n    print(len(25))\nexcept TypeError:\n    print("у числа нет длины")',
      },
      {
        title: 'NameError',
        paragraphs: [
          '`NameError` — имя не определено: опечатка в переменной или обращение до присваивания.',
        ],
        syntax: 'except NameError:',
        example: 'try:\n    print(unknown)\nexcept NameError:\n    print("имя не определено")',
      },
      {
        title: 'else',
        paragraphs: [
          '`else` у `try` выполняется, только если в `try` не было ошибки. Сюда ставят код, которому нужен успешный результат.',
        ],
        syntax: 'try:\n    код\nexcept ТипОшибки:\n    обработка\nelse:\n    если ошибки не было',
        example: 'try:\n    n = int("10")\nexcept ValueError:\n    print("не число")\nelse:\n    print("получилось", n)',
      },
      {
        title: 'finally',
        paragraphs: [
          '`finally` выполняется всегда: и при ошибке, и без неё. Так закрывают файл или пишут завершающее сообщение.',
        ],
        syntax: 'try:\n    код\nfinally:\n    выполнится всегда',
        example: 'try:\n    n = int("10")\nfinally:\n    print("конец попытки")',
      },
      {
        title: 'raise',
        paragraphs: [
          '`raise` создаёт исключение вручную, когда данные сами по себе некорректны.',
        ],
        syntax: 'raise ValueError("сообщение")',
        example: 'age = -1\nif age < 0:\n    raise ValueError("возраст не может быть отрицательным")',
      },
    ],
  },
];

const classes = [
  {
    title: 'Класс',
    lead: 'Класс описывает новый тип данных: какие значения хранит объект и какие действия с ними выполняет.',
    sections: [
      {
        title: 'Что описывает class',
        paragraphs: [
          'Класс — это описание, а экземпляр — конкретное значение, созданное по этому описанию. Например, `User` задаёт правила для пользователей, а `anna` хранит данные одного пользователя.',
          'После `class` пишут имя в стиле `PascalCase`. В теле размещают атрибуты и методы. Пустое тело можно обозначить `pass`, но в рабочем классе обычно сразу задают начальные данные и поведение.',
        ],
        useWhen: [
          'Несколько сущностей имеют одинаковый набор данных и действий.',
          'Нужно хранить состояние между вызовами методов.',
        ],
        steps: [
          'Назовите сущность существительным: `User`, `Order`, `Point`.',
          'Определите данные объекта и действия над ними.',
          'Создайте экземпляр вызовом класса.',
        ],
        mistakes: [
          'Создавать класс ради одной функции без состояния.',
          'Называть класс глаголом или со строчной буквы.',
        ],
        syntax: 'class ClassName:\n    def method(self):\n        ...',
        example: 'class Lamp:\n    def turn_on(self):\n        return "Лампа включена"\n\nlamp = Lamp()\nprint(lamp.turn_on())\n# Лампа включена',
        template: 'class Thing:\n    def action(self):\n        return "результат"',
        note: 'Критерий выбора: берите класс, когда данные и работающие с ними функции образуют одну сущность. Для независимого вычисления достаточно функции.',
      },
      {
        title: 'Класс и экземпляр — разные уровни',
        paragraphs: [
          'Имя `Lamp` ссылается на сам класс. Выражение `Lamp()` создаёт экземпляр этого класса. Один класс может создать сколько угодно независимых экземпляров.',
          '`isinstance` удобно использовать на границе программы, когда нужно проверить тип полученного объекта.',
        ],
        syntax: 'object = ClassName()\nisinstance(object, ClassName)',
        example: 'class Lamp:\n    pass\n\nlamp = Lamp()\nprint(isinstance(lamp, Lamp))\nprint(lamp is Lamp)\n# True\n# False',
        note: 'Класс — фабрика и набор правил; экземпляр — конкретный объект с собственным состоянием.',
      },
    ],
  },
  {
    title: 'Объект (экземпляр)',
    lead: 'Объект, или экземпляр, — конкретный результат вызова класса со своими данными.',
    sections: [
      {
        title: 'Создание независимых объектов',
        paragraphs: [
          'Класс вызывают как функцию: `Counter(5)`. Python создаёт объект, настраивает его и возвращает ссылку. Переменная хранит эту ссылку, а не копию класса.',
          'Два вызова создают два объекта. Изменение атрибута одного объекта не меняет другой, если данные не были ошибочно размещены в общем атрибуте класса.',
        ],
        useWhen: [
          'Нужны несколько значений одного типа с разным состоянием.',
          'Объект должен помнить изменения между вызовами.',
        ],
        steps: [
          'Вызовите класс и передайте нужные начальные значения.',
          'Сохраните объект в переменную.',
          'Обращайтесь к данным и методам через точку.',
        ],
        mistakes: [
          'Считать разные переменные разными объектами: `b = a` создаёт вторую ссылку на тот же объект.',
          'Сравнивать идентичность через `is`, когда нужны равные значения.',
        ],
        syntax: 'first = ClassName(...)\nsecond = ClassName(...)',
        example: 'class Counter:\n    def __init__(self, value=0):\n        self.value = value\n\nfirst = Counter(5)\nsecond = Counter(5)\nfirst.value += 1\nprint(first.value, second.value)\nprint(first is second)\n# 6 5\n# False',
        template: 'item = ClassName(...)\nitem.method()',
        note: 'Критерий выбора: создавайте отдельный экземпляр для каждой сущности, состояние которой должно изменяться независимо.',
      },
    ],
  },
  {
    title: 'self',
    lead: '`self` — ссылка на экземпляр, для которого сейчас выполняется метод.',
    sections: [
      {
        title: 'Как объект попадает в метод',
        paragraphs: [
          'В определении обычного метода первым параметром пишут `self`. При вызове `account.deposit(50)` Python сам передаёт `account` в этот параметр.',
          'Через `self` метод читает и меняет атрибуты именно текущего объекта, а также вызывает его другие методы.',
        ],
        useWhen: [
          'Методу нужны данные конкретного экземпляра.',
          'Метод изменяет состояние или вызывает другой метод объекта.',
        ],
        steps: [
          'Добавьте `self` первым параметром метода.',
          'Используйте `self.attribute` вместо случайной локальной переменной.',
          'При обычном вызове не передавайте `self` вручную.',
        ],
        mistakes: [
          'Забыть `self` в сигнатуре и получить ошибку о лишнем аргументе.',
          'Написать `balance += amount` вместо `self.balance += amount`.',
        ],
        syntax: 'def method(self, value):\n    self.attribute = value',
        example: 'class Account:\n    def __init__(self, balance):\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n\naccount = Account(100)\naccount.deposit(50)\nAccount.deposit(account, 25)\nprint(account.balance)\n# 175',
        template: 'def method(self, argument):\n    self.attribute = argument',
        note: 'Критерий выбора: нужен текущий объект — обычный метод с `self`; нужен класс — `classmethod`; не нужен ни один — `staticmethod` или отдельная функция.',
      },
    ],
  },
  {
    title: 'Конструктор __init__',
    lead: '`__init__` инициализирует уже созданный экземпляр начальными данными; он необязателен.',
    sections: [
      {
        title: 'Создание и инициализация',
        paragraphs: [
          'При вызове класса Python сначала создаёт объект, затем вызывает `__init__` для настройки его атрибутов. Поэтому точная формулировка такая: `__init__` инициализирует объект, а не создаёт его.',
          '`__init__` необязателен: если начальные данные не нужны, класс работает и без него. Низкоуровневое создание выполняет `__new__`; переопределять его в обычных прикладных классах почти никогда не требуется.',
        ],
        useWhen: [
          'Объекту нужны обязательные или начальные значения.',
          'Нужно сразу привести входные данные к нужному виду.',
        ],
        steps: [
          'Поставьте обязательные параметры перед параметрами со значениями по умолчанию.',
          'Сохраните входные значения через `self`.',
          'Создавайте личные списки и словари внутри `__init__`.',
        ],
        mistakes: [
          'Возвращать значение из `__init__`: он должен вернуть `None`.',
          'Считать `__init__` обязательным или вызывать его вручную.',
          'Использовать общий список вместо нового списка для каждого объекта.',
        ],
        syntax: 'def __init__(self, required, optional=default):\n    self.required = required\n    self.optional = optional',
        example: 'class Task:\n    def __init__(self, title, done=False):\n        self.title = title\n        self.done = done\n        self.tags = []\n\ntask = Task("Изучить классы")\nprint(task.title, task.done, task.tags)\n# Изучить классы False []',
        template: 'class Item:\n    def __init__(self, name, active=True):\n        self.name = name\n        self.active = active',
        note: 'Критерий выбора: добавляйте `__init__`, когда экземпляр должен сразу получить корректное состояние. `__new__` оставьте для редких задач управления самим созданием.',
      },
    ],
  },
  {
    title: 'Атрибуты класса и экземпляра',
    lead: 'Атрибут экземпляра хранит личное значение, атрибут класса — общее значение и значение по умолчанию.',
    sections: [
      {
        title: 'Где Python ищет атрибут',
        paragraphs: [
          'При чтении `user.role` Python сначала ищет `role` у экземпляра, затем у его класса и базовых классов. Если ничего не найдено, возникает `AttributeError`.',
          'Присваивание `user.role = ...` создаёт или меняет личный атрибут и перекрывает одноимённый атрибут класса только для этого объекта.',
        ],
        useWhen: [
          'Экземпляр: имя, баланс, текущий статус и другие личные данные.',
          'Класс: константа, общий счётчик или настройка для всех экземпляров.',
        ],
        steps: [
          'Личные данные записывайте через `self`.',
          'Общие значения объявляйте в теле класса.',
          'Общий атрибут меняйте через класс, чтобы случайно не создать личную копию.',
        ],
        mistakes: [
          'Класть изменяемый список или словарь в атрибут класса, когда он должен быть личным.',
          'Менять общий счётчик через `self.count += 1` и тем самым создавать личный атрибут.',
        ],
        syntax: 'class User:\n    role = "guest"\n\n    def __init__(self, name):\n        self.name = name',
        example: 'class User:\n    role = "guest"\n\n    def __init__(self, name):\n        self.name = name\n\nanna = User("Анна")\nboris = User("Борис")\nanna.role = "admin"\nprint(anna.role, boris.role, User.role)\n# admin guest guest',
        template: 'class Item:\n    shared = "общее"\n\n    def __init__(self, personal):\n        self.personal = personal',
        note: 'Критерий выбора: значение различается у объектов — атрибут экземпляра; едино для типа целиком — атрибут класса.',
      },
    ],
  },
  {
    title: 'Методы экземпляра',
    lead: 'Метод экземпляра получает `self` и выполняет действие с состоянием конкретного объекта.',
    sections: [
      {
        title: 'Команды и вычисления объекта',
        paragraphs: [
          'Метод экземпляра — обычный `def` в теле класса с первым параметром `self`. Он может менять состояние объекта или вычислять результат по его данным.',
          'Метод-команда обычно меняет объект. Метод-запрос возвращает результат через `return`. Печать внутри вычисляющего метода ограничивает повторное использование результата.',
        ],
        useWhen: [
          'Действию нужны атрибуты конкретного объекта.',
          'Правило изменения данных должно всегда выполняться одинаково.',
        ],
        steps: [
          'Назовите метод действием: `deposit`, `close`, `total`.',
          'Проверьте аргументы до изменения состояния.',
          'Верните вычисленное значение, если оно нужно вызывающему коду.',
        ],
        mistakes: [
          'Забыть `return` и неожиданно получить `None`.',
          'Менять атрибут до проверки аргумента.',
          'Передавать объект отдельным аргументом при вызове через точку.',
        ],
        syntax: 'def action(self, value):\n    if value_is_invalid:\n        raise ValueError("...")\n    self.attribute = value',
        example: 'class Wallet:\n    def __init__(self, balance=0):\n        self.balance = balance\n\n    def deposit(self, amount):\n        if amount <= 0:\n            raise ValueError("Сумма должна быть положительной")\n        self.balance += amount\n\n    def total(self):\n        return self.balance\n\nwallet = Wallet(100)\nwallet.deposit(50)\nprint(wallet.total())\n# 150',
        template: 'def method(self, value):\n    if value <= 0:\n        raise ValueError("Некорректное значение")\n    self.attribute += value',
        note: 'Критерий выбора: если действие зависит от состояния экземпляра, это метод экземпляра; иначе проверьте, нужен ли `classmethod`, `staticmethod` или обычная функция.',
      },
    ],
  },
  {
    title: 'Магические методы',
    lead: 'Dunder-методы связывают объект со стандартным синтаксисом Python: печатью, сравнением, длиной и операторами.',
    sections: [
      {
        title: 'Стандартные операции без ручных вызовов',
        paragraphs: [
          'Имена вида `__str__` называют dunder-методами. Обычно их не вызывают напрямую: `print(book)` запускает `book.__str__()`, а `left == right` запускает `left.__eq__(right)`.',
          'Добавляйте только те операции, смысл которых очевиден для сущности. Книга может иметь понятное строковое представление и сравнение, но операция сложения книг, скорее всего, запутает читателя.',
        ],
        useWhen: [
          '`__str__` — понятный текст для пользователя.',
          '`__repr__` — однозначный вид для разработчика и логов.',
          '`__eq__` — равенство по данным, а не по идентичности.',
        ],
        steps: [
          'Выберите стандартную операцию с ясным смыслом.',
          'Верните значение требуемого типа: строку из `__str__`, bool или `NotImplemented` из `__eq__`.',
          'Проверьте тип второго операнда перед чтением его атрибутов.',
        ],
        mistakes: [
          'Возвращать `print(...)` вместо значения.',
          'В `__eq__` обращаться к `other.title` без проверки типа.',
          'Возвращать `False` для неподдерживаемого типа вместо `NotImplemented`.',
        ],
        syntax: 'def __eq__(self, other):\n    if not isinstance(other, type(self)):\n        return NotImplemented\n    return self.value == other.value',
        example: 'class Book:\n    def __init__(self, title):\n        self.title = title\n\n    def __str__(self):\n        return f"Книга: {self.title}"\n\n    def __eq__(self, other):\n        if not isinstance(other, Book):\n            return NotImplemented\n        return self.title == other.title\n\nbook = Book("Мы")\nprint(book)\nprint(book == Book("Мы"))\nprint(book == "Мы")\n# Книга: Мы\n# True\n# False',
        template: 'def __str__(self):\n    return f"ClassName({self.attribute})"\n\ndef __eq__(self, other):\n    if not isinstance(other, ClassName):\n        return NotImplemented\n    return self.attribute == other.attribute',
        note: 'Критерий выбора: реализуйте dunder-метод, только если стандартная операция естественна для объекта. Для обычного действия лучше именованный метод.',
      },
      {
        title: '__str__ и __repr__ — два вида текста',
        paragraphs: [
          '`__str__` возвращает удобный текст для пользователя: его вызывают `print(obj)` и `str(obj)`. `__repr__` нужен разработчику: его используют `repr(obj)`, отладчик и отображение объектов внутри списка.',
          'Оба метода обязаны вернуть строку. Полезный `__repr__` показывает имя класса и основные поля, чтобы по результату было понятно, какой объект перед вами.',
        ],
        useWhen: [
          '`__str__` — объект показывают пользователю.',
          '`__repr__` — объект попадает в логи, консоль или коллекции.',
        ],
        mistakes: [
          'Печатать внутри метода вместо `return`.',
          'Возвращать пароль, токен или другие секретные поля.',
          'Делать `__repr__` слишком длинным и нечитабельным.',
        ],
        syntax: 'def __str__(self):\n    return "понятный текст"\n\ndef __repr__(self):\n    return "ClassName(...)"',
        example: 'class Book:\n    def __init__(self, title):\n        self.title = title\n\n    def __str__(self):\n        return f"Книга «{self.title}»"\n\n    def __repr__(self):\n        return f"Book({self.title!r})"\n\nbook = Book("Мы")\nprint(book)\nprint([book])\n# Книга «Мы»\n# [Book(\'Мы\')]',
        note: 'Если `__str__` не определён, `str(obj)` использует `__repr__`. Обратного правила нет: список всегда показывает элементы через `repr`.',
      },
      {
        title: '__len__, __iter__ и __contains__ — поведение коллекции',
        paragraphs: [
          '`len(obj)` вызывает `__len__`, цикл `for` получает итератор через `__iter__`, а `value in obj` использует `__contains__`. Обычно эти методы делегируют работу внутренней коллекции.',
          'Так объект можно использовать обычным синтаксисом Python, не раскрывая наружу список, в котором он хранит элементы.',
        ],
        useWhen: [
          'Объект по смыслу является контейнером или коллекцией.',
          'Нужно поддержать `len`, `for` или `in`.',
        ],
        steps: [
          '`__len__` возвращает целое неотрицательное число.',
          '`__iter__` возвращает итератор, например `iter(self.items)`.',
          '`__contains__` возвращает результат проверки принадлежности.',
        ],
        mistakes: [
          'Возвращать сам список из `__iter__` вместо итератора.',
          'Добавлять интерфейс коллекции объекту, который коллекцией не является.',
        ],
        syntax: 'def __len__(self):\n    return len(self.items)\n\ndef __iter__(self):\n    return iter(self.items)\n\ndef __contains__(self, item):\n    return item in self.items',
        example: 'class Playlist:\n    def __init__(self, tracks):\n        self._tracks = list(tracks)\n\n    def __len__(self):\n        return len(self._tracks)\n\n    def __iter__(self):\n        return iter(self._tracks)\n\n    def __contains__(self, track):\n        return track in self._tracks\n\nplaylist = Playlist(["Intro", "Finale"])\nprint(len(playlist))\nprint("Finale" in playlist)\nprint(list(playlist))\n# 2\n# True\n# [\'Intro\', \'Finale\']',
        template: 'def __len__(self):\n    return len(self._items)\n\ndef __iter__(self):\n    return iter(self._items)',
      },
      {
        title: 'Операторы: __add__ и сравнения',
        paragraphs: [
          'Выражение `left + right` вызывает `left.__add__(right)`, а `<` вызывает `__lt__`. Метод проверяет второй операнд и обычно возвращает новый объект, не изменяя исходные.',
          'Если операция с переданным типом не поддерживается, возвращайте `NotImplemented`. Тогда Python сможет попробовать отражённую операцию второго объекта или выдать корректный `TypeError`.',
        ],
        useWhen: [
          'Оператор имеет однозначный смысл для вашей сущности.',
          'Такой синтаксис делает код короче и понятнее именованного метода.',
        ],
        mistakes: [
          'Неожиданно изменять левый объект внутри `__add__`.',
          'Придумывать оператору смысл, который нельзя угадать.',
          'Читать атрибуты второго операнда без проверки типа.',
        ],
        syntax: 'def __add__(self, other):\n    if not isinstance(other, type(self)):\n        return NotImplemented\n    return type(self)(self.value + other.value)',
        example: 'class Money:\n    def __init__(self, amount):\n        self.amount = amount\n\n    def __add__(self, other):\n        if not isinstance(other, Money):\n            return NotImplemented\n        return Money(self.amount + other.amount)\n\n    def __repr__(self):\n        return f"Money({self.amount})"\n\nfirst = Money(100)\ntotal = first + Money(50)\nprint(first, total)\n# Money(100) Money(150)',
        note: 'Для изменения объекта на месте существуют отдельные методы вроде `__iadd__`, но в обычном классе безопаснее сначала возвращать новый объект.',
      },
    ],
  },
  {
    title: '@property',
    lead: '`property` сохраняет удобный доступ через точку, но выполняет код при чтении или записи значения.',
    sections: [
      {
        title: 'Проверка любой записи, включая начальную',
        paragraphs: [
          'Геттер с `@property` выполняется при чтении `product.price`, сеттер — при записи `product.price = value`. Реальное значение обычно хранится в `_price`, чтобы сеттер не вызывал сам себя.',
          'В `__init__` нужно писать `self.price = price`, а не `self._price = price`. Тогда начальное значение проходит через тот же сеттер и не обходит валидацию.',
        ],
        useWhen: [
          'Запись значения требует проверки или преобразования.',
          'Значение вычисляется из других атрибутов.',
          'Нужно сохранить внешний интерфейс `obj.attr` при добавлении логики.',
        ],
        steps: [
          'Создайте геттер с именем публичного атрибута.',
          'Добавьте `@name.setter` с проверкой.',
          'Внутри сеттера сохраните значение в атрибут с подчёркиванием.',
          'В `__init__` присвойте публичное свойство, чтобы запустить проверку.',
        ],
        mistakes: [
          'В сеттере писать `self.price = value` и получить бесконечную рекурсию.',
          'В `__init__` записать `_price` напрямую и пропустить проверку.',
          'Добавлять property к простому полю без логики.',
        ],
        syntax: '@property\ndef price(self):\n    return self._price\n\n@price.setter\ndef price(self, value):\n    if value < 0:\n        raise ValueError("...")\n    self._price = value',
        example: 'class Product:\n    def __init__(self, price):\n        self.price = price\n\n    @property\n    def price(self):\n        return self._price\n\n    @price.setter\n    def price(self, value):\n        if value < 0:\n            raise ValueError("Цена не может быть отрицательной")\n        self._price = value\n\nproduct = Product(100)\nproduct.price = 120\nprint(product.price)\ntry:\n    Product(-1)\nexcept ValueError as error:\n    print(error)\n# 120\n# Цена не может быть отрицательной',
        template: '@property\ndef attribute(self):\n    return self._attribute\n\n@attribute.setter\ndef attribute(self, value):\n    if value_is_invalid:\n        raise ValueError("Некорректное значение")\n    self._attribute = value',
        note: 'Критерий выбора: используйте property, когда доступ выглядит как работа со значением, но внутри нужна короткая проверка или вычисление. Сложное действие оформляйте методом.',
      },
    ],
  },
  {
    title: '@classmethod',
    lead: '`classmethod` получает класс в параметре `cls` и подходит для альтернативного создания объектов и работы с общими данными.',
    sections: [
      {
        title: 'Метод, которому нужен класс',
        paragraphs: [
          'После `@classmethod` первым параметром указывают `cls`. Python передаёт туда класс, через который вызван метод, поэтому экземпляр заранее не нужен.',
          'Практический случай — альтернативный конструктор `from_string`: он преобразует другой формат входных данных и вызывает `cls(...)`. Основные правила инициализации при этом остаются в одном `__init__`.',
        ],
        useWhen: [
          'Нужен способ создания `from_string`, `from_dict` или похожий.',
          'Метод читает или меняет атрибут класса.',
        ],
        steps: [
          'Поставьте декоратор `@classmethod`.',
          'Назовите первый параметр `cls`.',
          'Для создания верните `cls(...)`, а не жёстко указанное имя класса.',
        ],
        mistakes: [
          'Использовать `self` и ждать доступ к конкретному объекту.',
          'Писать `Person(...)` вместо `cls(...)` и ломать корректную работу подклассов.',
          'Дублировать проверки из `__init__`.',
        ],
        syntax: '@classmethod\ndef from_string(cls, text):\n    ...\n    return cls(...)',
        example: 'class Person:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    @classmethod\n    def from_string(cls, text):\n        name, age = text.split(",")\n        return cls(name, int(age))\n\nperson = Person.from_string("Анна,30")\nprint(person.name, person.age)\n# Анна 30',
        template: '@classmethod\ndef from_value(cls, value):\n    prepared = prepare(value)\n    return cls(prepared)',
        note: 'Критерий выбора: нужен сам класс — `classmethod`; нужен готовый экземпляр — обычный метод; класс и объект не нужны — `staticmethod` или функция.',
      },
    ],
  },
  {
    title: '@staticmethod',
    lead: '`staticmethod` — функция внутри пространства имён класса, которая не получает ни `self`, ни `cls`.',
    sections: [
      {
        title: 'Связанная по смыслу проверка',
        paragraphs: [
          'Статический метод получает только явно переданные аргументы. Его размещают в классе, когда функция тесно связана с его предметной областью, но не читает состояние объекта или класса.',
          'Например, проверка формата артикула полезна рядом с `Product`. Если функция полезна многим несвязанным классам, понятнее оставить её на уровне модуля.',
        ],
        useWhen: [
          'Функция относится к классу по смыслу.',
          'Вычислению не нужны атрибуты экземпляра и класса.',
        ],
        steps: [
          'Поставьте `@staticmethod`.',
          'Не добавляйте `self` или `cls`.',
          'Передайте все зависимости обычными аргументами.',
        ],
        mistakes: [
          'Обращаться внутри к `self.attribute` или `cls.attribute`.',
          'Делать статическим метод, которому вскоре понадобится состояние объекта.',
          'Собирать случайные функции в класс `Utils` без общей предметной области.',
        ],
        syntax: '@staticmethod\ndef validate(value):\n    return condition',
        example: 'class Product:\n    @staticmethod\n    def is_valid_sku(value):\n        return len(value) == 6 and value.isalnum()\n\nprint(Product.is_valid_sku("A12B34"))\nprint(Product.is_valid_sku("bad!"))\n# True\n# False',
        template: '@staticmethod\ndef helper(value):\n    return result',
        note: 'Критерий выбора: нужны данные объекта — instance method; данные класса — classmethod; нужна только связанная по смыслу функция — staticmethod.',
      },
    ],
  },
  {
    title: 'Конструкция класса',
    lead: 'Рабочий класс собирают от требований к данным и правилам, а затем добавляют только нужные возможности.',
    sections: [
      {
        title: 'Практический порядок сборки',
        paragraphs: [
          'Сначала перечислите обязательные данные и ограничения. Затем напишите `__init__`, свойства с проверками и основные методы. Dunder-, class- и static-методы добавляйте только под реальный сценарий.',
          'Обычный читаемый порядок: атрибуты класса, `__init__`, свойства, dunder-методы, classmethod, staticmethod, методы экземпляра. Python не требует именно такого порядка, но команде легче читать одинаково устроенные классы.',
        ],
        useWhen: [
          'Нужна сущность с состоянием, правилами изменения и понятным API.',
          'Нужно превратить разрозненные переменные и функции в одну модель.',
        ],
        steps: [
          'Опишите обязательные поля и допустимые значения.',
          'Проведите начальные значения через те же проверки, что и последующие.',
          'Добавьте поведение короткими методами с одной ответственностью.',
          'Проверьте создание, изменение, ошибочный ввод, печать и сравнение.',
        ],
        mistakes: [
          'Добавлять все возможные методы заранее.',
          'Дублировать валидацию в нескольких местах.',
          'Смешивать расчёты, печать и изменение состояния в одном методе.',
        ],
        syntax: 'class Model:\n    class_attribute = value\n\n    def __init__(self, ...): ...\n    @property\n    def field(self): ...\n    def __str__(self): ...\n    def action(self): ...',
        example: 'class Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\n    def distance_squared(self):\n        return self.x ** 2 + self.y ** 2\n\npoint = Point(3, 4)\nprint(point.distance_squared())\n# 25',
        note: 'Критерий выбора: класс готов, когда поддерживает требуемые сценарии и защищает свои правила; количество методов само по себе не показатель качества.',
      },
      {
        title: '@dataclass для классов с данными',
        paragraphs: [
          '`@dataclass` из модуля `dataclasses` автоматически создаёт `__init__`, понятный `__repr__` и сравнение `__eq__` по полям. Типы полей записывают в теле класса.',
          'Он удобен для простых моделей данных. Для изменяемых значений используйте `field(default_factory=list)`, чтобы каждый экземпляр получил свой список. Проверки после автоматического `__init__` можно выполнить в `__post_init__`.',
        ],
        useWhen: [
          'Класс в основном хранит несколько полей.',
          'Нужны стандартные создание, печать и сравнение без шаблонного кода.',
        ],
        mistakes: [
          'Писать `tags: list = []` и получить общий изменяемый список.',
          'Ждать автоматической проверки типов: аннотации сами данные не проверяют.',
          'Использовать dataclass для класса со сложным жизненным циклом только ради сокращения кода.',
        ],
        syntax: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Item:\n    name: str\n    tags: list[str] = field(default_factory=list)',
        example: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    score: int = 0\n    courses: list[str] = field(default_factory=list)\n\n    def __post_init__(self):\n        if not 0 <= self.score <= 100:\n            raise ValueError("Оценка должна быть от 0 до 100")\n\nstudent = Student("Анна", 90)\nstudent.courses.append("Python")\nprint(student)\n# Student(name=\'Анна\', score=90, courses=[\'Python\'])',
        template: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Model:\n    name: str\n    items: list[str] = field(default_factory=list)',
        note: 'Критерий выбора: dataclass — для модели, где главное поля; обычный класс — когда главное поведение, точный контроль инициализации или нестандартный интерфейс.',
      },
      {
        title: 'Итоговый рабочий класс',
        paragraphs: [
          'Этот пример объединяет личные и общие атрибуты, проверку начальной цены через setter, методы трёх видов и безопасное сравнение. Каждая часть решает конкретную задачу.',
          'Класс можно запустить целиком. Он создаёт товар из строки, пополняет остаток, считает стоимость и корректно сравнивает объекты.',
        ],
        useWhen: [
          'Нужен ориентир для прикладного класса с валидацией.',
          'Нужно увидеть изученные элементы в одном небольшом примере.',
        ],
        steps: [
          'Создайте объект обычным способом или через `from_string`.',
          'Меняйте состояние через методы и property.',
          'Используйте стандартные операции только с реализованным смыслом.',
        ],
        mistakes: [
          'Копировать весь шаблон, если задаче нужна только часть.',
          'Обходить property прямой записью `_price` снаружи.',
          'Сравнивать с чужим типом без возврата `NotImplemented`.',
        ],
        syntax: 'product = Product.from_string("Молоко,80,2")\nproduct.add_stock(3)\nprint(product.total())',
        example: 'class Product:\n    currency = "руб."\n\n    def __init__(self, name, price, quantity=0):\n        self.name = name\n        self.price = price\n        self.quantity = quantity\n\n    @property\n    def price(self):\n        return self._price\n\n    @price.setter\n    def price(self, value):\n        if value < 0:\n            raise ValueError("Цена не может быть отрицательной")\n        self._price = value\n\n    def __str__(self):\n        return f"{self.name}: {self.price} {self.currency}, остаток {self.quantity}"\n\n    def __eq__(self, other):\n        if not isinstance(other, Product):\n            return NotImplemented\n        return (self.name, self.price) == (other.name, other.price)\n\n    @classmethod\n    def from_string(cls, text):\n        name, price, quantity = text.split(",")\n        return cls(name, float(price), int(quantity))\n\n    @staticmethod\n    def is_valid_name(value):\n        return isinstance(value, str) and bool(value.strip())\n\n    def add_stock(self, amount):\n        if amount <= 0:\n            raise ValueError("Количество должно быть положительным")\n        self.quantity += amount\n\n    def total(self):\n        return self.price * self.quantity\n\nproduct = Product.from_string("Молоко,80,2")\nproduct.add_stock(3)\nprint(product)\nprint(product.total())\nprint(product == Product("Молоко", 80, 99))\n# Молоко: 80.0 руб., остаток 5\n# 400.0\n# True',
        template: 'class Model:\n    shared = value\n\n    def __init__(self, field):\n        self.field = field\n\n    @property\n    def field(self):\n        return self._field\n\n    @field.setter\n    def field(self, value):\n        if invalid(value):\n            raise ValueError("Некорректное значение")\n        self._field = value\n\n    def action(self):\n        return result',
        note: 'Критерий выбора частей: начинайте с `__init__` и обычных методов; property, dunder-, class- и static-методы добавляйте только под понятный сценарий использования.',
      },
    ],
  },
];

const oop = [
  {
    title: 'Инкапсуляция',
    lead: 'Объект сам хранит свои данные и не позволяет привести их в недопустимое состояние.',
    sections: [
      {
        title: 'Суть: данные под контролем класса',
        paragraphs: [
          'Инкапсуляция объединяет данные (атрибуты) и методы, работающие с ними, внутри одного класса и скрывает детали реализации. Снаружи пользуются понятным интерфейсом, а как всё устроено внутри — знает только класс.',
          'Зачем это нужно: если любое поле можно менять снаружи, объект не отвечает за свою правильность. Здесь кто-то записал отрицательный баланс, и класс об этом даже не узнал.',
        ],
        useWhen: [
          'У значения есть ограничения: сумма положительна, статус допустим, дата корректна.',
          'Внутреннее хранение может измениться, а публичный интерфейс нужно сохранить.',
        ],
        steps: [
          'Определите состояние, за правильность которого отвечает объект.',
          'Оставьте снаружи понятные свойства и методы.',
          'Проверяйте новое значение до изменения состояния.',
        ],
        mistakes: [
          'Проверять данные только во внешнем коде.',
          'Разрешать менять внутренний атрибут напрямую.',
        ],
        syntax: 'acc.balance = -500  # ничто не мешает',
        example: 'class BankAccount:\n    def __init__(self, balance):\n        self.balance = balance\n\naccount = BankAccount(100)\naccount.balance = -500\nprint(f"Некорректный баланс: {account.balance}")\n# Некорректный баланс: -500',
        template: 'class Model:\n    def __init__(self, value):\n        self.value = value\n\n    def change(self, value):\n        if value_is_invalid:\n            raise ValueError("Некорректное значение")\n        self.value = value',
        note: 'Ошибки не случилось, но данные испорчены: счёта с минусом существовать не должно. Ниже — соглашения и инструменты, которыми это закрывают.',
      },
      {
        title: 'Одно подчёркивание: защищённый атрибут',
        paragraphs: [
          'Одинарное подчёркивание `_protected` — только соглашение «внутренняя деталь, не используйте снаружи». Python не ограничивает доступ.',
          'Это сигнал другому разработчику, а не механизм безопасности. Снаружи на такое поле лучше не опираться: его могут переименовать в любой версии.',
        ],
        syntax: 'self._protected = "Защищённый"',
        example: 'class BankAccount:\n    def __init__(self, balance):\n        self._protected = "Защищённый"\n        self._balance = balance\n\n    def get_balance(self):\n        return self._balance\n\nacc = BankAccount(100)\nprint(acc.get_balance())\nprint(acc._protected)\n# 100\n# Защищённый',
      },
      {
        title: 'Два подчёркивания: name mangling',
        paragraphs: [
          'Двойное подчёркивание включает name mangling: атрибут `__private` внутри класса `BankAccount` превращается в `_BankAccount__private`.',
          'Поэтому обращение `account.__private` снаружи даёт ошибку, а `account._BankAccount__private` — работает. Доступ затруднён, но не запрещён. Главная польза не в секретности: так поле не столкнётся с одноимённым полем в классе-наследнике.',
        ],
        syntax: 'self.__private = balance\n# снаружи: объект._BankAccount__private',
        example: 'class BankAccount:\n    def __init__(self, balance):\n        self.__private = balance\n\n    def get_balance(self):\n        return self.__private\n\naccount = BankAccount(100)\nprint(account.get_balance())\nprint(account._BankAccount__private)\n# 100\n# 100',
        note: 'Пароли и секреты так не прячут — это не защита. Обращаться к `_Класс__поле` снаружи не следует: раз имя пришлось искажать, поле точно не для внешнего кода.',
      },
      {
        title: 'Доступ через property',
        paragraphs: [
          'Спрятать данные — половина дела: их нужно как-то читать и менять. Стандартный способ в Python — `@property`: снаружи это обычное поле, а внутри работает метод с проверкой.',
          'Начальное значение тоже должно пройти эту проверку: в `__init__` присваивают `self.balance = balance`, а не `self._balance = balance`. Иначе конструктор обойдёт setter.',
        ],
        useWhen: [
          'Нужна удобная запись `object.value`, но чтение или запись требуют логики.',
        ],
        steps: [
          'Getter возвращает внутреннее значение.',
          'Setter валидирует данные и только потом сохраняет их.',
          'Конструктор присваивает через публичное свойство и вызывает setter.',
        ],
        mistakes: [
          'В конструкторе писать прямо в `_balance` и обходить проверку.',
          'В setter присваивать `self.balance = value` и получить рекурсию.',
        ],
        syntax: '@property\ndef balance(self):\n    return self._balance\n\n@balance.setter\ndef balance(self, value):\n    ...',
        example: 'class BankAccount:\n    def __init__(self, balance):\n        self.balance = balance\n\n    @property\n    def balance(self):\n        return self._balance\n\n    @balance.setter\n    def balance(self, value):\n        if value < 0:\n            raise ValueError("Баланс не может быть отрицательным")\n        self._balance = value\n\naccount = BankAccount(100)\nprint(f"Баланс: {account.balance}")\ntry:\n    BankAccount(-1)\nexcept ValueError as error:\n    print(f"Ошибка: {error}")\n# Баланс: 100\n# Ошибка: Баланс не может быть отрицательным',
        template: 'class Model:\n    def __init__(self, value):\n        self.value = value\n\n    @property\n    def value(self):\n        return self._value\n\n    @value.setter\n    def value(self, value):\n        if value_is_invalid:\n            raise ValueError("Некорректное значение")\n        self._value = value',
        note: 'Критерий выбора: используйте `property`, когда снаружи нужен атрибут, а внутри — единый контроль чтения или записи.',
      },
    ],
  },
  {
    title: 'Наследование',
    lead: 'Наследование описывает отношение is-a, а композиция — has-a; связь выбирают по смыслу.',
    sections: [
      {
        title: 'Ребёнок получает всё от родителя',
        paragraphs: [
          'Наследование позволяет создать класс на основе другого. Родителя указывают в скобках: `class Dog(Animal)`. Всё, что есть у родителя, ребёнок получает готовым.',
          'Практический критерий — отношение is-a: собака является животным и может использоваться там, где ожидают животное. Наследование только ради готового метода обычно создаёт неверную модель.',
        ],
        useWhen: [
          'Наследник действительно является частным случаем базового типа.',
          'Его можно подставить вместо родителя без изменения смысла программы.',
        ],
        steps: [
          'Python ищет атрибут сначала в классе объекта, затем в базовых классах.',
          'Наследник получает общий интерфейс и может переопределить реализацию.',
          '`isinstance` учитывает всю цепочку наследования.',
        ],
        mistakes: [
          'Наследоваться только ради переиспользования нескольких строк.',
          'Строить глубокую иерархию для невзаимозаменяемых объектов.',
        ],
        syntax: 'class Dog(Animal):\n    ...',
        example: 'class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def eat(self):\n        print(f"{self.name} ест")\n\nclass Dog(Animal):\n    pass\n\ndog = Dog("Рекс")\ndog.eat()\nprint(f"Рекс — животное: {isinstance(dog, Animal)}")\n# Рекс ест\n# Рекс — животное: True',
        template: 'class Base:\n    def action(self):\n        return "общее поведение"\n\nclass Child(Base):\n    def own_action(self):\n        return "своё поведение"',
        note: 'Критерий выбора: наследование подходит только для устойчивого отношения is-a.',
      },
      {
        title: 'Переопределение метода',
        paragraphs: [
          'В ребёнке пишут то, чего у родителя нет: собака умеет лаять, а животное в целом — нет.',
          'А если имя метода совпало с родительским, работать будет версия ребёнка — это называют переопределением. Родительский метод Python даже не посмотрит, потому что нашёл подходящий раньше.',
        ],
        syntax: 'class Dog(Animal):\n    def voice(self):\n        print("Гав!")',
        example: 'class Animal:\n    def voice(self):\n        print("Какой-то звук")\n\nclass Dog(Animal):\n    def voice(self):\n        print("Гав!")\n\nAnimal().voice()\nDog().voice()\n# Какой-то звук\n# Гав!',
      },
      {
        title: 'super(): дополнить, а не потерять',
        paragraphs: [
          'Чем опасно переопределение, лучше всего видно на конструкторе. Написали свой `__init__` — родительский не вызовется, и заданные в нём поля просто не появятся.',
          '`super().__init__(name)` продолжает поиск в следующем классе по MRO, а не обязательно обращается к непосредственному родителю. После общей работы наследник добавляет свою.',
        ],
        syntax: 'super().__init__(name)\nself.breed = breed',
        example: 'class Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n\nrex = Dog("Рекс", "корги")\nprint(rex.name, rex.breed)\n# Рекс корги',
      },
      {
        title: 'Проверка «является»',
        paragraphs: [
          'Объект наследника — это одновременно и экземпляр родителя: `isinstance(rex, Animal)` даёт `True`. Список родителей класса виден в `__bases__`.',
          'Отсюда и правило выбора: наследование читают словом «является». Собака является животным — верно. А машина является двигателем? Нет, хотя метод `start()` хочется взять готовым. В таких случаях объект кладут внутрь другого объекта как атрибут — это называют композицией.',
        ],
        syntax: 'isinstance(объект, Родитель)',
        example: 'class Animal:\n    pass\n\nclass Dog(Animal):\n    pass\n\nrex = Dog()\nprint(isinstance(rex, Dog))\nprint(isinstance(rex, Animal))\nprint(Dog.__bases__)\n# True\n# True\n# (<class \'__main__.Animal\'>,)',
      },
      {
        title: 'Композиция: объект имеет зависимость',
        paragraphs: [
          'Практическая проблема: машине нужен двигатель, но машина не является двигателем. При композиции один объект хранит другой и поручает ему часть работы.',
          'Композиция выражает has-a: `Car` имеет `Engine`. Зависимость легко заменить без перестройки иерархии.',
        ],
        useWhen: [
          'Один объект использует другой как часть или сервис.',
          'Зависимость нужно отдельно настраивать, заменять или тестировать.',
        ],
        steps: [
          'Передайте зависимость в конструктор.',
          'Сохраните её в атрибуте.',
          'Делегируйте ей подходящую операцию.',
        ],
        mistakes: [
          'Писать `class Car(Engine)` ради метода `start`.',
          'Жёстко создавать зависимость внутри, когда её нужно подменять.',
        ],
        syntax: 'class Car:\n    def __init__(self, engine):\n        self.engine = engine',
        example: 'class Engine:\n    def start(self):\n        return "двигатель запущен"\n\nclass Car:\n    def __init__(self, engine):\n        self.engine = engine\n\n    def start(self):\n        print(f"Автомобиль: {self.engine.start()}")\n\nCar(Engine()).start()\n# Автомобиль: двигатель запущен',
        template: 'class Client:\n    def __init__(self, service):\n        self.service = service\n\n    def run(self):\n        return self.service.action()',
        note: 'Критерий выбора: is-a — наследование; has-a или uses-a — композиция. При сомнении чаще начинают с композиции.',
      },
    ],
  },
  {
    title: 'Множественное наследование',
    lead: 'Продвинутая техника: класс получает поведение нескольких базовых классов, а конфликты решает MRO.',
    sections: [
      {
        title: 'Несколько родителей через запятую',
        paragraphs: [
          'В скобках перечисляют всех родителей. Класс получает методы каждого из них, даже если своего кода у него нет вообще.',
          'Практическое применение — небольшие независимые mixins. Это продвинутая техника: для обычных частей и сервисов композиция обычно понятнее.',
        ],
        useWhen: [
          'Фреймворк уже использует кооперативные mixins.',
          'Каждая примесь мала и добавляет одну чёткую возможность.',
        ],
        steps: [
          'Python строит общую MRO для всех базовых классов.',
          'Метод ищется по этой очереди до первого совпадения.',
          'Кооперативные методы передают вызов дальше через `super()`.',
        ],
        mistakes: [
          'Смешивать большие классы предметной области ради готового кода.',
          'Не проверять конфликты имён и сигнатур.',
          'Забывать, что порядок базовых классов влияет на результат.',
        ],
        syntax: 'class Pegasus(Horse, Bird):\n    pass',
        example: 'class SaveMixin:\n    def save(self):\n        print(f"Сохранено: {self.name}")\n\nclass Product:\n    def __init__(self, name):\n        self.name = name\n\nclass SavedProduct(SaveMixin, Product):\n    pass\n\nSavedProduct("Книга").save()\n# Сохранено: Книга',
        template: 'class FeatureMixin:\n    def feature(self):\n        return "дополнительная возможность"\n\nclass Model(FeatureMixin, BaseModel):\n    pass',
        note: 'Критерий выбора: множественное наследование подходит для небольших согласованных mixins; зависимости подключайте композицией.',
      },
      {
        title: 'Примеси (mixins)',
        paragraphs: [
          'Самое частое применение — маленькие классы-добавки, каждый из которых даёт одну способность. Их называют примесями или миксинами и подмешивают к основному классу.',
          'Примесь не бывает самостоятельной вещью: `JsonMixin` — не объект предметной области, он только добавляет умение превращаться в текст. Поэтому его и не создают отдельно, а дописывают в скобки к нужному классу.',
        ],
        syntax: 'class User(JsonMixin):\n    ...',
        example: 'class JsonMixin:\n    def to_json(self):\n        return str(self.__dict__)\n\nclass User(JsonMixin):\n    def __init__(self, name):\n        self.name = name\n\nprint(User("Аня").to_json())\n# {\'name\': \'Аня\'}',
      },
      {
        title: 'Конфликт одинаковых имён',
        paragraphs: [
          'А если метод с одним именем есть у обоих родителей? Тогда побеждает тот, кто левее в скобках: у класса `C(A, B)` сработает метод из `A`.',
          'Порядок поиска не случайный — Python строит для каждого класса очередь классов и идёт по ней. Эта очередь называется MRO, ей посвящена отдельная тема.',
        ],
        syntax: 'class C(A, B):\n    pass',
        example: 'class A:\n    def method(self):\n        print("Метод из A")\n\nclass B:\n    def method(self):\n        print("Метод из B")\n\nclass C(A, B):\n    pass\n\nC().method()\n# Метод из A',
        note: 'Фраза «побеждает левый родитель» — упрощение. Точный порядок показывает `C.mro()`; `super()` всегда идёт к следующему классу в этой очереди.',
      },
    ],
  },
  {
    title: 'MRO',
    lead: 'Продвинутая тема: MRO задаёт единственный порядок поиска методов во всей иерархии.',
    sections: [
      {
        title: 'Очередь поиска метода',
        paragraphs: [
          'Когда у объекта вызывают метод, Python идёт по списку классов и берёт первое совпадение. Этот список и есть MRO.',
          'Посмотреть его можно через `Класс.mro()`. Первым всегда стоит сам класс, потом родители слева направо, последним — `object`, общий предок всех классов в Python. В примере `C` наследует `A` и `B`, у обоих есть `method`, и срабатывает вариант из `A`, потому что он раньше в очереди.',
        ],
        useWhen: [
          'Нужно объяснить выбор метода при множественном наследовании.',
          'Нужно отладить кооперативную цепочку `super()`.',
        ],
        steps: [
          'Поиск начинается с фактического класса объекта.',
          'Python идёт по `mro()` до первого совпадения.',
          '`object` обычно завершает очередь.',
        ],
        mistakes: [
          'Рассуждать только по одному уровню родителей.',
          'Менять порядок базовых классов без проверки новой MRO.',
        ],
        syntax: 'print(C.mro())',
        example: 'class A:\n    def method(self):\n        print("Метод из A")\n\nclass B:\n    def method(self):\n        print("Метод из B")\n\nclass C(A, B):\n    pass\n\nC().method()\nprint([cls.__name__ for cls in C.mro()])\n# Метод из A\n# [\'C\', \'A\', \'B\', \'object\']',
        template: 'for cls in Model.mro():\n    print(cls.__name__)',
        note: 'Критерий выбора: MRO — инструмент диагностики. Если очередь трудно объяснить, упростите иерархию.',
      },
      {
        title: 'Алгоритм C3-линеаризации',
        paragraphs: [
          'Очередь строит алгоритм C3-линеаризации. Правила простые: сначала сам класс, потом родители в порядке записи, и ни один класс не встаёт раньше своего наследника. Благодаря этому поиск предсказуем даже при сложном множественном наследовании.',
          'Если непротиворечивый порядок построить невозможно, Python отказывается создавать класс. Здесь `Broken(A, B)` требует, чтобы `A` шёл раньше `B`, но `B` — наследник `A` и должен быть раньше. Ошибка возникает сразу, а не при вызове метода.',
        ],
        syntax: 'class Broken(A, B):\n    pass',
        example: 'class A:\n    pass\n\nclass B(A):\n    pass\n\ntry:\n    class Broken(A, B):\n        pass\nexcept TypeError:\n    print("Ошибка: MRO построить нельзя")\n# Ошибка: MRO построить нельзя',
        note: 'Правильный порядок — от частного к общему: `class Fixed(B, A)`. Наследник всегда левее своего родителя.',
      },
      {
        title: 'Ромб: общий предок вызывается один раз',
        paragraphs: [
          'Классический случай — ромб: `D` наследует `B` и `C`, а те оба наследуют `A`. MRO получается `D, B, C, A`.',
          'Поэтому `super()` внутри `B` ведёт не в `A`, как кажется по скобкам, а в `C` — следующий класс по очереди. `super()` всегда смотрит на MRO того объекта, с которым работает. Благодаря этому общий предок `A` срабатывает один раз, а не дважды.',
        ],
        syntax: 'super().method()\n# следующий класс в MRO объекта',
        example: 'class A:\n    def method(self):\n        print("Общий класс A")\n\nclass B(A):\n    def method(self):\n        print("Ветка B")\n        super().method()\n\nclass C(A):\n    def method(self):\n        print("Ветка C")\n        super().method()\n\nclass D(B, C):\n    pass\n\nD().method()\nprint([cls.__name__ for cls in D.mro()])\n# Ветка B\n# Ветка C\n# Общий класс A\n# [\'D\', \'B\', \'C\', \'A\', \'object\']',
      },
    ],
  },
  {
    title: 'Полиморфизм',
    lead: 'Разные объекты используют через общий протокол поведения; обязательного родства между классами нет.',
    sections: [
      {
        title: 'Единый интерфейс',
        paragraphs: [
          'Практическая проблема: общий алгоритм должен работать с разными реализациями без цепочки `if type(...)`. Для этого объекты договариваются об одном поведении.',
          'Протокол включает не только имя метода, но и его параметры и смысл результата. Конкретный класс выбирает собственную реализацию.',
        ],
        useWhen: [
          'Несколько реализаций выполняют одну операцию по-разному.',
          'Новые реализации должны подключаться без изменения общего алгоритма.',
        ],
        steps: [
          'Определите минимальный протокол поведения.',
          'Реализуйте его в каждом подходящем классе.',
          'Общий код вызывает метод, не проверяя конкретный тип.',
        ],
        mistakes: [
          'Добавлять `if isinstance(...)` для каждой новой реализации.',
          'Считать методы общими, если их параметры или смысл различаются.',
        ],
        syntax: 'def notify(channel, text):\n    channel.send(text)',
        example: 'class EmailChannel:\n    def send(self, text):\n        print(f"Письмо: {text}")\n\nclass SmsChannel:\n    def send(self, text):\n        print(f"СМС: {text}")\n\ndef notify(channel, text):\n    channel.send(text)\n\nnotify(EmailChannel(), "Заказ готов")\nnotify(SmsChannel(), "Курьер выехал")\n# Письмо: Заказ готов\n# СМС: Курьер выехал',
        template: 'def process(item):\n    return item.action()\n\nclass First:\n    def action(self):\n        return "первый результат"\n\nclass Second:\n    def action(self):\n        return "второй результат"',
        note: 'Критерий выбора: полиморфизм нужен, когда алгоритм зависит от поведения, а не от конкретного класса.',
      },
      {
        title: 'Утиная типизация',
        paragraphs: [
          'Python не спрашивает, кто чей наследник. Важно только то, что у объекта есть нужный метод с ожидаемым поведением. Это называют утиной типизацией.',
          'Поэтому общий код не переписывают под новый класс. Добавили робота с методом `speak` — он встал в тот же цикл, а функцию `concert` не тронули. Родства с собакой и кошкой у него нет никакого.',
        ],
        syntax: 'concert([Dog(), Cat(), Robot()])',
        example: 'class Robot:\n    def speak(self):\n        print("Робот: бип!")\n\ndef concert(performers):\n    for performer in performers:\n        performer.speak()\n\nconcert([Robot()])\n# Робот: бип!',
        note: 'Если объект не поддерживает протокол, будет `AttributeError`. Родство классов для duck typing не требуется.',
      },
      {
        title: 'Полиморфизм через наследование',
        paragraphs: [
          'Второй обычный приём — общий родитель с методом, который дети переопределяют. Так в коде записано правило: у каждого животного есть `speak`.',
          'Работает всё так же: цикл вызывает один и тот же метод, а звук выбирает сам объект. Разница лишь в том, что родство классов теперь видно явно, и общий код можно держать в родителе.',
        ],
        syntax: 'class Animal:\n    def speak(self):\n        print("...")',
        example: 'class Animal:\n    def speak(self):\n        print("Неизвестный звук")\n\nclass Dog(Animal):\n    def speak(self):\n        print("Собака: гав!")\n\nclass Cat(Animal):\n    def speak(self):\n        print("Кошка: мяу!")\n\nfor animal in [Dog(), Cat(), Animal()]:\n    animal.speak()\n# Собака: гав!\n# Кошка: мяу!\n# Неизвестный звук',
      },
    ],
  },
  {
    title: 'Абстракция',
    lead: 'Абстракция оставляет существенные операции и скрывает детали; ABC лишь формализует контракт.',
    sections: [
      {
        title: 'Абстракция возможна без ABC',
        paragraphs: [
          'Практическая проблема: клиенту оплаты не нужны детали проверки и сетевого запроса. Ему нужна одна понятная операция `pay`.',
          'Абстракция выделяет существенный интерфейс и прячет технические шаги. Она возможна через функции, обычные классы, модули и протоколы — наследование от `ABC` не обязательно.',
        ],
        useWhen: [
          'Сложную подсистему нужно представить несколькими понятными операциями.',
          'Внутренние детали могут меняться без переписывания клиентского кода.',
        ],
        steps: [
          'Определите задачу пользователя объекта.',
          'Сформируйте минимальный публичный интерфейс.',
          'Спрячьте последовательность технических шагов внутри.',
        ],
        mistakes: [
          'Выдавать наружу все внутренние методы.',
          'Считать абстракцией только классы из модуля `abc`.',
        ],
        syntax: 'class PaymentService:\n    def pay(self, amount):\n        ...',
        example: 'class PaymentService:\n    def pay(self, amount):\n        if amount <= 0:\n            raise ValueError("Сумма должна быть положительной")\n        receipt = self._send_request(amount)\n        print(f"Оплачено: {receipt}")\n\n    def _send_request(self, amount):\n        return f"{amount} руб."\n\nPaymentService().pay(500)\n# Оплачено: 500 руб.',
        template: 'class Facade:\n    def operation(self, value):\n        prepared = self._prepare(value)\n        return self._perform(prepared)\n\n    def _prepare(self, value):\n        ...\n\n    def _perform(self, value):\n        ...',
        note: 'Критерий выбора: хорошая абстракция говорит «что можно сделать», не заставляя клиента знать «как это устроено».',
      },
      {
        title: 'Забытый метод — ошибка сразу',
        paragraphs: [
          'Если наследник не реализовал обязательный метод, Python не даст создать объект и скажет, чего не хватает.',
          '`ABC` и `@abstractmethod` — инструмент формального контракта: ошибку видно при создании объекта. Саму абстракцию можно построить и без ABC.',
        ],
        syntax: 'class Circle(Shape):\n    pass',
        example: 'from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self):\n        ...\n\nclass Circle(Shape):\n    pass\n\ntry:\n    Circle()\nexcept TypeError:\n    print("Ошибка: метод area не реализован")\n# Ошибка: метод area не реализован',
      },
      {
        title: 'Общий код рядом с требованием',
        paragraphs: [
          'В абстрактном классе держат не только пустые требования, но и готовые методы. `describe` написан один раз и пользуется `self.area()` — тем самым методом, который обязан быть у каждого наследника.',
          'Получается удобно: родитель задаёт правила и даёт общий код, а наследник дописывает только своё — формулу площади.',
        ],
        syntax: 'def describe(self):\n    print(f"Площадь: {self.area()}")',
        example: 'from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self):\n        pass\n\n    def describe(self):\n        print(f"Площадь: {self.area()}")\n\nclass Square(Shape):\n    def __init__(self, side):\n        self.side = side\n\n    def area(self):\n        return self.side ** 2\n\nSquare(3).describe()\n# Площадь: 9',
        note: 'Абстракцию часто не выделяют отдельным принципом ООП — считают её частью инкапсуляции и наследования. На практике важно другое: `abc` фиксирует обязательные методы и ловит забытую реализацию сразу.',
      },
    ],
  },
];

const builtins = [
  {
    title: 'len, type и isinstance',
    lead: 'Длина объекта, его тип и проверка типа.',
    sections: [
      {
        title: 'len строки',
        paragraphs: [
          '`len` отвечает, сколько элементов в объекте. Для строки это число символов. У числа `len` нет: будет `TypeError`.',
        ],
        syntax: 'len(строка)',
        example: 'print(len("Python"))        # 6',
      },
      {
        title: 'len списка',
        paragraphs: [
          'Для списка `len` считает элементы, не символы внутри них.',
        ],
        syntax: 'len(список)',
        example: 'print(len([10, 20, 30]))     # 3',
      },
      {
        title: 'len словаря',
        paragraphs: [
          'Для словаря `len` — число ключей. Для множества — число уникальных значений.',
        ],
        syntax: 'len(словарь)',
        example: 'print(len({"a": 1}))         # 1',
      },
      {
        title: 'type',
        paragraphs: [
          '`type(x)` показывает тип объекта. Так смотрят, число перед нами, строка или коллекция.',
        ],
        syntax: 'type(объект)',
        example: 'print(type(25))\n# <class \'int\'>',
      },
      {
        title: 'isinstance',
        paragraphs: [
          '`isinstance(x, тип)` отвечает, является ли объект этим типом. Этот вариант предпочтительнее `type(x) == ...`: он учитывает и наследников. Можно передать несколько типов кортежем.',
        ],
        syntax: 'isinstance(объект, тип)',
        example: 'print(isinstance(25, int))       # True',
        note: '`type(x) == int` работает, но в проверках обычно пишут `isinstance`.',
      },
    ],
  },
  {
    title: 'int, float, str и bool',
    lead: 'Преобразование значения из одного типа в другой.',
    sections: [
      {
        title: 'int',
        paragraphs: [
          '`int` делает целое число из строки или отбрасывает дробную часть у `float`. `int("10")` читает десятичную запись.',
        ],
        syntax: 'int(x)',
        example: 'print(int("10"))        # 10',
        note: '`int("3.9")` нельзя: это не целая запись. Сначала `float("3.9")`, потом `int(...)`.',
      },
      {
        title: 'int из другой системы',
        paragraphs: [
          'Второй аргумент — система счисления. `int("101", 2)` читает двоичную запись.',
        ],
        syntax: 'int("101", 2)',
        example: 'print(int("101", 2))    # 5',
      },
      {
        title: 'float',
        paragraphs: [
          '`float` делает дробное число. Строка должна быть с точкой: `"1.5"`, не `"1,5"`.',
        ],
        syntax: 'float(x)',
        example: 'print(float("1.5"))     # 1.5',
      },
      {
        title: 'str',
        paragraphs: [
          '`str` превращает значение в текст. Так число можно склеить с надписью.',
        ],
        syntax: 'str(x)',
        example: 'print(str(25) + " кг")  # 25 кг',
      },
      {
        title: 'bool: ложное',
        paragraphs: [
          '`bool(x)` отвечает, истинно ли значение в условии. Ложные: `0`, `0.0`, `""`, `[]`, `{}`, `set()`, `None`.',
        ],
        syntax: 'bool(x)',
        example: 'print(bool(0))          # False',
      },
      {
        title: 'bool: строка "0"',
        paragraphs: [
          'Строка `"0"` истинна: она не пустая. То же со строкой `"False"`.',
        ],
        syntax: 'bool("0")',
        example: 'print(bool("0"))        # True',
      },
    ],
  },
  {
    title: 'list, tuple, set и dict',
    lead: 'Конструкторы коллекций: собрать список, кортеж, множество или словарь.',
    sections: [
      {
        title: 'list',
        paragraphs: [
          '`list` собирает список из другой последовательности. Так копируют и превращают `range` или строку в список. Число перебрать нельзя: `list(25)` вызовет ошибку.',
        ],
        syntax: 'list(iterable)',
        example: 'print(list("abc"))                    # ["a", "b", "c"]',
      },
      {
        title: 'tuple',
        paragraphs: [
          '`tuple` собирает кортеж. Исходный список при этом не меняется.',
        ],
        syntax: 'tuple(iterable)',
        example: 'print(tuple([1, 2]))                  # (1, 2)',
      },
      {
        title: 'set',
        paragraphs: [
          '`set` собирает множество: повторы исчезают.',
        ],
        syntax: 'set(iterable)',
        example: 'print(set([1, 1, 2]))                 # {1, 2}',
      },
      {
        title: 'dict',
        paragraphs: [
          '`dict` собирает словарь из пар `(ключ, значение)` или из именованных аргументов. Пустые объекты: `list()`, `tuple()`, `set()`, `dict()`. `{}` — словарь, не множество.',
        ],
        syntax: 'dict(пары)',
        example: 'print(dict([("a", 1), ("b", 2)]))     # {"a": 1, "b": 2}',
      },
    ],
  },
  {
    title: 'abs, round и pow',
    lead: 'Модуль, округление и возведение в степень.',
    sections: [
      {
        title: 'abs',
        paragraphs: [
          '`abs` берёт модуль: отрицательное число становится положительным.',
        ],
        syntax: 'abs(x)',
        example: 'print(abs(-7))             # 7',
      },
      {
        title: 'round',
        paragraphs: [
          '`round` округляет: без второго аргумента к целому, со вторым — до указанного числа знаков.',
        ],
        syntax: 'round(x, n)',
        example: 'print(round(3.14159, 2))   # 3.14',
      },
      {
        title: 'int — не округление',
        paragraphs: [
          '`int(3.9)` всегда отбрасывает дробь к нулю. Это не округление: `3.9` станет `3`, а не `4`.',
        ],
        syntax: 'int(3.9)',
        example: 'print(int(3.9))            # 3',
      },
      {
        title: 'pow',
        paragraphs: [
          '`pow(x, y)` — то же, что `x ** y`.',
        ],
        syntax: 'pow(x, y)',
        example: 'print(pow(2, 10))          # 1024',
      },
      {
        title: 'pow с остатком',
        paragraphs: [
          'Третий аргумент сразу берёт остаток: `pow(x, y, mod)` считают как `(x ** y) % mod`, часто быстрее.',
        ],
        syntax: 'pow(x, y, mod)',
        example: 'print(pow(2, 10, 100))     # 24',
      },
    ],
  },
  {
    title: 'min, max и sum',
    lead: 'Найти крайние значения и сложить числа.',
    sections: [
      {
        title: 'min',
        paragraphs: [
          '`min` находит наименьшее значение. Можно передать коллекцию или перечислить аргументы через запятую.',
        ],
        syntax: 'min(iterable)',
        example: 'nums = [3, 1, 8, 2]\nprint(min(nums))          # 1',
      },
      {
        title: 'max',
        paragraphs: [
          '`max` находит наибольшее значение. Аргументы можно перечислить через запятую без списка.',
        ],
        syntax: 'max(a, b, c)',
        example: 'print(max(3, 1, 8))       # 8',
      },
      {
        title: 'sum',
        paragraphs: [
          '`sum` складывает числа. Пустой `sum([])` даёт `0`. Строки `sum` не склеивает — для этого `"".join(...)`.',
        ],
        syntax: 'sum(iterable)',
        example: 'print(sum([3, 1, 8, 2]))  # 14',
      },
      {
        title: 'sum со стартом',
        paragraphs: [
          'Второй аргумент — начальное значение. Его прибавляют к сумме элементов.',
        ],
        syntax: 'sum(iterable, start)',
        example: 'print(sum([3, 1, 8, 2], 100))     # 114',
      },
      {
        title: 'min с key',
        paragraphs: [
          '`key=` говорит, по чему сравнивать. `min(words, key=len)` найдёт самое короткое слово, а не первое по алфавиту.',
        ],
        syntax: 'min(iterable, key=len)',
        example: 'words = ["я", "питон", "код"]\nprint(min(words, key=len))    # я',
      },
      {
        title: 'max с key',
        paragraphs: [
          'Тот же `key=` для `max` находит элемент с наибольшим признаком: здесь самое длинное слово.',
        ],
        syntax: 'max(iterable, key=len)',
        example: 'words = ["я", "питон", "код"]\nprint(max(words, key=len))    # питон',
      },
    ],
  },
  {
    title: 'sorted и reversed',
    lead: 'Новый отсортированный список и обход в обратном порядке.',
    sections: [
      {
        title: 'sorted',
        paragraphs: [
          '`sorted` всегда возвращает новый список и не меняет исходную коллекцию.',
        ],
        syntax: 'sorted(iterable)',
        example: 'nums = [3, 1, 2]\nprint(sorted(nums))                 # [1, 2, 3]',
      },
      {
        title: 'sorted по убыванию',
        paragraphs: [
          '`reverse=True` сортирует по убыванию. Исходная коллекция по-прежнему не меняется.',
        ],
        syntax: 'sorted(iterable, reverse=True)',
        example: 'print(sorted([3, 1, 2], reverse=True))   # [3, 2, 1]',
      },
      {
        title: 'sorted с key',
        paragraphs: [
          '`key=` задаёт признак сравнения. Здесь слова упорядочены по длине.',
        ],
        syntax: 'sorted(iterable, key=функция)',
        example: 'print(sorted(["я", "питон", "код"], key=len))\n# ["я", "код", "питон"]',
      },
      {
        title: 'sort vs sorted',
        paragraphs: [
          'У списка есть метод `.sort()`: он меняет объект на месте и возвращает `None`. `sorted` возвращает новый список.',
        ],
        syntax: 'lst.sort()',
        example: 'nums = [3, 1, 2]\nnums.sort()\nprint(nums)                         # [1, 2, 3]',
        note: 'Не пишите `lst = lst.sort()`. Метод вернёт `None`, и список пропадёт из переменной.',
      },
      {
        title: 'reversed списка',
        paragraphs: [
          '`reversed` даёт элементы с конца, но это не список, а итератор. Обычно сразу пишут `list(reversed(...))`. Нужна последовательность с индексами.',
        ],
        syntax: 'list(reversed(список))',
        example: 'print(list(reversed([1, 2, 3])))    # [3, 2, 1]',
      },
      {
        title: 'reversed строки',
        paragraphs: [
          'Строку так же разворачивают через `reversed`, затем склеивают обратно. Множество так развернуть нельзя.',
        ],
        syntax: '"".join(reversed(строка))',
        example: 'print("".join(reversed("кот")))     # ток',
      },
    ],
  },
  {
    title: 'range, enumerate и zip',
    lead: 'Числа для цикла, нумерация элементов и пары из двух коллекций.',
    sections: [
      {
        title: 'range',
        paragraphs: [
          '`range` даёт последовательность чисел. Сам по себе он не список: содержимое смотрят через `list(range(...))` или сразу в `for`. Правая граница не входит.',
        ],
        syntax: 'range(stop)',
        example: 'print(list(range(3)))          # [0, 1, 2]',
      },
      {
        title: 'range с шагом',
        paragraphs: [
          'Три аргумента: начало, конец и шаг. Конец по-прежнему не входит.',
        ],
        syntax: 'range(start, stop, step)',
        example: 'print(list(range(1, 8, 2)))    # [1, 3, 5, 7]',
      },
      {
        title: 'enumerate',
        paragraphs: [
          '`enumerate` нумерует элементы и заменяет ручной счётчик `i = 0`. `start` задаёт первый номер, по умолчанию `0`.',
        ],
        syntax: 'enumerate(iterable, start=0)',
        example: 'names = ["Анна", "Борис"]\nfor i, name in enumerate(names, start=1):\n    print(i, name)',
      },
      {
        title: 'zip',
        paragraphs: [
          '`zip` идёт по нескольким коллекциям сразу и берёт пары элементов с одинаковой позицией. Останавливается, когда заканчивается самая короткая. Лишние элементы длинной коллекции в результат не попадут.',
        ],
        syntax: 'zip(a, b, ...)',
        example: 'print(list(zip([1, 2, 3], ["a", "b"])))\n# [(1, "a"), (2, "b")]',
        note: '`range` и `zip` сами по себе не списки. Чтобы увидеть содержимое, пишут `list(...)` или обходят их в `for`.',
      },
    ],
  },
  {
    title: 'map и filter',
    lead: 'Пройти по коллекции: либо преобразовать каждый элемент, либо оставить только подходящие.',
    sections: [
      {
        title: 'Что делает map',
        paragraphs: [
          '`map` берёт функцию и коллекцию. Функцию вызывают для **каждого** элемента, и из ответов собирают новую последовательность.',
          'Сам `map(...)` — ещё не список. Чтобы увидеть результат целиком, его оборачивают в `list(...)`. Функцией может быть `str`, `int`, `lambda` или обычный `def`.',
        ],
        syntax: 'map(функция, коллекция)',
        example: 'print(list(map(str, [1, 2, 3])))\n# ["1", "2", "3"]',
        note: '`map` сам по себе ничего не печатает. Либо пишите `list(map(...))`, либо сразу распаковывайте: `a, b = map(...)`.',
      },
      {
        title: 'map: числа сделать строками',
        paragraphs: [
          'Готовая функция `str` превращает каждый элемент в текст. Список чисел становится списком строк. Это удобно, если потом нужно склеить значения или напечатать их как текст.',
        ],
        syntax: 'list(map(str, коллекция))',
        example: 'print(list(map(str, [10, 20])))\n# ["10", "20"]',
      },
      {
        title: 'map: умножить каждый элемент',
        paragraphs: [
          'Если готовой функции нет, правило пишут через `lambda`. Здесь каждый элемент списка умножается на 10. `x` — текущий элемент, `x * 10` — то, что кладут в ответ.',
        ],
        syntax: 'list(map(lambda x: x * 10, коллекция))',
        example: 'print(list(map(lambda x: x * 10, [1, 2, 3, 4])))\n# [10, 20, 30, 40]',
      },
      {
        title: 'map: ввод двух чисел',
        paragraphs: [
          'Классическая строка ввода: прочитать `"3 7"`, разрезать по пробелам и каждое слово превратить в `int`. `map` здесь не оборачивают в `list`, а сразу распаковывают в две переменные.',
        ],
        syntax: 'a, b = map(int, input().split())',
        example: '# человек ввёл: 3 7\na, b = map(int, input().split())\nprint(a + b)     # 10',
        note: 'Сколько чисел во вводе — столько переменных слева. Для трёх чисел: `a, b, c = map(int, input().split())`.',
      },
      {
        title: 'Что делает filter',
        paragraphs: [
          '`filter` тоже идёт по коллекции, но **не меняет** элементы. Он оставляет только те, для которых функция дала истину. Остальные выбрасывает.',
          'Как и у `map`, результат — не список. Чтобы получить список, пишут `list(filter(...))`.',
        ],
        syntax: 'filter(функция, коллекция)',
        example: 'print(list(filter(lambda x: x > 0, [-2, 0, 5])))\n# [5]',
      },
      {
        title: 'filter: только чётные',
        paragraphs: [
          'Функция `lambda x: x % 2 == 0` для каждого числа отвечает, чётное ли оно. `filter` оставляет те элементы, где ответ `True`. Нечётные в результат не попадут.',
        ],
        syntax: 'list(filter(lambda x: x % 2 == 0, коллекция))',
        example: 'print(list(filter(lambda x: x % 2 == 0, [1, 2, 3, 4])))\n# [2, 4]',
      },
      {
        title: 'Тот же отбор без filter',
        paragraphs: [
          'Тот же смысл можно записать включением: «положи `x`, если условие истинно». Оба варианта правильные. Включение часто читается проще, `filter` — короче, если функция уже есть.',
        ],
        syntax: '[x for x in коллекция if условие]',
        example: 'print([x for x in [1, 2, 3, 4] if x % 2 == 0])\n# [2, 4]',
      },
    ],
  },
  {
    title: 'functools.reduce',
    lead: 'Свернуть коллекцию в одно значение: сумму, произведение, максимум.',
    sections: [
      {
        title: 'Зачем нужен reduce',
        paragraphs: [
          '`reduce` берёт функцию **двух** аргументов и проходит по коллекции слева направо. На каждом шаге в функцию попадают уже накопленный результат и следующий элемент. В конце остаётся одно значение.',
          'Функция лежит в модуле `functools`, поэтому её сначала импортируют. Для обычной суммы проще `sum`, для максимума — `max`. `reduce` нужен, когда готовой функции нет: перемножить все числа, склеить по своему правилу, накопить результат иначе, чем умеют `sum` и `max`.',
        ],
        syntax: 'from functools import reduce\n\nreduce(функция, коллекция)',
        example: 'from functools import reduce\n\nprint(reduce(lambda a, b: a + b, [1, 2, 3, 4]))\n# 10\n# шаг 1: 1 + 2 = 3\n# шаг 2: 3 + 3 = 6\n# шаг 3: 6 + 4 = 10',
      },
      {
        title: 'Сумма',
        paragraphs: [
          'Функция всегда выглядит как `lambda a, b: ...`: `a` — то, что уже накопили, `b` — следующий элемент. Здесь складывают.',
        ],
        syntax: 'reduce(lambda a, b: a + b, коллекция)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [1, 2, 3, 4]))\n# 10',
      },
      {
        title: 'Произведение',
        paragraphs: [
          'Готовой функции «перемножить все» нет. `reduce` с `a * b` как раз для этого.',
        ],
        syntax: 'reduce(lambda a, b: a * b, коллекция)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a * b, [1, 2, 3, 4]))\n# 24',
      },
      {
        title: 'Максимум',
        paragraphs: [
          'Можно накопить наибольшее: на каждом шаге оставить большее из двух. Для обычного случая проще `max`.',
        ],
        syntax: 'reduce(lambda a, b: a if a > b else b, коллекция)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a if a > b else b, [3, 1, 8, 2]))\n# 8',
      },
      {
        title: 'Склейка текста',
        paragraphs: [
          'Строки тоже можно накапливать: `a + b` склеивает куски. Для обычной склейки чаще берут `"".join(...)`.',
        ],
        syntax: 'reduce(lambda a, b: a + b, строки)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, ["Py", "thon"]))\n# Python',
      },
      {
        title: 'Старт',
        paragraphs: [
          'Если передать третье значение, с него начинают, а не с первого элемента. Так добавляют «начальный вклад».',
        ],
        syntax: 'reduce(функция, коллекция, старт)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [1, 2, 3], 10))\n# 16  — начали с 10, затем +1 +2 +3',
      },
      {
        title: 'Пустой список',
        paragraphs: [
          'Со стартом пустой список безопасен: `reduce` вернёт сам старт. Без стартового значения по пустому списку будет `TypeError`.',
        ],
        syntax: 'reduce(функция, [], старт)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [], 0))\n# 0',
        note: 'Без стартового значения `reduce` по пустому списку упадёт с `TypeError`. Со стартом вернёт сам старт.',
      },
      {
        title: 'То же циклом',
        paragraphs: [
          '`reduce` — компактная запись цикла с накопителем. Если цикл читается проще, пишите цикл.',
        ],
        syntax: 'total = 0\nfor x in nums:\n    total = total + x',
        example: 'nums = [1, 2, 3, 4]\ntotal = 0\nfor x in nums:\n    total = total + x\nprint(total)    # 10',
      },
      {
        title: 'То же через reduce',
        paragraphs: [
          'Та же сумма одной строкой. Смысл тот же, что у цикла с накопителем.',
        ],
        syntax: 'reduce(lambda a, b: a + b, nums)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [1, 2, 3, 4]))\n# 10',
      },
      {
        title: 'Готовый sum',
        paragraphs: [
          'Для суммы и максимума обычно берут готовые `sum` и `max`. `reduce` оставляйте для своих правил накопления.',
        ],
        syntax: 'sum(коллекция)',
        example: 'print(sum([1, 2, 3, 4]))    # 10',
        note: '`reduce` оставляйте для своих правил накопления. Суммировать «вручную через reduce» чаще всего незачем.',
      },
    ],
  },
];

const methods = {
  'str — строки': [
    ['.upper()', 'все буквы в верхний регистр', '"кот".upper()  # "КОТ"'],
    ['.lower()', 'все буквы в нижний регистр', '"КОТ".lower()  # "кот"'],
    ['.capitalize()', 'первая буква заглавная', '"привет мир".capitalize()  # "Привет мир"'],
    ['.title()', 'каждое слово с заглавной', '"привет мир".title()  # "Привет Мир"'],
    ['.swapcase()', 'инвертировать регистр', '"Кот".swapcase()  # "кОТ"'],
    ['.count(sub)', 'число вхождений', '"banana".count("a")  # 3'],
    ['.find(sub)', 'индекс первого вхождения или -1', '"python".find("th")  # 2'],
    ['.rfind(sub)', 'индекс последнего вхождения или -1', '"ababa".rfind("ab")  # 2'],
    ['.index(sub)', 'индекс первого вхождения; иначе ValueError', '"python".index("th")  # 2'],
    ['.startswith(prefix)', 'начинается ли строка с текста', '"Python".startswith("Py")  # True'],
    ['.endswith(suffix)', 'заканчивается ли строка текстом', '"file.txt".endswith(".txt")  # True'],
    ['.strip()', 'убрать символы с краёв', '"  кот  ".strip()  # "кот"'],
    ['.lstrip()', 'убрать слева', '"  кот".lstrip()  # "кот"'],
    ['.rstrip()', 'убрать справа', '"кот  ".rstrip()  # "кот"'],
    ['.replace(old, new)', 'заменить подстроку', '"кот".replace("о", "а")  # "кат"'],
    ['.split()', 'строка → список', '"a b c".split()  # ["a", "b", "c"]'],
    ['.join(iterable)', 'склеить строки разделителем', '"-".join(["a", "b"])  # "a-b"'],
    ['.isdigit()', 'только цифры?', '"12".isdigit()  # True'],
    ['.isalpha()', 'только буквы?', '"кот".isalpha()  # True'],
    ['.isalnum()', 'буквы и цифры?', '"a1".isalnum()  # True'],
    ['.islower()', 'все буквы строчные?', '"кот".islower()  # True'],
    ['.isupper()', 'все буквы заглавные?', '"КОТ".isupper()  # True'],
    ['.isspace()', 'только пробелы?', '"  ".isspace()  # True'],
  ],
  'list — списки': [
    ['.append(x)', 'добавить один элемент в конец', 'a = [1, 2]; a.append(3)  # [1, 2, 3]'],
    ['.extend(iterable)', 'добавить элементы iterable', 'a = [1, 2]; a.extend([3, 4])  # [1, 2, 3, 4]'],
    ['.insert(i, x)', 'вставить перед индексом i', 'a = [1, 3]; a.insert(1, 2)  # [1, 2, 3]'],
    ['.remove(x)', 'удалить первое вхождение', 'a = [1, 2, 2]; a.remove(2)  # [1, 2]'],
    ['.pop()', 'удалить и вернуть последний', 'a = [1, 2, 3]; a.pop()  # 3, список [1, 2]'],
    ['.pop(i)', 'удалить и вернуть по индексу', 'a = [1, 2, 3]; a.pop(0)  # 1, список [2, 3]'],
    ['.clear()', 'очистить список', 'a = [1, 2]; a.clear()  # []'],
    ['.index(x)', 'индекс первого x', '[10, 20, 30].index(20)  # 1'],
    ['.count(x)', 'сколько раз есть x', '[1, 2, 2, 3].count(2)  # 2'],
    ['.sort()', 'сортировать на месте', 'a = [3, 1, 2]; a.sort()  # [1, 2, 3]'],
    ['.reverse()', 'развернуть на месте', 'a = [1, 2, 3]; a.reverse()  # [3, 2, 1]'],
    ['.copy()', 'поверхностная копия', 'b = [1, 2].copy()  # [1, 2]'],
  ],
  'tuple — кортежи': [
    ['.count(x)', 'сколько раз встречается x', '(1, 2, 2).count(2)  # 2'],
    ['.index(x)', 'индекс первого x', '(10, 20, 30).index(20)  # 1'],
  ],
  'dict — словари': [
    ['.get(key, default)', 'значение или default', '{"a": 1}.get("b", 0)  # 0'],
    ['.keys()', 'все ключи', 'list({"a": 1, "b": 2}.keys())  # ["a", "b"]'],
    ['.values()', 'все значения', 'list({"a": 1}.values())  # [1]'],
    ['.items()', 'пары ключ, значение', 'list({"a": 1}.items())  # [("a", 1)]'],
    ['.setdefault(key, default)', 'получить или создать ключ', 'd = {}; d.setdefault("a", 1)  # 1, словарь {"a": 1}'],
    ['.update(other)', 'добавить/обновить пары', 'd = {"a": 1}; d.update({"b": 2})  # {"a": 1, "b": 2}'],
    ['.pop(key)', 'удалить ключ, вернуть значение', 'd = {"a": 1}; d.pop("a")  # 1'],
    ['.popitem()', 'удалить последнюю пару', 'd = {"a": 1, "b": 2}; d.popitem()  # ("b", 2)'],
    ['.clear()', 'очистить', 'd = {"a": 1}; d.clear()  # {}'],
    ['.copy()', 'поверхностная копия', '{"a": 1}.copy()  # {"a": 1}'],
  ],
  'set — множества': [
    ['.add(x)', 'добавить элемент', 's = {1}; s.add(2)  # {1, 2}'],
    ['.update(iterable)', 'добавить элементы', 's = {1}; s.update([2, 3])  # {1, 2, 3}'],
    ['.remove(x)', 'удалить; нет x → KeyError', 's = {1, 2}; s.remove(2)  # {1}'],
    ['.discard(x)', 'удалить безопасно', 's = {1}; s.discard(9)  # {1}'],
    ['.pop()', 'удалить произвольный элемент', 's = {1}; s.pop()  # 1, множество set()'],
    ['.clear()', 'очистить', 's = {1, 2}; s.clear()  # set()'],
    ['.copy()', 'копия', '{1, 2}.copy()  # {1, 2}'],
    ['.union(other)', 'объединение', '{1, 2}.union({2, 3})  # {1, 2, 3}'],
    ['.intersection(other)', 'пересечение', '{1, 2}.intersection({2, 3})  # {2}'],
    ['.difference(other)', 'разность', '{1, 2}.difference({2, 3})  # {1}'],
    ['.symmetric_difference(other)', 'не общие элементы', '{1, 2}.symmetric_difference({2, 3})  # {1, 3}'],
    ['.intersection_update(other)', 'пересечение на месте', 's = {1, 2}; s.intersection_update({2, 3})  # {2}'],
    ['.difference_update(other)', 'разность на месте', 's = {1, 2}; s.difference_update({2})  # {1}'],
    ['.isdisjoint(other)', 'нет общих элементов?', '{1}.isdisjoint({2})  # True'],
    ['.issubset(other)', 'является подмножеством?', '{1}.issubset({1, 2})  # True'],
    ['.issuperset(other)', 'содержит другое множество?', '{1, 2}.issuperset({1})  # True'],
  ],
  'frozenset': [
    ['.copy()', 'копия', 'frozenset({1, 2}).copy()  # frozenset({1, 2})'],
    ['.union(other)', 'объединение', 'frozenset({1, 2}).union({2, 3})  # frozenset({1, 2, 3})'],
    ['.intersection(other)', 'пересечение', 'frozenset({1, 2}).intersection({2, 3})  # frozenset({2})'],
    ['.difference(other)', 'разность', 'frozenset({1, 2}).difference({2, 3})  # frozenset({1})'],
    ['.symmetric_difference(other)', 'не общие элементы', 'frozenset({1, 2}).symmetric_difference({2, 3})  # frozenset({1, 3})'],
    ['.isdisjoint(other)', 'нет общих элементов?', 'frozenset({1}).isdisjoint({2})  # True'],
    ['.issubset(other)', 'подмножество?', 'frozenset({1}).issubset({1, 2})  # True'],
    ['.issuperset(other)', 'надмножество?', 'frozenset({1, 2}).issuperset({1})  # True'],
  ],
};

const quick = [
  ['Вывод', 'print(a, b, sep=" ", end="\\n")'],
  ['Ввод числа', 'n = int(input())'],
  ['Два числа', 'a, b = map(int, input().split())'],
  ['Условие', 'if condition:'],
  ['Цикл', 'for x in iterable:'],
  ['Функция', 'def f(x): return result'],
];

const $ = (s) => document.querySelector(s);
const escapeHtml = (value) => String(value).replace(/[&<>'"]/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[c]));
const renderCode = (text) => escapeHtml(text).replace(/`([^`]+)`/g, '<code>$1</code>');
const renderTip = (text) => renderCode(text).replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
const sources = { topics, builtins, classes, oop };
const sourceMeta = {
  topics: { kicker: 'Основная теория', prefix: 'topic' },
  builtins: { kicker: 'Встроенные функции', prefix: 'builtin' },
  classes: { kicker: 'Классы', prefix: 'class' },
  oop: { kicker: 'ООП', prefix: 'oop' },
};
let mode = 'theory';
let modalSource = 'topics';
let modalIndex = 0;

function cardText(topic) {
  const chunks = [topic.title, topic.lead];
  (topic.sections || []).forEach((section) => {
    chunks.push(
      section.title,
      ...(section.paragraphs || []),
      ...(section.useWhen || []),
      ...(section.steps || []),
      ...(section.mistakes || []),
      section.template || '',
      section.syntax || '',
      section.example || '',
      section.note || '',
      ...(section.samples || []).flatMap((sample) => [sample.title, sample.code]),
    );
  });
  return chunks.join(' ');
}

function renderRows(items, allItems, source) {
  return items.map((topic) => {
    const index = allItems.indexOf(topic);
    return `<article class="topic-row" id="${sourceMeta[source].prefix}-${index}" data-open-topic="${source}:${index}"><span class="theory-number">${String(index + 1).padStart(2, '0')}</span><div class="topic-row-text"><h3>${escapeHtml(topic.title)}</h3><p>${renderCode(topic.lead)}</p></div><button type="button" class="open-topic">Открыть</button></article>`;
  }).join('');
}

function renderSection(section, index) {
  const paragraphs = (section.paragraphs || []).map((text) => `<p>${renderTip(text)}</p>`).join('');
  const renderPracticalList = (title, modifier, items) => (items || []).length
    ? `<section class="practical-card ${modifier}"><h4>${title}</h4><ul>${items.map((item) => `<li>${renderTip(item)}</li>`).join('')}</ul></section>`
    : '';
  const practical = section.useWhen || section.steps || section.mistakes
    ? `<div class="practical-grid">${renderPracticalList('КОГДА ПРИМЕНЯТЬ', 'is-when', section.useWhen)}${renderPracticalList('КАК РАБОТАЕТ', 'is-steps', section.steps)}${renderPracticalList('ЧАСТЫЕ ОШИБКИ', 'is-mistakes', section.mistakes)}</div>`
    : '';
  const syntax = section.syntax ? `<figure class="code-panel"><figcaption>КОНСТРУКЦИЯ</figcaption><pre>${escapeHtml(section.syntax)}</pre></figure>` : '';
  const example = section.example ? `<figure class="code-panel is-example"><figcaption>ПРИМЕР</figcaption><pre>${escapeHtml(section.example)}</pre></figure>` : '';
  const grid = syntax || example ? `<div class="code-grid">${syntax}${example}</div>` : '';
  const template = section.template ? `<figure class="code-panel is-template"><figcaption>ГОТОВЫЙ ШАБЛОН</figcaption><pre>${escapeHtml(section.template)}</pre></figure>` : '';
  const samples = (section.samples || []).length
    ? `<div class="sample-list">${section.samples.map((sample) => `<article class="sample-card"><h4>${escapeHtml(sample.title)}</h4><pre>${escapeHtml(sample.code)}</pre></article>`).join('')}</div>`
    : '';
  const note = section.note ? `<p class="tip"><strong>Не перепутать:</strong> ${renderTip(section.note)}</p>` : '';
  return `<section class="subtopic" id="sub-${index}"><div class="subtopic-head"><span class="subtopic-num">${String(index + 1).padStart(2, '0')}</span><h3>${escapeHtml(section.title)}</h3></div><div class="prose">${paragraphs}</div>${practical}${grid}${template}${samples}${note}</section>`;
}

function openModal(source, index) {
  const list = sources[source];
  const topic = list?.[index];
  if (!topic) return;
  modalSource = source;
  modalIndex = index;
  $('#modal-kicker').textContent = sourceMeta[source].kicker;
  $('#modal-title').textContent = topic.title;
  $('#modal-lead').innerHTML = renderCode(topic.lead || '');
  $('#modal-toc').innerHTML = topic.sections.map((section, i) => `<a href="#sub-${i}">${escapeHtml(section.title)}</a>`).join('');
  $('#modal-body').innerHTML = topic.sections.map(renderSection).join('');
  $('#modal-body').scrollTop = 0;
  $('#modal-prev').disabled = index <= 0;
  $('#modal-next').disabled = index >= list.length - 1;
  $('#topic-modal').hidden = false;
  document.body.classList.add('modal-open');
  history.replaceState(null, '', `#${sourceMeta[source].prefix}-${index}`);
  $('#modal-close').focus();
}

function closeModal() {
  if ($('#topic-modal').hidden) return;
  $('#topic-modal').hidden = true;
  document.body.classList.remove('modal-open');
  history.replaceState(null, '', '#top');
}

function render() {
  const query = $('#search-input').value.trim().toLowerCase();
  const matches = (value) => !query || value.toLowerCase().includes(query);
  const methodNames = Object.keys(methods);
  const activeFilter = document.querySelector('.filter.active')?.dataset.filter || methodNames[0];
  const visibleTopics = topics.filter((topic) => matches(cardText(topic)));
  const visibleClasses = classes.filter((topic) => matches(cardText(topic)));
  const visibleOop = oop.filter((topic) => matches(cardText(topic)));
  const visibleBuiltins = builtins.filter((topic) => matches(cardText(topic)));
  const visibleGroups = Object.entries(methods).filter(([name, rows]) => name === activeFilter && matches(`${name} ${rows.flat().join(' ')}`));

  $('#theory-list').innerHTML = renderRows(visibleTopics, topics, 'topics');
  $('#classes-list').innerHTML = renderRows(visibleClasses, classes, 'classes');
  $('#oop-list').innerHTML = renderRows(visibleOop, oop, 'oop');
  $('#builtins-list').innerHTML = renderRows(visibleBuiltins, builtins, 'builtins');
  $('#methods-list').innerHTML = visibleGroups.map(([group, rows]) => {
    const visibleRows = rows.filter((row) => matches(row.join(' ')));
    return `<article class="method-group"><h3>${escapeHtml(group)}</h3>${visibleRows.map(([method, text, example]) => `<div class="method-row"><code>${escapeHtml(method)}</code><span>${escapeHtml(text)}</span><code class="method-example">${escapeHtml(example)}</code></div>`).join('')}</article>`;
  }).join('');

  const counts = { theory: visibleTopics.length, classes: visibleClasses.length, oop: visibleOop.length, builtins: visibleBuiltins.length, methods: visibleGroups.length };
  const visibleCount = counts[mode];
  $('#empty-state').hidden = visibleCount > 0;
}

function setMode(nextMode) {
  mode = nextMode;
  document.querySelectorAll('.switch-button').forEach((button) => {
    const active = button.dataset.mode === mode;
    button.classList.toggle('active', active);
    button.setAttribute('aria-selected', String(active));
  });
  $('#quick-section').hidden = mode !== 'theory';
  $('#theory-section').hidden = mode !== 'theory';
  $('#classes-section').hidden = mode !== 'classes';
  $('#oop-section').hidden = mode !== 'oop';
  $('#builtins-section').hidden = mode !== 'builtins';
  $('#methods-section').hidden = mode !== 'methods';
  render();
}

function parseOpenTarget(value) {
  const [source, index] = String(value || '').split(':');
  if (!sources[source] || Number.isNaN(Number(index))) return null;
  return [source, Number(index)];
}

function init() {
  $('#quick-grid').innerHTML = quick.map(([label, code]) => `<article class="quick-card"><p>${escapeHtml(label)}</p><code>${escapeHtml(code)}</code></article>`).join('');
  $('#method-filters').innerHTML = Object.keys(methods).map((name, i) => `<button class="filter ${i === 0 ? 'active' : ''}" data-filter="${escapeHtml(name)}">${escapeHtml(name)}</button>`).join('');
  $('#topic-count').textContent = topics.length;
  $('#class-count').textContent = classes.length;
  $('#oop-count').textContent = oop.length;
  $('#builtin-count').textContent = builtins.length;
  $('#method-count').textContent = Object.values(methods).flat().length;
  $('#search-input').addEventListener('input', render);
  $('.content-switch').addEventListener('click', (e) => {
    if (e.target.matches('.switch-button')) setMode(e.target.dataset.mode);
  });
  $('#method-filters').addEventListener('click', (e) => {
    if (!e.target.matches('.filter')) return;
    document.querySelectorAll('.filter').forEach((button) => button.classList.remove('active'));
    e.target.classList.add('active');
    render();
  });
  document.addEventListener('click', (e) => {
    const openFrom = e.target.closest('[data-open-topic]');
    if (openFrom) {
      e.preventDefault();
      const target = parseOpenTarget(openFrom.dataset.openTopic);
      if (target) openModal(target[0], target[1]);
      return;
    }
    if (e.target.closest('[data-close-modal]')) closeModal();
  });
  $('#modal-toc').addEventListener('click', (e) => {
    const link = e.target.closest('a[href^="#sub-"]');
    if (!link) return;
    e.preventDefault();
    $('#modal-body').querySelector(link.getAttribute('href'))?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  });
  $('#modal-prev').addEventListener('click', () => openModal(modalSource, modalIndex - 1));
  $('#modal-next').addEventListener('click', () => openModal(modalSource, modalIndex + 1));
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      if (!$('#topic-modal').hidden) {
        closeModal();
        return;
      }
      $('#search-input').value = '';
      $('#search-input').blur();
      render();
    }
  });
  document.querySelectorAll('[data-query]').forEach((button) => button.addEventListener('click', () => {
    $('#search-input').value = button.dataset.query;
    render();
    $('#search-input').focus();
  }));
  const top = $('#to-top');
  window.addEventListener('scroll', () => top.classList.toggle('visible', window.scrollY > 500));
  top.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
  setMode('theory');
  const hash = location.hash.slice(1);
  if (hash.startsWith('topic-')) openModal('topics', Number(hash.slice(6)));
  if (hash.startsWith('class-')) {
    setMode('classes');
    openModal('classes', Number(hash.slice(6)));
  }
  if (hash.startsWith('oop-')) {
    setMode('oop');
    openModal('oop', Number(hash.slice(4)));
  }
  if (hash.startsWith('builtin-')) {
    setMode('builtins');
    openModal('builtins', Number(hash.slice(8)));
  }
}

init();
