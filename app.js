const topics = [
  {
    title: 'Значения, типы и переменные',
    lead: 'Что такое данные в программе и как дать им имя.',
    sections: [
      {
        title: 'Что такое тип',
        paragraphs: [
          'В программе всё, с чем мы работаем, — это значения: число, текст, список. У каждого значения есть тип. Тип говорит Python, что с объектом можно делать: складывать числа, склеивать строки, менять список.',
          'Коллекции `list`, `tuple`, `dict` и `set` разобраны в отдельных темах. Здесь — базовые типы.',
        ],
        syntax: 'type(значение)',
        example: 'print(type(25))\n# <class \'int\'>',
      },
      {
        title: 'Число',
        paragraphs: [
          'Целые числа имеют тип `int`: `25`, `0`, `-7`. Дробные — тип `float`: `19.99`, `3.0`. Для арифметики оба подходят, но `int` не хранит дробную часть.',
        ],
        syntax: 'целое = 25\nдробное = 19.99',
        example: 'age = 25\nprint(age)          # 25'
      },
      {
        title: 'Строка',
        paragraphs: [
          'Текст имеет тип `str`. Его пишут в кавычках. Даже если внутри цифры, это всё равно текст: `"10"` нельзя сложить как число, пока не превратить в `int`.',
        ],
        syntax: 'имя = "Анна"',
        example: 'name = "Анна"\nprint(name)         # Анна'
      },
      {
        title: 'Логическое значение',
        paragraphs: [
          '`bool` бывает только двух видов: `True` и `False`. Так отмечают «да» и «нет». Их получают из сравнений и пишут в условиях.',
        ],
        syntax: 'ok = True',
        example: 'ok = True\nprint(ok)           # True'
      },
      {
        title: 'None — пусто',
        paragraphs: [
          '`None` означает «значения нет». Это не ноль и не пустая строка, а отдельный объект. Его часто возвращает функция, которая ничего не отдаёт через `return`.',
        ],
        syntax: 'value = None',
        example: 'value = None\nprint(value)        # None',
      },
      {
        title: 'Переменная',
        paragraphs: [
          'Переменная — имя, которое ссылается на значение. Сначала значение создают, затем записывают в имя через `=`. После этого имя можно использовать вместо самого значения.',
          'Имя: буквы, цифры и `_`. Нельзя начинать с цифры: `age1` можно, `1age` нельзя. Регистр важен: `Age` и `age` — разные имена.',
        ],
        syntax: 'имя = значение',
        example: 'age = 25\nprint(age)         # 25\nage = 26\nprint(age)         # 26 — старое значение заменили',
        note: '`=` записывает значение в переменную. `==` сравнивает два значения. Строка `age = 25` — это не проверка, а запись.',
      },
      {
        title: 'Несколько имён сразу',
        paragraphs: [
          'Слева и справа от `=` можно поставить несколько имён и значений через запятую. Количество слева должно совпасть с количеством справа.',
        ],
        syntax: 'a, b = 1, 2',
        example: 'x, y = 10, 20\nprint(x, y)     # 10 20',
      },
      {
        title: 'Обмен значений',
        paragraphs: [
          'Две переменные можно поменять местами без третьей: справа пишут старые значения в новом порядке. Python сначала считает правую часть, потом записывает влево.',
        ],
        syntax: 'a, b = b, a',
        example: 'x, y = 10, 20\nx, y = y, x\nprint(x, y)     # 20 10',
      },
    ],
  },
  {
    title: 'Ввод, вывод и преобразование типов',
    lead: 'Как напечатать ответ, прочитать строку и превратить её в число.',
    sections: [
      {
        title: 'Одно значение',
        paragraphs: [
          '`print()` показывает значение на экране. После печати курсор переходит на новую строку.',
        ],
        syntax: 'print(значение)',
        example: 'print("Привет")\n# Привет',
      },
      {
        title: 'Несколько значений',
        paragraphs: [
          'Несколько аргументов печатаются через пробел. Так удобно вывести подпись и число рядом.',
        ],
        syntax: 'print(a, b)',
        example: 'print(10, 20)\n# 10 20',
      },
      {
        title: 'Разделитель sep',
        paragraphs: [
          'Параметр `sep` меняет то, что ставится между аргументами. По умолчанию это пробел.',
        ],
        syntax: 'print(a, b, sep=";")',
        example: 'print(10, 20, sep=";")\n# 10;20',
      },
      {
        title: 'Конец строки end',
        paragraphs: [
          'Параметр `end` — то, что ставится в конце. По умолчанию это перевод строки. Пустая строка оставляет курсор на той же линии, и следующий `print` допишет туда.',
        ],
        syntax: 'print(значение, end="")',
        example: 'print("A", end="")\nprint("B")\n# AB',
      },
      {
        title: 'Ввод: input',
        paragraphs: [
          '`input()` читает одну строку с клавиатуры. Даже если человек ввёл `10`, программа получит строку `"10"`, а не число.',
        ],
        syntax: 'текст = input("Подсказка: ")',
        example: 'name = input("Имя: ")\nprint("Привет,", name)',
        note: '`input()` всегда возвращает `str`. Пока строку не преобразовали, арифметика с ней будет склейкой, а не сложением.',
      },
      {
        title: 'int — целое число',
        paragraphs: [
          'Чтобы работать с целым числом, строку превращают через `int`. Если в `int()` попадёт не число, например `"abc"`, программа остановится с `ValueError`.',
        ],
        syntax: 'int("10")',
        example: 'n = int(input())\nprint(n + 1)',
      },
      {
        title: 'float — дробь',
        paragraphs: [
          '`float()` превращает строку в дробное число. Точка в записи обязательна в том виде, как её ждёт Python: `"1.5"`, не `"1,5"`.',
        ],
        syntax: 'float("1.5")',
        example: 'price = float("19.99")\nprint(price)        # 19.99',
      },
      {
        title: 'str — текст',
        paragraphs: [
          '`str()` превращает число в текст. Это нужно, когда число склеивают со строкой или пишут в файл как надпись.',
        ],
        syntax: 'str(25)',
        example: 'print("возраст: " + str(25))\n# возраст: 25',
      },
      {
        title: 'Ловушка: сложение строк',
        paragraphs: [
          'Две строки склеиваются знаком `+`. `"10" + "20"` даёт `"1020"`, а не `30`. Сначала каждую строку нужно превратить в число.',
        ],
        syntax: '"10" + "20"',
        example: 'print("10" + "20")             # 1020\nprint(int("10") + int("20"))   # 30',
      },
      {
        title: 'Два числа из одной строки',
        paragraphs: [
          'Частый приём: прочитать `"1 2"`, разрезать по пробелам и превратить каждый кусок в число. Слева должно быть столько переменных, сколько чисел во вводе.',
        ],
        syntax: 'a, b = map(int, input().split())',
        example: '# ввод: 3 7\na, b = map(int, input().split())\nprint(a + b)     # 10',
      },
      {
        title: 'Три числа из одной строки',
        paragraphs: [
          'Тот же приём работает для трёх (и больше) чисел: слева столько имён, сколько значений после `split`.',
        ],
        syntax: 'a, b, c = map(int, input().split())',
        example: '# ввод: 1 2 3\na, b, c = map(int, input().split())\nprint(a + b + c)     # 6',
      },
      {
        title: 'Список чисел из строки',
        paragraphs: [
          'Если чисел заранее неизвестно сколько, результат `map` собирают в список. Тогда длина не обязана совпадать с числом переменных слева.',
        ],
        syntax: 'nums = list(map(int, input().split()))',
        example: '# ввод: 4 8 15\nnums = list(map(int, input().split()))\nprint(nums)     # [4, 8, 15]',
      },
    ],
  },
  {
    title: 'Выражения и операторы',
    lead: 'Сложение, деление, остаток, степень и сравнения.',
    sections: [
      {
        title: 'Сложение',
        paragraphs: [
          'Выражение — то, что Python вычисляет и получает результат. `+` складывает числа. Для строк тот же знак склеивает текст — это другая операция.',
        ],
        syntax: 'a + b',
        example: 'print(10 + 4)     # 14',
      },
      {
        title: 'Умножение',
        paragraphs: [
          '`*` умножает числа. Для строки тот же знак повторяет текст: `"ab" * 3` даёт `"ababab"`.',
        ],
        syntax: 'a * b',
        example: 'print(3 * 5)      # 15',
      },
      {
        title: 'Степень',
        paragraphs: [
          'Степень записывают как `**`, не как `^`. `2 ** 10` — это 1024.',
        ],
        syntax: 'a ** b',
        example: 'print(2 ** 10)    # 1024',
      },
      {
        title: 'Деление /',
        paragraphs: [
          'Обычное деление `/` всегда даёт дробь: `8 / 2` это `4.0`, не `4`.',
        ],
        syntax: 'a / b',
        example: 'print(17 / 5)     # 3.4',
      },
      {
        title: 'Целое деление //',
        paragraphs: [
          '`//` делит нацело и отбрасывает дробную часть вниз. `17 // 5` даёт `3`.',
        ],
        syntax: 'a // b',
        example: 'print(17 // 5)    # 3',
      },
      {
        title: 'Остаток %',
        paragraphs: [
          '`%` — то, что не поместилось в целое число раз. `17 % 5` даёт `2`.',
        ],
        syntax: 'a % b',
        example: 'print(17 % 5)     # 2',
        note: 'Тождество: `a == (a // b) * b + (a % b)`.',
      },
      {
        title: 'Отрицательное //',
        paragraphs: [
          'Для отрицательных чисел `//` тоже идёт вниз, а не к нулю: `-5 // 2` даёт `-3`. Это легко перепутать с «отрезать дробь».',
        ],
        syntax: '-a // b',
        example: 'print(-5 // 2)    # -3',
      },
      {
        title: 'Сравнения',
        paragraphs: [
          'Сравнения дают `True` или `False`. Их пишут в условиях. Знак `=` там нельзя: он не сравнивает, а присваивает.',
        ],
        syntax: 'a == b    a != b\na < b     a <= b\na > b     a >= b',
        example: 'print(7 > 3)       # True',
        note: 'В условии пишут `if x == 0:`.',
      },
      {
        title: 'Приоритет и скобки',
        paragraphs: [
          'Сначала скобки, затем степень, затем умножение и деление, затем сложение, затем сравнения. Если сомневаетесь — ставьте скобки.',
        ],
        syntax: '(a + b) * c',
        example: 'print(2 + 2 * 2)    # 6\nprint((2 + 2) * 2)  # 8',
      },
    ],
  },
  {
    title: 'Условия',
    lead: 'Как выбрать одну из веток кода: if, elif, else.',
    sections: [
      {
        title: 'Конструкция if',
        paragraphs: [
          'Сначала проверяется `if`. Если условие ложно, Python смотрит следующий `elif`. Если ни одна проверка не подошла, выполняется `else`. После `if`, `elif` и `else` нужно двоеточие, тело блока пишут с отступом.',
          '`elif` может быть несколько. `else` — не больше одного, и только в конце. Ветку `else` можно не писать, если запасной код не нужен.',
        ],
        syntax: 'if условие:\n    код\nelif другое_условие:\n    код\nelse:\n    код',
        example: 'age = 20\nif age >= 18:\n    print("Доступ разрешён")\nelif age >= 14:\n    print("Нужен взрослый")\nelse:\n    print("Доступ запрещён")',
      },
      {
        title: 'and',
        paragraphs: [
          '`and` требует, чтобы оба условия были истинны. Если хотя бы одно ложно, вся проверка ложна.',
        ],
        syntax: 'условие1 and условие2',
        example: 'age, city = 20, "Казань"\nif age >= 18 and city == "Казань":\n    print("подойдёт")',
      },
      {
        title: 'or',
        paragraphs: [
          '`or` достаточно одного истинного условия. Оба ложны — тогда вся проверка ложна.',
        ],
        syntax: 'условие1 or условие2',
        example: 'age = 70\nif age < 14 or age > 65:\n    print("другой тариф")',
      },
      {
        title: 'Пустой список в if',
        paragraphs: [
          'В `if` ложными считаются `False`, `0`, пустая строка `""`, пустые коллекции и `None`. Пустой список `[]` ложный: ветка не выполнится.',
        ],
        syntax: 'if список:',
        example: 'if []:\n    print("не напечатается")',
        note: 'Список из нуля `[0]` истинный: в нём есть элемент.',
      },
      {
        title: 'Строка "0" в if',
        paragraphs: [
          'Непустая строка истинна, даже если внутри ноль. `"0"` — это текст из одного символа, не число ноль.',
        ],
        syntax: 'if "0":',
        example: 'if "0":\n    print("напечатается")',
      },
      {
        title: 'Короткий вариант',
        paragraphs: [
          'Если нужно выбрать одно из двух значений, пишут тернарный оператор. Для длинной развилки обычно понятнее обычный `if`. В Python 3.10+ для нескольких фиксированных вариантов есть `match-case`.',
        ],
        syntax: 'результат = a if условие else b',
        example: 'age = 20\nlabel = "взрослый" if age >= 18 else "ребёнок"\nprint(label)     # взрослый',
      },
    ],
  },
  {
    title: 'Строки',
    lead: 'Текст: создание, индексы, срезы и неизменяемость.',
    sections: [
      {
        title: 'Кавычки',
        paragraphs: [
          'Строка — это текст. Её пишут в двойных или одинарных кавычках, это равноправно. Пустая строка — `""`. Тройные кавычки удобны, когда внутри есть переносы.',
        ],
        syntax: 's = "Python"',
        example: 's = "Python"\nprint(s)            # Python',
      },
      {
        title: 'str()',
        paragraphs: [
          'Число превращают в текст через `str()`. Пока число не стало строкой, его нельзя склеить с надписью через `+`.',
        ],
        syntax: 'str(25)',
        example: 'print(str(5) + " яблок")\n# 5 яблок',
      },
      {
        title: 'Склейка +',
        paragraphs: [
          'Две строки склеивают знаком `+`. Получается новая строка, исходные не меняются.',
        ],
        syntax: '"Py" + "thon"',
        example: 'name = "Анна"\nprint("Привет, " + name)\n# Привет, Анна',
      },
      {
        title: 'Повтор *',
        paragraphs: [
          'Знак `*` повторяет строку указанное число раз.',
        ],
        syntax: '"ha" * 3',
        example: 'print("ha" * 3)\n# hahaha',
      },
      {
        title: 'Первый символ',
        paragraphs: [
          'Символы нумеруются с нуля: первый имеет индекс `0`.',
        ],
        syntax: 's[0]',
        example: 'word = "Python"\nprint(word[0])       # P',
      },
      {
        title: 'Последний символ',
        paragraphs: [
          'Индекс `-1` — последний символ. `-2` — предпоследний.',
        ],
        syntax: 's[-1]',
        example: 'word = "Python"\nprint(word[-1])      # n',
      },
      {
        title: 'Срез',
        paragraphs: [
          'Полная конструкция среза — три слота через двоеточие. `start` — откуда, включительно. `stop` — докуда, не включительно. `step` — шаг. Любую часть можно опустить: пустой `start` — с начала, пустой `stop` — до конца, пустой `step` — шаг `1`.',
        ],
        syntax: 's[start:stop:step]',
        example: 'word = "Python"\nprint(word[1:4])     # yth',
        note: 'Правая граница не входит в результат. `"Python"[0:3]` — это `"Pyt"`, без `h`.',
      },
      {
        title: 'Шаг среза',
        paragraphs: [
          'Третий слот — шаг. `s[0:6:2]` берёт символы через один. Все три части на месте — это та же конструкция `s[start:stop:step]`.',
        ],
        syntax: 's[start:stop:step]',
        example: 'word = "Python"\nprint(word[0:6:2])   # Pto',
      },
      {
        title: 'Переворот [::-1]',
        paragraphs: [
          'Если `start` и `stop` пустые, а шаг `-1`, срез идёт с конца: строка переворачивается.',
        ],
        syntax: 's[::-1]',
        example: 'word = "Python"\nprint(word[::-1])    # nohtyP',
      },
      {
        title: 'Строку нельзя изменить на месте',
        paragraphs: [
          'Нельзя написать `s[0] = "A"`. Нужно собрать новую строку: срезом и склейкой. Длина — `len(s)`. В условии пустая строка ложная.',
        ],
        syntax: 's = "К" + s[1:]',
        example: 's = "кот"\n# s[0] = "К"   # ошибка\ns = "К" + s[1:]\nprint(s)          # Кот',
      },
      {
        title: 'replace',
        paragraphs: [
          'Метод `.replace()` не меняет исходную строку: он возвращает новую, где кусок текста заменён.',
        ],
        syntax: 's.replace("о", "а")',
        example: 's = "кот"\nprint(s.replace("о", "а"))  # кат',
      },
      {
        title: 'f-строка',
        paragraphs: [
          'f-строка подставляет значения прямо в текст внутри фигурных скобок.',
        ],
        syntax: 'f"Возраст: {age}"',
        example: 'age = 25\nprint(f"Возраст: {age}")\n# Возраст: 25',
      },
      {
        title: 'Перенос строки',
        paragraphs: [
          'Служебная последовательность `\\n` — новая строка. `\\t` — табуляция, `\\\\` — сам слэш.',
        ],
        syntax: '"раз\\nдва"',
        example: 'print("раз\\nдва")\n# раз\n# два',
      },
    ],
  },
  {
    title: 'Списки',
    lead: 'Как создать список всеми обычными способами и как его менять.',
    sections: [
      {
        title: 'Пустой список',
        paragraphs: [
          'Список — упорядоченная коллекция, которую можно менять: добавлять, удалять и заменять элементы. Пустой список получают как `[]` или `list()`.',
        ],
        syntax: 'lst = []',
        example: 'a = []\nprint(a)           # []',
      },
      {
        title: 'Перечислить элементы',
        paragraphs: [
          'Элементы пишут в квадратных скобках через запятую. В одном списке могут лежать числа, строки и даже другие списки.',
        ],
        syntax: 'lst = [10, 20, 30]',
        example: 'a = [10, 20, 30]\nprint(a[0])        # 10',
      },
      {
        title: 'list из строки',
        paragraphs: [
          'Функция `list()` собирает список из другой последовательности. Из строки получаются символы, не целое слово.',
        ],
        syntax: 'list("abc")',
        example: 'print(list("кот"))\n# ["к", "о", "т"]',
        note: '`list("12")` — это `["1", "2"]`, а не `[12]`.',
      },
      {
        title: 'list из range',
        paragraphs: [
          'Из `range` получается список чисел. Правая граница в `range` не входит.',
        ],
        syntax: 'list(range(1, 5))',
        example: 'print(list(range(1, 4)))\n# [1, 2, 3]',
      },
      {
        title: 'Повтор [0] * n',
        paragraphs: [
          'Умножение `[0] * n` повторяет значение n раз. Так удобно завести список из нулей заданной длины.',
        ],
        syntax: '[0] * 4',
        example: 'print([0] * 4)\n# [0, 0, 0, 0]',
        note: '`[0] * 4` копирует число — так можно. А `[[0]] * 3` даёт три ссылки на один и тот же внутренний список. Для таблицы пишите `[[0] for _ in range(3)]`.',
      },
      {
        title: 'split',
        paragraphs: [
          '`split()` режет строку по пробелам и даёт список кусков. Каждый кусок — всё ещё строка, не число.',
        ],
        syntax: '"1 2 3".split()',
        example: 'print("1 2 3".split())\n# ["1", "2", "3"]',
      },
      {
        title: 'Чтение по индексу',
        paragraphs: [
          'Индекс `0` — первый элемент. `len(lst)` — длина списка.',
        ],
        syntax: 'lst[0]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[0])      # 10',
      },
      {
        title: 'Последний элемент',
        paragraphs: [
          'Индекс `-1` — последний элемент, как у строки.',
        ],
        syntax: 'lst[-1]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[-1])     # 40',
      },
      {
        title: 'Срез списка',
        paragraphs: [
          'У списка та же полная конструкция, что у строки: `start`, `stop` и `step`. Правая граница не входит. Результат — новый список, исходный не меняется.',
        ],
        syntax: 'lst[start:stop:step]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[1:3])    # [20, 30]',
      },
      {
        title: 'Шаг среза списка',
        paragraphs: [
          'Третий слот задаёт шаг. `lst[0:4:2]` берёт элементы через один.',
        ],
        syntax: 'lst[start:stop:step]',
        example: 'nums = [10, 20, 30, 40]\nprint(nums[0:4:2])  # [10, 30]',
      },
      {
        title: 'append',
        paragraphs: [
          '`append(x)` добавляет один элемент в конец. Список меняется на месте.',
        ],
        syntax: 'lst.append(x)',
        example: 'nums = [10, 20, 30]\nnums.append(40)\nprint(nums)         # [10, 20, 30, 40]',
      },
      {
        title: 'Замена по индексу',
        paragraphs: [
          '`lst[i] = x` заменяет элемент на месте. Новый список не создаётся.',
        ],
        syntax: 'lst[i] = x',
        example: 'nums = [10, 20, 30]\nnums[1] = 99\nprint(nums)         # [10, 99, 30]',
      },
      {
        title: 'pop',
        paragraphs: [
          '`pop()` удаляет последний элемент и возвращает его. Можно указать индекс: `pop(0)` снимет первый.',
        ],
        syntax: 'lst.pop()',
        example: 'nums = [10, 20, 30]\nlast = nums.pop()\nprint(last)         # 30\nprint(nums)         # [10, 20]',
      },
    ],
  },
  {
    title: 'Кортежи',
    lead: 'Неизменяемая последовательность: зачем нужна и как её создать.',
    sections: [
      {
        title: 'Обычный кортеж',
        paragraphs: [
          'Кортеж похож на список, но его нельзя менять: нет `append`, нельзя написать `t[0] = 1`. Так защищают набор, который не должен измениться: точку на плоскости, пару «имя и возраст».',
          'Обычный кортеж пишут в круглых скобках. Пустой — `()`.',
        ],
        syntax: 't = (10, 20, 30)',
        example: 'point = (3, 5)\nprint(point[0])       # 3',
      },
      {
        title: 'Один элемент: запятая',
        paragraphs: [
          'Кортеж из одного элемента обязательно содержит запятую: `(42,)`. Без запятой `(42)` — это просто число в скобках.',
        ],
        syntax: 't = (42,)',
        example: 'print((42,) == 42)    # False',
        note: 'Запятая важнее скобок. `(42)` — число, `(42,)` — кортеж.',
      },
      {
        title: 'tuple(...)',
        paragraphs: [
          'Из списка или строки кортеж собирают через `tuple(...)`.',
        ],
        syntax: 'tuple([10, 20])',
        example: 'print(tuple([10, 20]))\n# (10, 20)',
      },
      {
        title: 'Распаковка',
        paragraphs: [
          'Кортеж можно разобрать по переменным: слева столько имён, сколько элементов справа.',
        ],
        syntax: 'a, b = (10, 20)',
        example: 'lo, hi = (1, 8)\nprint(lo, hi)     # 1 8',
      },
      {
        title: 'return нескольких значений',
        paragraphs: [
          'Функция возвращает несколько значений через `return a, b`. На самом деле это кортеж. Его сразу распаковывают в переменные.',
        ],
        syntax: 'return a, b',
        example: 'def stats(nums):\n    return min(nums), max(nums)\n\nlo, hi = stats([3, 1, 8])\nprint(lo, hi)     # 1 8',
      },
    ],
  },
  {
    title: 'Словари',
    lead: 'Пары «ключ → значение»: создание, чтение и безопасный доступ.',
    sections: [
      {
        title: 'Пустой словарь',
        paragraphs: [
          'Словарь хранит пары «ключ → значение». К значению обращаются не по номеру, а по ключу. Пустой словарь — `{}` или `dict()`.',
        ],
        syntax: 'd = {}',
        example: 'd = {}\nprint(d)               # {}',
        note: '`{}` — это пустой словарь, не множество. Пустое множество создают только через `set()`.',
      },
      {
        title: 'Литерал',
        paragraphs: [
          'Пары перечисляют в фигурных скобках: ключ, двоеточие, значение. Ключ должен быть неизменяемым: строка, число или кортеж. Список ключом быть не может. Ключи уникальны: повторная запись затирает старое значение.',
        ],
        syntax: 'd = {"name": "Анна", "age": 25}',
        example: 'user = {"name": "Анна", "age": 25}\nprint(user["name"])     # Анна',
      },
      {
        title: 'dict(имя=...)',
        paragraphs: [
          '`dict(имя=значение)` подходит, если ключи — допустимые имена Python. Ключи станут строками.',
        ],
        syntax: 'dict(name="Анна", age=25)',
        example: 'd = dict(x=10, y=20)\nprint(d)               # {\'x\': 10, \'y\': 20}',
      },
      {
        title: 'Чтение d[key]',
        paragraphs: [
          '`d[key]` даёт значение. Если ключа нет, программа остановится с `KeyError`.',
        ],
        syntax: 'd[key]',
        example: 'user = {"name": "Анна"}\nprint(user["name"])     # Анна',
      },
      {
        title: 'Безопасное чтение get',
        paragraphs: [
          '`d.get(key)` возвращает `None`, если ключа нет, и не бросает ошибку. Второй аргумент — значение по умолчанию.',
        ],
        syntax: 'd.get(key, default)',
        example: 'user = {"name": "Анна"}\nprint(user.get("city", "нет"))    # нет',
      },
      {
        title: 'Запись',
        paragraphs: [
          '`d[key] = value` создаёт ключ или заменяет значение.',
        ],
        syntax: 'd[key] = value',
        example: 'user = {"name": "Анна"}\nuser["city"] = "Казань"\nprint(user["city"])     # Казань',
      },
      {
        title: 'Проверка in',
        paragraphs: [
          'Проверка `"name" in user` смотрит ключи, не значения. Обход пар: `for key, value in d.items()`.',
        ],
        syntax: 'key in d',
        example: 'user = {"name": "Анна"}\nprint("age" in user)    # False',
      },
    ],
  },
  {
    title: 'Множества',
    lead: 'Уникальные элементы без порядка и без индексов.',
    sections: [
      {
        title: 'Пустое множество',
        paragraphs: [
          'Множество хранит только уникальные элементы и не обещает порядок. Индексов нет: `s[0]` написать нельзя. Пустое множество — только `set()`.',
        ],
        syntax: 's = set()',
        example: 's = set()\nprint(s)               # set()',
        note: '`{}` создаёт словарь. Пустое множество нельзя записать как `{}`.',
      },
      {
        title: 'Литерал {1, 2, 3}',
        paragraphs: [
          'Литерал `{1, 2, 3}` уже множество, потому что внутри нет пар `ключ: значение`. Проверка `x in s` быстрая, дубликаты пропадают сами.',
        ],
        syntax: 's = {1, 2, 3}',
        example: 'print(len({1, 2, 2, 2}))  # 2',
      },
      {
        title: 'set из списка',
        paragraphs: [
          'Из списка множество собирают через `set(...)`. Повторы исчезают. Так удобно убирать дубликаты.',
        ],
        syntax: 'set([1, 1, 2, 3])',
        example: 'print(set([1, 1, 2]))     # {1, 2}',
      },
      {
        title: 'Объединение |',
        paragraphs: [
          '`|` даёт все элементы, которые есть хотя бы в одном из двух множеств.',
        ],
        syntax: 'a | b',
        example: 'a, b = {1, 2, 3}, {3, 4, 5}\nprint(a | b)     # {1, 2, 3, 4, 5}',
      },
      {
        title: 'Пересечение &',
        paragraphs: [
          '`&` оставляет только общие элементы.',
        ],
        syntax: 'a & b',
        example: 'a, b = {1, 2, 3}, {3, 4, 5}\nprint(a & b)     # {3}',
      },
      {
        title: 'Разность -',
        paragraphs: [
          '`a - b` — то, что есть в `a`, но нет в `b`.',
        ],
        syntax: 'a - b',
        example: 'a, b = {1, 2, 3}, {3, 4, 5}\nprint(a - b)     # {1, 2}',
      },
    ],
  },
  {
    title: 'Цикл while',
    lead: 'Повторять код, пока условие истинно.',
    sections: [
      {
        title: 'Когда берут while',
        paragraphs: [
          '`while` повторяет тело, пока условие истинно. Его используют, когда заранее неизвестно, сколько кругов понадобится: читать ввод до нуля, разбирать цифры числа, ждать условия. Если условие сразу ложно, тело не выполнится ни разу.',
        ],
        syntax: 'while условие:\n    тело_цикла',
        example: 'count = 1\nwhile count <= 5:\n    print(count)\n    count += 1',
        note: 'В теле должен быть шаг, который когда-нибудь сделает условие ложным. Иначе цикл станет бесконечным.',
      },
      {
        title: 'Цифры числа',
        paragraphs: [
          'Типичная задача: пока число больше нуля, брать последнюю цифру через `% 10` и отбрасывать её через `// 10`.',
        ],
        syntax: 'n = 123\nwhile n > 0:\n    digit = n % 10\n    n //= 10',
        example: 'n = 123\nwhile n > 0:\n    print(n % 10)    # 3, затем 2, затем 1\n    n //= 10',
      },
      {
        title: 'Бесконечный цикл и выход',
        paragraphs: [
          'Иногда пишут `while True` и выходят через `break`, когда пора остановиться. `continue` пропускает остаток текущего круга и возвращается к проверке условия.',
        ],
        syntax: 'while True:\n    ...\n    if пора_выйти:\n        break',
      },
    ],
  },
  {
    title: 'Цикл for и range',
    lead: 'Перебор элементов коллекции и последовательности чисел.',
    sections: [
      {
        title: 'for по списку',
        paragraphs: [
          '`for` берёт элементы по одному. Для списка переменная на каждом круге — очередной элемент, не индекс.',
        ],
        syntax: 'for элемент in список:\n    тело_цикла',
        example: 'for name in ["Анна", "Борис"]:\n    print(name)',
      },
      {
        title: 'for по строке',
        paragraphs: [
          'По строке цикл идёт по символам: на каждом круге один символ.',
        ],
        syntax: 'for символ in строка:\n    тело_цикла',
        example: 'for letter in "кот":\n    print(letter)',
      },
      {
        title: 'Повторить n раз',
        paragraphs: [
          'Когда нужно просто повторить код n раз, пишут `for i in range(n)`. Переменная `i` при этом часто не используется.',
        ],
        syntax: 'for i in range(n):',
        example: 'for i in range(3):\n    print("привет")',
      },
      {
        title: 'range(stop)',
        paragraphs: [
          '`range(stop)` даёт числа от `0` до `stop - 1`. Правая граница никогда не входит. Сам `range` — не список; список получают через `list(range(...))`.',
        ],
        syntax: 'range(stop)',
        example: 'print(list(range(5)))          # [0, 1, 2, 3, 4]',
      },
      {
        title: 'range(start, stop)',
        paragraphs: [
          'Два аргумента: начало и конец. Конец по-прежнему не входит.',
        ],
        syntax: 'range(start, stop)',
        example: 'print(list(range(1, 6)))       # [1, 2, 3, 4, 5]',
        note: '`range(1, 6)` — это 1, 2, 3, 4, 5. Шестёрки нет. Нужны числа от 1 до n: `range(1, n + 1)`.',
      },
      {
        title: 'range с шагом',
        paragraphs: [
          'Третий аргумент — шаг. Отрицательный шаг идёт назад: тогда начало должно быть больше конца.',
        ],
        syntax: 'range(start, stop, step)',
        example: 'print(list(range(1, 8, 2)))    # [1, 3, 5, 7]',
      },
      {
        title: 'enumerate',
        paragraphs: [
          'По индексам ходят так: `for i in range(len(items))`. Удобнее `enumerate`: он сразу даёт номер и элемент. Параметр `start` задаёт первый номер, по умолчанию `0`.',
        ],
        syntax: 'for i, item in enumerate(items, start=1):',
        example: 'names = ["Анна", "Борис"]\nfor i, name in enumerate(names, start=1):\n    print(i, name)     # 1 Анна, 2 Борис',
      },
    ],
  },
  {
    title: 'break, continue и else у цикла',
    lead: 'Выйти из цикла, пропустить круг или узнать, что break не случился.',
    sections: [
      {
        title: 'break и continue',
        paragraphs: [
          '`break` полностью прекращает цикл. `continue` пропускает только текущий круг и переходит к следующему элементу или к новой проверке `while`. Оба работают и в `for`, и в `while`.',
        ],
        syntax: 'for item in items:\n    if надо_пропустить:\n        continue\n    if надо_выйти:\n        break',
        example: 'for n in [1, 2, 3]:\n    if n % 2 == 0:\n        continue\n    print(n)     # 1 и 3, двойка пропущена',
        note: '`break` — выход из всего цикла. `continue` — «этот элемент не трогаем, берём следующий».',
      },
      {
        title: 'else у цикла',
        paragraphs: [
          '`else` после цикла выполняется, только если цикл дошёл до конца сам и `break` не срабатывал. Это не `else` у `if`: здесь ветка связана с циклом. Типичный приём: ищем элемент, при находке делаем `break`, в `else` пишем «не нашли».',
        ],
        syntax: 'for item in items:\n    if нашли:\n        break\nelse:\n    print("не нашли")',
        example: 'for n in [1, 3, 5]:\n    if n % 2 == 0:\n        print("нашли чётное")\n        break\nelse:\n    print("Чётных нет")',
      },
    ],
  },
  {
    title: 'Функции и return',
    lead: 'Как объявить функцию, передать аргументы и вернуть результат.',
    sections: [
      {
        title: 'Без аргументов',
        paragraphs: [
          'Функция — именованный блок кода, который можно вызывать много раз. Сначала её определяют через `def`, потом вызывают именем со скобками. Вызов без скобок не запускает код, а даёт сам объект функции.',
        ],
        syntax: 'def имя():\n    тело',
        example: 'def hello():\n    print("Привет")\n\nhello()               # Привет',
      },
      {
        title: 'Один аргумент',
        paragraphs: [
          'В скобках у `def` пишут параметр — имя, через которое внутрь попадёт значение. При вызове в скобках передают аргумент.',
        ],
        syntax: 'def имя(параметр):\n    return результат',
        example: 'def square(x):\n    return x * x\n\nprint(square(4))      # 16',
      },
      {
        title: 'Два аргумента',
        paragraphs: [
          'Параметров может быть несколько: их перечисляют через запятую. При вызове значения идут в том же порядке.',
        ],
        syntax: 'def имя(a, b):\n    return результат',
        example: 'def add(a, b):\n    return a + b\n\nprint(add(2, 3))      # 5',
      },
      {
        title: 'return одного значения',
        paragraphs: [
          '`return` отдаёт результат туда, откуда функцию вызвали, и сразу заканчивает работу функции. Если `return` нет, функция вернёт `None`.',
        ],
        syntax: 'return результат',
        example: 'def add(a, b):\n    return a + b\n\nprint(add(2, 3))      # 5',
        note: '`print()` показывает значение на экране. `return` возвращает его в программу. Это разные вещи.',
      },
      {
        title: 'return двух значений',
        paragraphs: [
          '`return a, b` возвращает кортеж. Его сразу распаковывают в переменные слева от `=`.',
        ],
        syntax: 'return a, b',
        example: 'def pair():\n    return 1, 2\n\nx, y = pair()\nprint(x, y)           # 1 2',
      },
      {
        title: 'Вызов с двумя аргументами',
        paragraphs: [
          'Параметру можно задать значение по умолчанию прямо в `def`. Если передать оба аргумента, значение по умолчанию не используется.',
        ],
        syntax: 'def add(a, b=0):\n    return a + b',
        example: 'def add(a, b=0):\n    return a + b\n\nprint(add(2, 3))     # 5',
        note: 'Не ставьте в значение по умолчанию изменяемый объект вроде `[]`: список будет общим для всех вызовов.',
      },
      {
        title: 'Вызов с одним аргументом',
        paragraphs: [
          'Если второй аргумент не передали, подставится значение из `def`. Здесь `b` станет `0`.',
        ],
        syntax: 'add(2)',
        example: 'def add(a, b=0):\n    return a + b\n\nprint(add(2))        # 2',
      },
    ],
  },
  {
    title: '*args и **kwargs',
    lead: 'Неизвестное число позиционных и именованных аргументов.',
    sections: [
      {
        title: '*args — лишние позиционные',
        paragraphs: [
          'Если функция должна принять сколько угодно значений без имён, пишут `*args`. Все лишние позиционные аргументы собираются в кортеж. Имя `args` — договорённость, работает сама звёздочка: можно написать `*numbers`.',
        ],
        syntax: 'def f(*args):\n    ...              # args — кортеж',
        example: 'def total(*nums):\n    return sum(nums)\n\nprint(total(1, 2, 3))     # 6',
        note: 'Так устроены `print()` и `min()`: они принимают сколько угодно значений.',
      },
      {
        title: '**kwargs — лишние именованные',
        paragraphs: [
          '`**kwargs` собирает лишние именованные аргументы в словарь. Ключи — имена, значения — то, что передали.',
        ],
        syntax: 'def f(**kwargs):\n    ...              # kwargs — словарь',
        example: 'def greet(name, **info):\n    print(name, info)\n\ngreet("Анна", city="Казань", age=25)\n# Анна {\'city\': \'Казань\', \'age\': 25}',
      },
      {
        title: 'Распаковка f(*args)',
        paragraphs: [
          'Звёздочка в вызове распаковывает список в отдельные аргументы. В `def` звёздочка упаковывает, в вызове — распаковывает.',
        ],
        syntax: 'f(*args)',
        example: 'def total(*nums):\n    return sum(nums)\n\nargs = [1, 2, 3]\nprint(total(*args))     # 6',
        note: '`f(args)` передаст один список. `f(*args)` передаст каждый элемент отдельно.',
      },
      {
        title: 'Ошибка: f(args) без звёздочки',
        paragraphs: [
          'Если передать список без `*`, функция получит один аргумент — весь список. Для `sum` это ещё сработает иначе, а для разбора по числам часто будет `TypeError`.',
        ],
        syntax: 'f(args)',
        example: 'def total(*nums):\n    return sum(nums)\n\nprint(total([1, 2, 3]))\n# TypeError: unsupported operand type(s)',
      },
    ],
  },
  {
    title: 'lambda',
    lead: 'Короткая функция в одну строку: слева аргументы, справа то, что вернуть.',
    sections: [
      {
        title: 'Что это такое',
        paragraphs: [
          '`lambda` — это маленькая функция без имени. Её пишут в одну строку. Слева от двоеточия — аргументы, справа — выражение, которое функция вернёт.',
          'Слово `return` здесь не пишут: результат выражения и есть ответ. Если нужен цикл, несколько строк или `print`, берите обычный `def`.',
        ],
        syntax: 'lambda аргументы: выражение',
        example: 'f = lambda x: x * 2\nprint(f(5))     # 10',
        note: '`f(5)` вызывает эту функцию так же, как обычную. Сначала записали `lambda` в имя `f`, потом вызвали со скобками.',
      },
      {
        title: 'Тот же смысл через def',
        paragraphs: [
          'Любую `lambda` можно переписать обычной функцией. Ниже обе делают одно и то же: берут число и умножают его на 2. Разница только в записи.',
        ],
        syntax: 'def double(x):\n    return x * 2',
        example: 'double = lambda x: x * 2\nprint(double(5))     # 10',
        note: 'Если логика длиннее одного выражения, сразу пишите `def`. `lambda` — только для короткого правила.',
      },
      {
        title: 'Один аргумент',
        paragraphs: [
          'Самый частый вид: один параметр слева от двоеточия. Справа с этим параметром считают ответ. Здесь `x` возводится в квадрат.',
        ],
        syntax: 'lambda x: x ** 2',
        example: 'square = lambda x: x ** 2\nprint(square(4))     # 16',
      },
      {
        title: 'Два аргумента',
        paragraphs: [
          'Аргументов может быть несколько — их перечисляют через запятую, как в `def`. Здесь функция складывает два числа.',
        ],
        syntax: 'lambda a, b: a + b',
        example: 'add = lambda a, b: a + b\nprint(add(2, 3))     # 5',
      },
      {
        title: 'Без аргументов',
        paragraphs: [
          'Если вход не нужен, слева от двоеточия ничего не пишут. При вызове скобки всё равно нужны: иначе функция не запустится.',
        ],
        syntax: 'lambda: выражение',
        example: 'hi = lambda: "привет"\nprint(hi())     # привет',
      },
    ],
  },
  {
    title: 'Генераторы коллекций',
    lead: 'Собрать список, словарь или множество в одну строку.',
    sections: [
      {
        title: 'Список без фильтра',
        paragraphs: [
          'Включение читается так: «положи выражение для каждого `x` из коллекции». Это короткий `for` с `append`. Это не генераторная функция с `yield`.',
        ],
        syntax: '[выражение for x in коллекция]',
        example: 'squares = [x ** 2 for x in range(1, 6)]\nprint(squares)     # [1, 4, 9, 16, 25]',
      },
      {
        title: 'Список с if',
        paragraphs: [
          'После коллекции можно написать `if`. В результат попадут только те `x`, для которых условие истинно.',
        ],
        syntax: '[выражение for x in коллекция if условие]',
        example: 'even = [x for x in range(10) if x % 2 == 0]\nprint(even)        # [0, 2, 4, 6, 8]',
      },
      {
        title: 'Словарь',
        paragraphs: [
          'Для словаря в фигурных скобках пишут пару `ключ: значение`.',
        ],
        syntax: '{ключ: значение for x in коллекция}',
        example: 'squares = {x: x ** 2 for x in range(4)}\nprint(squares)     # {0: 0, 1: 1, 2: 4, 3: 9}',
      },
      {
        title: 'Множество',
        paragraphs: [
          'Для множества в фигурных скобках одно выражение без двоеточия. Повторы исчезнут сами.',
        ],
        syntax: '{выражение for x in коллекция}',
        example: 'letters = {ch.lower() for ch in "AbA"}\nprint(letters)     # {"a", "b"}',
        note: '`[]` собирает список, `{ключ: значение}` — словарь, `{значение}` — множество. Круглые скобки `(x for x in ...)` дают генератор, который обходят один раз: это уже не список.',
      },
      {
        title: 'Когда включение не нужно',
        paragraphs: [
          'Если вы накапливаете одно число — сумму, произведение, максимум — новую коллекцию строить незачем. Пишите обычный цикл: `total += x`. Если строка с двумя `for` становится нечитаемой, тоже лучше обычный цикл.',
        ],
        syntax: 'total = 0\nfor x in nums:\n    total += x',
        example: 'total = 0\nfor x in [1, 2, 3]:\n    total += x\nprint(total)       # 6',
      },
    ],
  },
  {
    title: 'Исключения',
    lead: 'Как поймать ошибку и не дать программе упасть.',
    sections: [
      {
        title: 'Полная конструкция',
        paragraphs: [
          'Исключение — ошибка во время работы. Если её не поймать, программа остановится. Полный шаблон идёт строго в таком порядке: `try`, затем один или несколько `except`, затем необязательный `else`, затем необязательный `finally`.',
          '`except Тип as error` кладёт объект ошибки в переменную. Несколько `except` проверяются сверху вниз: более узкий тип пишите раньше.',
        ],
        syntax: 'try:\n    рискованный_код\nexcept ТипОшибки as error:\n    обработка\nexcept ДругойТип:\n    обработка\nelse:\n    если ошибки не было\nfinally:\n    выполнится всегда',
        example: 'try:\n    n = int("10")\nexcept ValueError as error:\n    print("не число", error)\nelse:\n    print("получилось", n)\nfinally:\n    print("конец попытки")',
        note: 'Ловите конкретные ошибки. Голый `except:` перехватит всё подряд, в том числе опечатки в коде.',
      },
      {
        title: 'try и except',
        paragraphs: [
          'Самый частый вид: рискованный ввод в `try` и отдельные обработчики на каждую ожидаемую ошибку. Здесь ловят нечисловой ввод и деление на ноль.',
        ],
        syntax: 'try:\n    рискованный_код\nexcept ТипОшибки:\n    обработка',
        example: 'try:\n    a = int(input())\n    b = int(input())\n    print(a / b)\nexcept ValueError:\n    print("Введите числа")\nexcept ZeroDivisionError:\n    print("На ноль нельзя")',
      },
      {
        title: 'ValueError',
        paragraphs: [
          '`ValueError` — значение не того вида. Часто ловят при `int(...)` и `float(...)`: человек ввёл `"abc"` вместо числа.',
        ],
        syntax: 'except ValueError:',
        example: 'try:\n    int("abc")\nexcept ValueError:\n    print("не число")',
      },
      {
        title: 'ZeroDivisionError',
        paragraphs: [
          '`ZeroDivisionError` — деление на ноль. Проверяйте, если делитель приходит из ввода или из расчёта.',
        ],
        syntax: 'except ZeroDivisionError:',
        example: 'try:\n    print(1 / 0)\nexcept ZeroDivisionError:\n    print("на ноль нельзя")',
      },
      {
        title: 'IndexError',
        paragraphs: [
          '`IndexError` — нет такого индекса у строки, списка или кортежа. Стоит ловить, если индекс считают, а не берут константой.',
        ],
        syntax: 'except IndexError:',
        example: 'try:\n    print([1, 2][5])\nexcept IndexError:\n    print("нет такого индекса")',
      },
      {
        title: 'KeyError',
        paragraphs: [
          '`KeyError` — в словаре нет такого ключа. Если ключ может отсутствовать, либо ловите ошибку, либо берите `.get()`.',
        ],
        syntax: 'except KeyError:',
        example: 'try:\n    print({"a": 1}["b"])\nexcept KeyError:\n    print("нет такого ключа")',
      },
      {
        title: 'TypeError',
        paragraphs: [
          '`TypeError` — операция не для этого типа: сложили число со строкой, вызвали `len` у числа, передали не тот аргумент.',
        ],
        syntax: 'except TypeError:',
        example: 'try:\n    print(len(25))\nexcept TypeError:\n    print("у числа нет длины")',
      },
      {
        title: 'NameError',
        paragraphs: [
          '`NameError` — имя не определено: опечатка в переменной или обращение до присваивания.',
        ],
        syntax: 'except NameError:',
        example: 'try:\n    print(unknown)\nexcept NameError:\n    print("имя не определено")',
      },
      {
        title: 'else',
        paragraphs: [
          '`else` у `try` выполняется, только если в `try` не было ошибки. Сюда ставят код, которому нужен успешный результат.',
        ],
        syntax: 'try:\n    код\nexcept ТипОшибки:\n    обработка\nelse:\n    если ошибки не было',
        example: 'try:\n    n = int("10")\nexcept ValueError:\n    print("не число")\nelse:\n    print("получилось", n)',
      },
      {
        title: 'finally',
        paragraphs: [
          '`finally` выполняется всегда: и при ошибке, и без неё. Так закрывают файл или пишут завершающее сообщение.',
        ],
        syntax: 'try:\n    код\nfinally:\n    выполнится всегда',
        example: 'try:\n    n = int("10")\nfinally:\n    print("конец попытки")',
      },
      {
        title: 'raise',
        paragraphs: [
          '`raise` создаёт исключение вручную, когда данные сами по себе некорректны.',
        ],
        syntax: 'raise ValueError("сообщение")',
        example: 'age = -1\nif age < 0:\n    raise ValueError("возраст не может быть отрицательным")',
      },
    ],
  },
];

const oop = [
  {
    title: 'Зачем нужны классы',
    lead: 'Сначала проблема: данные об одном герое лежат в разных переменных. Класс собирает их в одну вещь.',
    sections: [
      {
        title: 'Данные разъезжаются',
        paragraphs: [
          'Представьте игру. У героя есть имя и здоровье. Пока герой один, хватит двух переменных.',
          'Появился второй герой — уже четыре переменные. Появится двадцать — будет сорок. В них легко запутаться: глядя на `hp_7`, уже не скажешь, чьё это здоровье.',
        ],
        syntax: 'name_1 = "Arragoornis"\nhp_1 = 100',
        example: 'name_1 = "Arragoornis"\nhp_1 = 100\nname_2 = "Leegoolases"\nhp_2 = 80\nprint(name_1, hp_1)\nprint(name_2, hp_2)\n# Arragoornis 100\n# Leegoolases 80',
        note: 'Эти переменные связаны только в вашей голове. Для Python это четыре отдельных значения, и `hp_1` легко уменьшить вместо `hp_2`.',
      },
      {
        title: 'Класс собирает данные вместе',
        paragraphs: [
          'Класс — это описание: «у каждого героя есть имя и здоровье». Один герой после этого живёт в одной переменной, а не в пяти.',
          'К данным обращаются через точку. `hero.name` — имя **этого** героя, `hero.hp` — его здоровье. Перепутать данные двух героев теперь трудно.',
        ],
        syntax: 'class Hero:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp',
        example: 'class Hero:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\nhero = Hero("Arragoornis", 100)\nprint(hero.name, hero.hp)\n# Arragoornis 100',
      },
      {
        title: 'Рядом с данными живут действия',
        paragraphs: [
          'В классе держат не только данные, но и то, что вещь умеет делать. Герой умеет получать удар — значит, это действие описывают внутри класса.',
          'Получается самостоятельная вещь: она хранит своё состояние и сама умеет его менять. Отдельная функция «отнять здоровье у кого-то» больше не нужна.',
        ],
        syntax: 'class Hero:\n    def take_hit(self):\n        self.hp -= 10',
        example: 'class Hero:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\n    def take_hit(self):\n        self.hp -= 10\n\nhero = Hero("Arragoornis", 100)\nhero.take_hit()\nprint(hero.hp)\n# 90',
      },
    ],
  },
  {
    title: 'Класс и объект',
    lead: 'Класс — чертёж. Объект — конкретная вещь, построенная по этому чертежу.',
    sections: [
      {
        title: 'Класс — это чертёж',
        paragraphs: [
          'Класс описывает, какой бывает вещь: какие у неё данные и какие действия. Сам он вещью не является.',
          'В чертеже дома нельзя жить. По нему только строят дома. Поэтому `Character` — это не герой, а описание того, каким бывает герой.',
        ],
        syntax: 'class Character:\n    ...',
        example: 'class Character:\n    pass\n\nprint(Character)\n# <class \'__main__.Character\'>',
      },
      {
        title: 'Объект — это дом по чертежу',
        paragraphs: [
          'Объект — одна конкретная вещь, построенная по классу. Его же называют «экземпляр»: это одно и то же слово.',
          'Чтобы построить объект, имя класса пишут со скобками: `Character("Zonanas")`. В скобках передают то, чем этот объект отличается от других.',
        ],
        syntax: 'объект = Character(...)',
        example: 'class Character:\n    def __init__(self, name):\n        self.name = name\n\nhero = Character("Zonanas")\nprint(hero.name)\n# Zonanas',
      },
      {
        title: 'Из одного класса — много объектов',
        paragraphs: [
          'По одному чертежу строят целую улицу домов: планировка одинаковая, жильцы разные. С классом так же.',
          'Каждый объект хранит свои данные. Изменили здоровье одного героя — второй об этом даже не узнает.',
        ],
        syntax: 'a = Character("Zonanas")\nb = Character("Landalif")',
        example: 'class Character:\n    def __init__(self, name):\n        self.name = name\n\na = Character("Zonanas")\nb = Character("Landalif")\nprint(a.name)\nprint(b.name)\n# Zonanas\n# Landalif',
      },
    ],
  },
  {
    title: '__init__ — конструктор',
    lead: '`__init__` запускается сам в момент создания объекта и задаёт его начальные данные.',
    sections: [
      {
        title: 'Рождение объекта',
        paragraphs: [
          'Когда пишут `Character("Zonanas", 50)`, Python делает две вещи: создаёт пустой объект и сразу вызывает у него `__init__`.',
          'Задача `__init__` одна — разложить пришедшие значения по атрибутам объекта. Поэтому его и называют конструктором: он «собирает» новую вещь.',
        ],
        syntax: 'def __init__(self, name, power):\n    self.name = name\n    self.power = power',
        example: 'class Character:\n    def __init__(self, name, power):\n        self.name = name\n        self.power = power\n\nhero = Character("Zonanas", 50)\nprint(hero.name, hero.power)\n# Zonanas 50',
        note: 'Писать `hero.__init__("Zonanas", 50)` руками не нужно. Python вызывает этот метод сам, ровно один раз при создании объекта.',
      },
      {
        title: 'Аргументы попадают в атрибуты',
        paragraphs: [
          'Слева от `=` стоит `self.name` — это ячейка внутри объекта. Справа — `name`, обычный аргумент функции, который исчезнет после выхода из метода.',
          'Строка `self.name = name` как раз переносит значение из аргумента в объект. Без неё имя пришло бы в класс и тут же потерялось.',
        ],
        syntax: 'self.name = name',
        example: 'class Character:\n    def __init__(self, name):\n        self.name = name\n\nhero = Character("Zonanas")\nprint(hero.name)\n# Zonanas',
      },
      {
        title: 'Общее стартовое значение',
        paragraphs: [
          'Не каждое значение нужно передавать в скобках. Если у всех новых объектов одинаковое начало, его пишут прямо в `__init__`.',
          'Все герои рождаются со здоровьем `100`. Значит, `hp` не спрашивают при создании, а задают внутри.',
        ],
        syntax: 'self.hp = 100',
        example: 'class Character:\n    def __init__(self, name):\n        self.name = name\n        self.hp = 100\n\nhero = Character("Zonanas")\nprint(hero.hp)\n# 100',
      },
    ],
  },
  {
    title: 'self — ссылка на себя',
    lead: '`self` — это текущий объект внутри метода. Читайте его как «я» или «мой собственный».',
    sections: [
      {
        title: 'Зачем он нужен',
        paragraphs: [
          'Класс один, а объектов по нему много. Когда метод работает, Python должен знать, у какого именно объекта он выполняется.',
          'Этот объект и приходит первым аргументом под именем `self`. Через него метод достаёт данные: `self.name` — имя того героя, которому метод сейчас принадлежит.',
        ],
        syntax: 'def метод(self):\n    ...',
        example: 'class Character:\n    def __init__(self, name):\n        self.name = name\n\n    def say_name(self):\n        print(self.name)\n\nCharacter("Zonanas").say_name()\n# Zonanas',
      },
      {
        title: 'Правило «я»',
        paragraphs: [
          'Строку `self.hp -= 10` читают как «уменьшить **моё** здоровье на 10». Не здоровье класса и не здоровье соседа.',
          'Если написать просто `hp -= 10`, Python будет искать обычную переменную `hp` и не найдёт её. Данные объекта живут только за точкой после `self`.',
        ],
        syntax: 'self.hp -= 10',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\n    def take_hit(self):\n        self.hp -= 10\n\nhero = Character("Zonanas", 100)\nhero.take_hit()\nprint(hero.hp)\n# 90',
      },
      {
        title: 'self подставляется сам',
        paragraphs: [
          'В методе два параметра, а при вызове `p1.attack(p2)` передают один аргумент. Кажется, что одного не хватает.',
          'На самом деле Python сам подставляет объект слева от точки. Вызов превращается в `Character.attack(p1, p2)`: `p1` становится `self`, а `p2` — вторым аргументом.',
        ],
        syntax: 'p1.attack(p2)',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\n    def attack(self, other):\n        other.hp -= 10\n\np1 = Character("Batman", 100)\np2 = Character("Joker", 100)\np1.attack(p2)\nprint(p2.hp)\n# 90',
      },
    ],
  },
  {
    title: 'Атрибуты экземпляра',
    lead: 'Личные данные одного объекта: у каждого героя своё имя, своё здоровье и свой рюкзак.',
    sections: [
      {
        title: 'Личные данные',
        paragraphs: [
          'Всё, что задано через `self` в `__init__`, принадлежит одному конкретному объекту. Такие данные и называют атрибутами экземпляра.',
          'Два героя не делят имя и здоровье. Это как два школьника с одинаковыми портфелями: портфели похожи, а тетради внутри у каждого свои.',
        ],
        syntax: 'self.name = name\nself.hp = hp',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\na = Character("Zonanas", 100)\nb = Character("Landalif", 80)\nprint(a.name, a.hp)\nprint(b.name, b.hp)\n# Zonanas 100\n# Landalif 80',
      },
      {
        title: 'Свой список в __init__',
        paragraphs: [
          'Рюкзак героя — тоже личные данные. Пустой список создают внутри `__init__`, и тогда на каждый вызов класса появляется новый список.',
          'Воин поднял меч — меч лежит только у воина. У мага рюкзак остался пустым, как и должно быть.',
        ],
        syntax: 'self.inventory = []',
        example: 'class Hero:\n    def __init__(self, name):\n        self.name = name\n        self.inventory = []\n\n    def pick_item(self, item):\n        self.inventory.append(item)\n\nwarrior = Hero("Warrior")\nmage = Hero("Mage")\nwarrior.pick_item("Sword")\nprint(warrior.inventory)\nprint(mage.inventory)\n# [\'Sword\']\n# []',
      },
      {
        title: 'Читают через точку',
        paragraphs: [
          'Снаружи атрибут читают как `объект.имя`. Скобок нет: это не вызов, а просто значение, которое лежит внутри.',
          'Записывают так же: `hero.hp = 50` положит новое значение в этого героя. Другие герои не изменятся.',
        ],
        syntax: 'объект.атрибут',
        example: 'class Hero:\n    def __init__(self, hp):\n        self.hp = hp\n\nhero = Hero(100)\nhero.hp = 50\nprint(hero.hp)\n# 50',
      },
    ],
  },
  {
    title: 'Атрибуты класса',
    lead: 'Данные, общие сразу для всех объектов: гравитация мира, адрес сервера, счётчик созданных героев.',
    sections: [
      {
        title: 'Одно значение на всех',
        paragraphs: [
          'Имя, записанное прямо в теле класса (без `self`), принадлежит самому классу. Оно одно, сколько бы объектов ни создали.',
          'Так хранят то, что у всех одинаково. Гравитация мира не зависит от того, сколько в нём персонажей.',
        ],
        syntax: 'class World:\n    gravity = 9.8',
        example: 'class World:\n    gravity = 9.8\n\nprint(World.gravity)\n# 9.8',
      },
      {
        title: 'Счётчик созданных объектов',
        paragraphs: [
          'Число «сколько героев уже создано» нельзя держать внутри героя: у каждого была бы своя единица, а общего счёта не получилось бы.',
          'Счётчик живёт в классе, а увеличивают его в `__init__` — то есть на каждом рождении нового объекта.',
        ],
        syntax: 'class Hero:\n    total = 0\n\n    def __init__(self, name):\n        self.name = name\n        Hero.total += 1',
        example: 'class Hero:\n    total = 0\n\n    def __init__(self, name):\n        self.name = name\n        Hero.total += 1\n\nHero("Warrior")\nHero("Mage")\nHero("Rogue")\nprint(Hero.total)\n# 3',
        note: '`Hero.total` — общее на весь тип. `self.name` — личные данные одного объекта. Это разные вещи.',
      },
      {
        title: 'Запись через объект не меняет класс',
        paragraphs: [
          'Читать общее значение можно и через объект: Python сначала смотрит у объекта, а если там пусто — у класса.',
          'А вот запись работает иначе. `hero.total = 100` не меняет `Hero.total`, а создаёт личный атрибут у этого героя с тем же именем. У класса значение остаётся прежним.',
        ],
        syntax: 'объект.поле = значение',
        example: 'class Hero:\n    total = 3\n\nhero = Hero()\nhero.total = 100\nprint(hero.total)\nprint(Hero.total)\n# 100\n# 3',
        note: 'Менять общее значение нужно через класс: `Hero.total = ...`. Через объект вы заведёте новое личное поле и не заметите этого.',
      },
      {
        title: 'Ловушка: общий рюкзак',
        paragraphs: [
          'Список в теле класса создаётся один раз — в момент чтения кода класса. Дальше все объекты смотрят на один и тот же список в памяти.',
          'Поэтому воин поднял меч, а меч внезапно оказался и у мага. Если у каждого должен быть свой набор, список создают в `__init__`, как в предыдущей теме.',
        ],
        syntax: 'class Hero:\n    inventory = []',
        example: 'class Hero:\n    inventory = []\n\n    def __init__(self, name):\n        self.name = name\n\n    def pick_item(self, item):\n        self.inventory.append(item)\n\nwarrior = Hero("Warrior")\nmage = Hero("Mage")\nwarrior.pick_item("Sword")\nprint(mage.inventory)\n# [\'Sword\']',
        note: 'Числу или строке в теле класса ничего не грозит. Опасны именно изменяемые объекты: список, словарь, множество.',
      },
    ],
  },
  {
    title: 'Методы',
    lead: 'Метод — функция внутри класса. Она работает с данными объекта и первым аргументом принимает `self`.',
    sections: [
      {
        title: 'Действие объекта',
        paragraphs: [
          'Метод описывает, что вещь умеет делать. Герой умеет атаковать — значит, у класса есть метод `attack`.',
          'Метод может менять и чужой объект, если его передали аргументом. Здесь атакующий читает своё имя через `self`, а здоровье отнимает у `other`.',
        ],
        syntax: 'def attack(self, other):\n    ...',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\n    def attack(self, other):\n        print(f"{self.name} атакует {other.name}!")\n        other.hp -= 10\n\np1 = Character("Batman", 100)\np2 = Character("Joker", 100)\np1.attack(p2)\nprint(p2.hp)\n# Batman атакует Joker!\n# 90',
      },
      {
        title: 'Метод возвращает ответ',
        paragraphs: [
          'Если от метода нужен ответ — число или `True` / `False` — его отдают через `return`. Тогда ответ можно сохранить в переменную или проверить в `if`.',
          'Здесь метод отвечает на вопрос «этот персонаж совершеннолетний?». Граница включает 18 лет, поэтому в сравнении стоит `>=`.',
        ],
        syntax: 'def is_adult(self):\n    return self.age >= 18',
        example: 'class Character:\n    def __init__(self, age):\n        self.age = age\n\n    def is_adult(self):\n        return self.age >= 18\n\nprint(Character(20).is_adult())\nprint(Character(17).is_adult())\n# True\n# False',
      },
      {
        title: 'Печать и возврат — разное',
        paragraphs: [
          '`print` показывает текст на экране и ничего не отдаёт программе. `return` наоборот: отдаёт значение туда, где метод вызвали.',
          'Приветствие достаточно напечатать — его никуда не сохраняют. А ответ «взрослый или нет» нужно возвращать, иначе программа его не увидит.',
        ],
        syntax: 'def say_hello(self):\n    print(f"Привет, меня зовут {self.name}")',
        example: 'class Character:\n    def __init__(self, name):\n        self.name = name\n\n    def say_hello(self):\n        print(f"Привет, меня зовут {self.name}")\n\nCharacter("Zonanas").say_hello()\n# Привет, меня зовут Zonanas',
        note: 'Метод без `return` возвращает `None`. Если вместо возврата написать `print`, сохранить результат будет нечего.',
      },
      {
        title: 'Проверка внутри метода',
        paragraphs: [
          'Правило «так делать нельзя» кладут в тот метод, который меняет данные. Тогда проверка сработает при любом вызове.',
          'Если проверять снаружи, её однажды забудут написать, и в кошелёк попадёт отрицательная сумма. Внутри метода забыть невозможно.',
        ],
        syntax: 'def add(self, amount):\n    if amount <= 0:\n        return\n    self.gold += amount',
        example: 'class Wallet:\n    def __init__(self, gold):\n        self.gold = gold\n\n    def add(self, amount):\n        if amount <= 0:\n            print("Сумма должна быть положительной")\n            return\n        self.gold += amount\n\nw = Wallet(1000)\nw.add(-100)\nprint(w.gold)\n# Сумма должна быть положительной\n# 1000',
      },
      {
        title: 'Нельзя уйти в минус',
        paragraphs: [
          'Второе типичное правило: нельзя снять больше, чем лежит. Метод сообщает о проблеме и выходит через пустой `return`.',
          'Главное — баланс при этом не меняется. Данные остаются правильными, даже если вызов был неправильным.',
        ],
        syntax: 'def spend(self, amount):\n    if amount > self.gold:\n        return\n    self.gold -= amount',
        example: 'class Wallet:\n    def __init__(self, gold):\n        self.gold = gold\n\n    def spend(self, amount):\n        if amount > self.gold:\n            print("Недостаточно средств")\n            return\n        self.gold -= amount\n\nw = Wallet(1000)\nw.spend(2000)\nprint(w.gold)\n# Недостаточно средств\n# 1000',
      },
      {
        title: 'Защита от пустого списка',
        paragraphs: [
          'Метод считает средний балл. Если очков ещё нет, длина списка равна нулю, а делить на ноль нельзя — программа упадёт.',
          'Поэтому сначала проверяют, пуст ли список, и возвращают `0`. Правило выбирают заранее и придерживаются его.',
        ],
        syntax: 'def average(self):\n    if not self.scores:\n        return 0\n    return sum(self.scores) / len(self.scores)',
        example: 'class Hero:\n    def __init__(self):\n        self.scores = []\n\n    def average(self):\n        if not self.scores:\n            return 0\n        return sum(self.scores) / len(self.scores)\n\nprint(Hero().average())\n# 0',
      },
    ],
  },
  {
    title: 'Инкапсуляция',
    lead: 'Как показать, что поле внутреннее. В Python это договорённость, а не замок.',
    sections: [
      {
        title: 'Одно подчёркивание _имя',
        paragraphs: [
          'В некоторых языках есть слово `private`, которое запрещает доступ к полю. В Python такого запрета нет.',
          'Вместо запрета ставят подчёркивание: `self._port`. Это значит «поле внутреннее, снаружи лучше не трогать». Читать и менять его никто не мешает — это просьба, а не замок.',
        ],
        syntax: 'self._port = 5432',
        example: 'class DatabaseConnection:\n    def __init__(self):\n        self._port = 5432\n\ndb = DatabaseConnection()\nprint(db._port)\ndb._port = 9090\nprint(db._port)\n# 5432\n# 9090',
        note: 'Одно подчёркивание — ленточка «не входить», а не стена. Предупреждение людям, не команда интерпретатору.',
      },
      {
        title: 'Два подчёркивания __имя',
        paragraphs: [
          'Два подчёркивания включают искажение имени: Python втихую переименовывает поле в `_ИмяКласса__поле`.',
          'Из-за этого обращение к `acc.__balance` снаружи даёт ошибку: поля с таким именем действительно нет.',
        ],
        syntax: 'self.__balance = balance',
        example: 'class Account:\n    def __init__(self, balance):\n        self.__balance = balance\n\nacc = Account(1000)\nprint(acc.__balance)\n# AttributeError: \'Account\' object has no attribute \'__balance\'',
      },
      {
        title: 'Обход через служебное имя',
        paragraphs: [
          'Значение всё равно можно прочитать, если знать новое имя: `acc._Account__balance`. Никакой защиты здесь нет.',
          'Зачем тогда двойное подчёркивание? Чтобы наследник со своим полем `__balance` случайно не затёр поле родителя. Пароли и секреты так не прячут.',
        ],
        syntax: 'объект._Account__balance',
        example: 'class Account:\n    def __init__(self, balance):\n        self.__balance = balance\n\nacc = Account(1000)\nprint(acc._Account__balance)\n# 1000',
        note: 'В обычном коде чаще хватает одного `_`. Двойное подчёркивание берут, когда класс рассчитан на наследование.',
      },
    ],
  },
  {
    title: '@property',
    lead: 'Снаружи выглядит как обычное поле через точку. Внутри — метод, который может проверить или посчитать значение.',
    sections: [
      {
        title: 'Геттер: поле без скобок',
        paragraphs: [
          '`@property` превращает метод в поле для чтения. Пишут `person.age`, без скобок, будто это обычный атрибут.',
          'Внутри при этом выполняется код: можно отдать спрятанное `_age`, а можно посчитать значение на месте, например возраст по дате рождения.',
        ],
        syntax: '@property\ndef age(self):\n    return self._age',
        example: 'class Person:\n    def __init__(self, age):\n        self._age = age\n\n    @property\n    def age(self):\n        return self._age\n\nbob = Person(25)\nprint(bob.age)\n# 25',
      },
      {
        title: 'Сеттер с проверкой',
        paragraphs: [
          '`@age.setter` срабатывает при записи: `person.age = -50`. Обычный атрибут проглотил бы любое значение, а сеттер сначала проверит его.',
          'Снаружи код не меняется — как будто просто присвоили значение. А внутри отрицательный возраст уже не пройдёт.',
        ],
        syntax: '@age.setter\ndef age(self, value):\n    ...',
        example: 'class Person:\n    def __init__(self, age):\n        self._age = age\n\n    @property\n    def age(self):\n        return self._age\n\n    @age.setter\n    def age(self, value):\n        if value < 0:\n            print("Возраст не может быть отрицательным, будет 0")\n            self._age = 0\n        else:\n            self._age = value\n\nbob = Person(25)\nbob.age = -50\nprint(bob.age)\n# Возраст не может быть отрицательным, будет 0\n# 0',
        note: 'Методы `get_age()` и `set_age()` в Python обычно не пишут. Оставляют удобную точку, а проверку прячут в property.',
      },
    ],
  },
  {
    title: 'Наследование',
    lead: 'Общее пишут один раз в родителе. Дети получают это даром и добавляют своё.',
    sections: [
      {
        title: 'Общее — в родителя',
        paragraphs: [
          'У воина и у мага одинаковые имя и здоровье. Если написать это в двух классах, любая правка придётся делать дважды и где-нибудь о ней забудут.',
          'Поэтому общее описывают в родительском классе, а в скобках у ребёнка указывают, от кого он наследует: `class Mage(Character)`.',
        ],
        syntax: 'class Mage(Character):\n    ...',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\n    def move(self):\n        print(f"{self.name} идет вперед.")\n\nclass Mage(Character):\n    def cast_spell(self):\n        print(f"{self.name} кидает огненный шар!")\n\ngandalf = Mage("Landalif", 50)\ngandalf.cast_spell()\n# Landalif кидает огненный шар!',
      },
      {
        title: 'Ребёнок умеет родительское и своё',
        paragraphs: [
          'Объект дочернего класса вызывает методы родителя, хотя в самом ребёнке их нет. Python не нашёл метод у `Mage` и пошёл искать выше, у `Character`.',
          'Отношение здесь читается словом «является»: маг является персонажем. Если такое предложение звучит странно, наследование, скорее всего, не подходит.',
        ],
        syntax: 'объект.move()',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\n    def move(self):\n        print(f"{self.name} идет вперед.")\n\nclass Mage(Character):\n    pass\n\nMage("Landalif", 50).move()\n# Landalif идет вперед.',
      },
    ],
  },
  {
    title: 'super()',
    lead: '`super()` вызывает метод родителя. Нужен, чтобы дополнить его работу, а не потерять её.',
    sections: [
      {
        title: 'Свой метод затирает родительский',
        paragraphs: [
          'Если у ребёнка есть метод с тем же именем, будет работать только его версия. Родительскую Python даже не посмотрит — это называют переопределением.',
          'Для `__init__` это опасно: напишете свой и забудете про родительский — имя и здоровье не зададутся. Объект родится пустым.',
        ],
        syntax: 'class Mage(Character):\n    def __init__(self, name, hp, mana):\n        ...',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\nclass Mage(Character):\n    def __init__(self, mana):\n        self.mana = mana\n\nm = Mage(100)\nprint(m.name)\n# AttributeError: \'Mage\' object has no attribute \'name\'',
      },
      {
        title: 'Сначала общее, потом своё',
        paragraphs: [
          '`super().__init__(name, hp)` просит родителя сделать его обычную работу: разложить имя и здоровье.',
          'После этого ребёнок дописывает то, чего у родителя нет, — ману. Порядок именно такой: сначала общее, затем личное.',
        ],
        syntax: 'super().__init__(name, hp)\nself.mana = mana',
        example: 'class Character:\n    def __init__(self, name, hp):\n        self.name = name\n        self.hp = hp\n\nclass Mage(Character):\n    def __init__(self, name, hp, mana):\n        super().__init__(name, hp)\n        self.mana = mana\n\nm = Mage("Landalif", 60, 100)\nprint(m.name, m.hp, m.mana)\n# Landalif 60 100',
      },
    ],
  },
  {
    title: 'Множественное наследование и MRO',
    lead: 'У класса может быть несколько родителей. Порядок поиска метода называется MRO.',
    sections: [
      {
        title: 'Два родителя через запятую',
        paragraphs: [
          'Пегас — и лошадь, и птица. В скобках перечисляют оба класса через запятую.',
          'Он получает `run` от лошади и `fly` от птицы. Своего кода у `Pegasus` нет вообще, а умеет он всё сразу.',
        ],
        syntax: 'class Pegasus(Horse, Bird):\n    pass',
        example: 'class Horse:\n    def run(self):\n        print("Тыгыдык-тыгыдык")\n\nclass Bird:\n    def fly(self):\n        print("Вжух! Я лечу!")\n\nclass Pegasus(Horse, Bird):\n    pass\n\np = Pegasus()\np.run()\np.fly()\n# Тыгыдык-тыгыдык\n# Вжух! Я лечу!',
      },
      {
        title: 'Конфликт одинаковых имён',
        paragraphs: [
          'А если метод с одним именем есть у обоих родителей? И лошадь, и птица умеют `noise()` — кто победит?',
          'Побеждает тот, кто левее в скобках. `Pegasus(Horse, Bird)` сначала смотрит в `Horse`, поэтому пегас ржёт, а не чирикает.',
        ],
        syntax: 'class Pegasus(Horse, Bird):\n    pass',
        example: 'class Horse:\n    def noise(self):\n        print("Иго-го!")\n\nclass Bird:\n    def noise(self):\n        print("Чик-чирик!")\n\nclass Pegasus(Horse, Bird):\n    pass\n\nPegasus().noise()\n# Иго-го!',
      },
      {
        title: 'Очередь поиска: mro()',
        paragraphs: [
          'Python держит для каждого класса список: где искать метод первым, где вторым и так далее. Этот список и называется MRO.',
          'Посмотреть его можно через `Класс.mro()`. Если запутались, кто чей родитель, — распечатайте очередь и всё станет видно.',
        ],
        syntax: 'print(Pegasus.mro())',
        example: 'class Horse:\n    pass\n\nclass Bird:\n    pass\n\nclass Pegasus(Horse, Bird):\n    pass\n\nprint([cls.__name__ for cls in Pegasus.mro()])\n# [\'Pegasus\', \'Horse\', \'Bird\', \'object\']',
        note: 'В конце очереди всегда стоит `object` — общий предок всех классов в Python.',
      },
      {
        title: 'Ромб: общий предок',
        paragraphs: [
          'Бывает, что оба родителя сами наследуют один класс. Схему рисуют ромбом: `D` наследует `B` и `C`, а те оба — `A`.',
          'У `D` своего метода нет, поэтому берётся метод `B`. Внутри стоит `super()`, и дальше вызовы идут по очереди, пока не дойдут до `A`.',
        ],
        syntax: 'class D(B, C):\n    pass',
        example: 'class A:\n    def method(self):\n        print("A")\n\nclass B(A):\n    def method(self):\n        print("B")\n        super().method()\n\nclass C(A):\n    def method(self):\n        print("C")\n        super().method()\n\nclass D(B, C):\n    pass\n\nD().method()\n# B\n# C\n# A',
      },
      {
        title: 'Почему после B идёт C, а не A',
        paragraphs: [
          'В классе `B` написано `super().method()`. Кажется, что родитель `B` — это `A`, и сейчас напечатается `A`. Но печатается `C`.',
          'Дело в том, что `super()` смотрит не на скобки класса `B`, а на MRO **того объекта**, с которым работает. Объект у нас типа `D`, его очередь — `D, B, C, A`. Следующий после `B` — `C`, и только из `C` вызов доходит до `A`.',
        ],
        syntax: 'super().method()\n# следующий класс в MRO объекта',
        example: 'class A:\n    def method(self):\n        print("A")\n\nclass B(A):\n    def method(self):\n        print("B")\n        super().method()\n\nclass C(A):\n    def method(self):\n        print("C")\n        super().method()\n\nclass D(B, C):\n    pass\n\nprint([cls.__name__ for cls in D.mro()])\n# [\'D\', \'B\', \'C\', \'A\', \'object\']',
        note: 'Если бы `super()` всегда прыгал к родителю из скобок, `A.method` вызвался бы дважды. MRO нужен как раз для того, чтобы общий предок сработал один раз.',
      },
    ],
  },
  {
    title: 'Полиморфизм',
    lead: 'Один и тот же код работает с разными объектами, если они умеют одно и то же действие.',
    sections: [
      {
        title: 'Одно действие, разные объекты',
        paragraphs: [
          'Воин, маг и дракон бьют по-разному, но метод у всех называется одинаково — `attack()`.',
          'Поэтому функция может просто пройти по списку и сказать каждому «атакуй». Ей не нужно знать, кто именно перед ней: каждый сделает по-своему.',
        ],
        syntax: 'for unit in units:\n    unit.attack()',
        example: 'class Warrior:\n    def attack(self):\n        print("Воин бьет мечом: ХРЯСЬ!")\n\nclass Dragon:\n    def attack(self):\n        print("Дракон дышит огнем: ПФФФ!")\n\ndef mass_battle(units):\n    for unit in units:\n        unit.attack()\n\nmass_battle([Warrior(), Dragon()])\n# Воин бьет мечом: ХРЯСЬ!\n# Дракон дышит огнем: ПФФФ!',
      },
      {
        title: 'Утиная типизация',
        paragraphs: [
          'В Python не спрашивают «ты наследник класса Animal?». Важно только то, что объект умеет делать.',
          'Правило описывают шуткой: ходит как утка и крякает как утка — значит, для кода это утка. Родство классов здесь ни при чём, нужен только нужный метод.',
        ],
        syntax: 'unit.attack()',
        example: 'class Robot:\n    def attack(self):\n        print("Робот стреляет лазером: ПИУ!")\n\ndef mass_battle(units):\n    for unit in units:\n        unit.attack()\n\nmass_battle([Robot()])\n# Робот стреляет лазером: ПИУ!',
        note: 'Если у объекта нет метода `attack()`, будет `AttributeError`. Пока имена методов совпадают — всё работает.',
      },
      {
        title: 'Новый класс не требует правок',
        paragraphs: [
          'Главная выгода — общий код не переписывают. Добавили `Healer` с методом `attack()`, и он встал в тот же цикл.',
          'Функцию `mass_battle` при этом не тронули ни одной строкой. Без полиморфизма там росла бы лестница из `if`: «если воин… если маг… если дракон…».',
        ],
        syntax: 'units.append(НовыйКласс())',
        example: 'class Healer:\n    def attack(self):\n        print("Лекарь неловко машет посохом")\n\ndef mass_battle(units):\n    for unit in units:\n        unit.attack()\n\nmass_battle([Healer()])\n# Лекарь неловко машет посохом',
      },
    ],
  },
  {
    title: 'Абстрактные классы',
    lead: 'Каркас с обязательными методами: пока наследник их не напишет, объект создать нельзя.',
    sections: [
      {
        title: 'Каркас с обязательными методами',
        paragraphs: [
          'Иногда нужно потребовать: любой враг обязан уметь атаковать и умирать. Как именно — пусть решает наследник.',
          'Для этого берут модуль `abc`: класс наследует `ABC`, а обязательные методы помечают декоратором `@abstractmethod`. Обычные готовые методы рядом тоже можно оставить.',
        ],
        syntax: 'from abc import ABC, abstractmethod\n\nclass Enemy(ABC):\n    @abstractmethod\n    def attack(self):\n        pass',
        example: 'from abc import ABC, abstractmethod\n\nclass Enemy(ABC):\n    @abstractmethod\n    def attack(self):\n        pass\n\n    def scream(self):\n        print("ААРГХ!!!")\n\nEnemy()\n# TypeError: Can\'t instantiate abstract class Enemy with abstract method attack',
        note: 'Сам абстрактный класс — только идея, не конкретный монстр. Поэтому `Enemy()` создать нельзя.',
      },
      {
        title: 'Неполный наследник нельзя создать',
        paragraphs: [
          'Орк реализовал `attack`, а про `die` забыл. Python не даст создать такой объект и скажет, какого метода не хватает.',
          'Это удобно: ошибку видно сразу при создании, а не через час игры, когда орка впервые попробуют убить.',
        ],
        syntax: 'class LazyOrc(Enemy):\n    def attack(self):\n        ...',
        example: 'from abc import ABC, abstractmethod\n\nclass Enemy(ABC):\n    @abstractmethod\n    def attack(self):\n        pass\n\n    @abstractmethod\n    def die(self):\n        pass\n\nclass LazyOrc(Enemy):\n    def attack(self):\n        print("Орк лениво машет дубиной")\n\nLazyOrc()\n# TypeError: Can\'t instantiate abstract class LazyOrc with abstract method die',
      },
      {
        title: 'Когда все методы на месте',
        paragraphs: [
          'Как только наследник написал все обязательные методы, объект создаётся нормально.',
          'Вместе с ними он получает и обычные методы родителя — здесь готовый `scream()`. Абстрактный класс задал правила и подарил общий код.',
        ],
        syntax: 'class ProperOrc(Enemy):\n    def attack(self):\n        ...\n\n    def die(self):\n        ...',
        example: 'from abc import ABC, abstractmethod\n\nclass Enemy(ABC):\n    @abstractmethod\n    def attack(self):\n        pass\n\n    @abstractmethod\n    def die(self):\n        pass\n\n    def scream(self):\n        print("ААРГХ!!!")\n\nclass ProperOrc(Enemy):\n    def attack(self):\n        print("Орк яростно бьет!")\n\n    def die(self):\n        print("Орк падает замертво.")\n\nProperOrc().scream()\n# ААРГХ!!!',
      },
    ],
  },
  {
    title: '__str__ и __repr__',
    lead: 'Методы с подчёркиваниями по краям вызывает сам Python. Эти два отвечают за текст объекта.',
    sections: [
      {
        title: 'Без них виден только адрес',
        paragraphs: [
          'Напечатайте объект без подготовки — увидите имя класса и адрес в памяти. Человеку из этого ничего не понятно.',
          'Такие методы, как `__str__`, называют дандер-методами: от английского «double underscore». Руками их почти не вызывают — за них это делает синтаксис Python.',
        ],
        syntax: 'print(объект)',
        example: 'class Item:\n    def __init__(self, name, value):\n        self.name = name\n        self.value = value\n\nprint(Item("Меч", 100))\n# <__main__.Item object at 0x...>',
      },
      {
        title: '__str__ — для человека',
        paragraphs: [
          '`print(obj)` и `str(obj)` заглядывают в метод `__str__`. Что он вернёт, то и покажут.',
          'Поэтому внутри описывают понятный текст: название предмета и цену. Никакой магии — обычный метод, который Python вызывает за вас.',
        ],
        syntax: 'def __str__(self):\n    return f"{self.name} (цена: {self.value})"',
        example: 'class Item:\n    def __init__(self, name, value):\n        self.name = name\n        self.value = value\n\n    def __str__(self):\n        return f"{self.name} (цена: {self.value})"\n\nprint(Item("Меч", 100))\n# Меч (цена: 100)',
      },
      {
        title: 'Строку возвращают, а не печатают',
        paragraphs: [
          'Внутри `__str__` пишут `return`, а не `print`. Печатать будет тот, кто вызвал: `print` сам выведет полученную строку.',
          'Если внутри поставить `print` и забыть `return`, метод отдаст `None`. А текст объекта часто нужен не на экране, а внутри другой строки или в логе.',
        ],
        syntax: 'return f"{self.name} (цена: {self.value})"',
        example: 'class Item:\n    def __init__(self, name, value):\n        self.name = name\n        self.value = value\n\n    def __str__(self):\n        return f"{self.name} (цена: {self.value})"\n\ntext = str(Item("Меч", 100))\nprint("в инвентаре: " + text)\n# в инвентаре: Меч (цена: 100)',
      },
      {
        title: '__repr__ — для программиста',
        paragraphs: [
          '`__repr__` — второй текст объекта, для логов и отладки. Его показывает функция `repr()`.',
          'Хороший `__repr__` похож на код создания объекта: по строке `Item(\'Меч\', 100)` сразу видно, из чего объект собран.',
        ],
        syntax: 'def __repr__(self):\n    return f"Item(\'{self.name}\', {self.value})"',
        example: 'class Item:\n    def __init__(self, name, value):\n        self.name = name\n        self.value = value\n\n    def __repr__(self):\n        return f"Item(\'{self.name}\', {self.value})"\n\nsword = Item("Меч", 100)\nprint(repr(sword))\nprint(sword)\n# Item(\'Меч\', 100)\n# Item(\'Меч\', 100)',
        note: 'Если писать только один метод, пишите `__repr__`. При отсутствии `__str__` Python подставит его и в `print`.',
      },
    ],
  },
  {
    title: '__eq__ — сравнение ==',
    lead: 'Знак `==` для объектов вызывает метод `__eq__`. В нём решают, какие объекты считать равными.',
    sections: [
      {
        title: 'Без __eq__ сравниваются адреса',
        paragraphs: [
          'Два кошелька с одинаковым золотом по умолчанию не равны: Python сравнивает не содержимое, а сам объект — тот же он или другой.',
          'Метод `__eq__` меняет правило. Внутри пишут, какие данные важны, и `==` начинает сравнивать по смыслу.',
        ],
        syntax: 'def __eq__(self, other):\n    return self.gold == other.gold',
        example: 'class Wallet:\n    def __init__(self, gold):\n        self.gold = gold\n\n    def __eq__(self, other):\n        return self.gold == other.gold\n\nprint(Wallet(50) == Wallet(50))\n# True',
      },
      {
        title: 'Сравнивают выбранное поле',
        paragraphs: [
          'Сравнивать все поля подряд обычно не нужно. Решают, что именно делает объекты «одним и тем же».',
          'В магазине это код товара: меч и клинок с кодом `123` — один и тот же лот, хотя названия разные.',
        ],
        syntax: 'return self.code == other.code',
        example: 'class Item:\n    def __init__(self, name, code):\n        self.name = name\n        self.code = code\n\n    def __eq__(self, other):\n        return self.code == other.code\n\na = Item("Меч", "123")\nb = Item("Клинок", "123")\nprint(a == b)\n# True',
        note: 'Если написать `return True`, равными окажутся вообще любые объекты, даже предмет и строка. Смысл `==` при этом пропадёт.',
      },
      {
        title: 'Чужой тип не равен объекту',
        paragraphs: [
          'Справа может оказаться не предмет, а строка или число. У них нет поля `code`, и программа упадёт с ошибкой.',
          'Поэтому сначала проверяют тип через `isinstance`. Сравнение с чужим типом должно давать `False`, а не ломать программу.',
        ],
        syntax: 'if not isinstance(other, Item):\n    return False',
        example: 'class Item:\n    def __init__(self, name, code):\n        self.name = name\n        self.code = code\n\n    def __eq__(self, other):\n        if not isinstance(other, Item):\n            return False\n        return self.code == other.code\n\nprint(Item("Меч", "123") == "123")\n# False',
      },
    ],
  },
  {
    title: '__add__ — сложение +',
    lead: 'Запись `a + b` вызывает `a.__add__(b)`. Так объекты учат складываться.',
    sections: [
      {
        title: 'Сложение объектов',
        paragraphs: [
          'Метод получает левое слагаемое как `self`, правое — как `other`. Внутри складывают нужные поля.',
          'Возвращают при этом новый объект того же класса. Сумма двух кошельков — это третий кошелёк.',
        ],
        syntax: 'def __add__(self, other):\n    return Wallet(self.gold + other.gold)',
        example: 'class Wallet:\n    def __init__(self, gold):\n        self.gold = gold\n\n    def __add__(self, other):\n        return Wallet(self.gold + other.gold)\n\ntotal = Wallet(50) + Wallet(100)\nprint(total.gold)\n# 150',
      },
      {
        title: 'Слагаемые не портятся',
        paragraphs: [
          'Сложение не должно менять исходные объекты. После `w1 + w2` золото в `w1` и `w2` остаётся прежним.',
          'Так же ведут себя числа: `2 + 3` не превращает двойку в пятёрку. Объекты лучше делать предсказуемыми и не ломать это ожидание.',
        ],
        syntax: 'c = a + b',
        example: 'class Wallet:\n    def __init__(self, gold):\n        self.gold = gold\n\n    def __add__(self, other):\n        return Wallet(self.gold + other.gold)\n\nw1 = Wallet(50)\nw2 = Wallet(100)\nw3 = w1 + w2\nprint(w1.gold, w3.gold)\n# 50 150',
      },
      {
        title: 'Сложение с числом: NotImplemented',
        paragraphs: [
          'Если справа не кошелёк, а число, у него нет поля `gold`. Без проверки будет непонятная ошибка про отсутствующий атрибут.',
          'В таком случае возвращают особое значение `NotImplemented` — «я не умею такую операцию». Python сам превратит его в понятное сообщение про оператор `+`.',
        ],
        syntax: 'return NotImplemented',
        example: 'class Wallet:\n    def __init__(self, gold):\n        self.gold = gold\n\n    def __add__(self, other):\n        if isinstance(other, Wallet):\n            return Wallet(self.gold + other.gold)\n        return NotImplemented\n\nprint(Wallet(50) + 10)\n# TypeError: unsupported operand type(s) for +: \'Wallet\' and \'int\'',
        note: '`NotImplemented` — это не ошибка и не `None`. Это ответ «попробуй другой способ», и его именно возвращают через `return`.',
      },
    ],
  },
  {
    title: '__lt__ и __le__ — сравнение < и <=',
    lead: 'Эти методы включают операторы `<` и `<=`. Сравнивать удобно не поля по отдельности, а посчитанное значение.',
    sections: [
      {
        title: 'Сравнивают посчитанное значение',
        paragraphs: [
          'Щит `1×4` и щит `2×2` разной формы. Сравнивать их ширину с шириной бессмысленно: ответ ничего не скажет о размере.',
          'Если вопрос «какой щит больше», сначала считают площадь. Дальше сравнивают уже её — одно число против другого.',
        ],
        syntax: 'def area(self):\n    return self.width * self.height',
        example: 'class Shield:\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n\n    def area(self):\n        return self.width * self.height\n\nprint(Shield(1, 4).area())\nprint(Shield(2, 2).area())\n# 4\n# 4',
      },
      {
        title: 'Оператор <',
        paragraphs: [
          'Запись `shield1 < shield2` — это вызов `shield1.__lt__(shield2)`. Метод возвращает `True` или `False`.',
          'Внутри сравнивают площади. Равные щиты дают `False`: «меньше» — это строго меньше.',
        ],
        syntax: 'def __lt__(self, other):\n    return self.area() < other.area()',
        example: 'class Shield:\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n\n    def area(self):\n        return self.width * self.height\n\n    def __lt__(self, other):\n        return self.area() < other.area()\n\nprint(Shield(1, 1) < Shield(2, 2))\nprint(Shield(2, 2) < Shield(2, 2))\n# True\n# False',
        note: 'С `__lt__` объекты уже умеет сортировать функция `sorted()`: ей нужен именно этот метод.',
      },
      {
        title: 'Оператор <=',
        paragraphs: [
          'За `<=` отвечает отдельный метод — `__le__`. Python не догадается собрать его из `__lt__` сам.',
          'Разница только в знаке внутри: равные по площади щиты теперь дают `True`.',
        ],
        syntax: 'def __le__(self, other):\n    return self.area() <= other.area()',
        example: 'class Shield:\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n\n    def area(self):\n        return self.width * self.height\n\n    def __le__(self, other):\n        return self.area() <= other.area()\n\nprint(Shield(2, 2) <= Shield(2, 2))\n# True',
      },
      {
        title: 'Сравнение с другим типом',
        paragraphs: [
          'У числа `10` нет метода `area()`. Сравнение щита с числом без проверки закончится ошибкой атрибута.',
          'Как и в сложении, здесь возвращают `NotImplemented`. Тогда Python скажет понятное: такие типы сравнивать нельзя.',
        ],
        syntax: 'if not isinstance(other, Shield):\n    return NotImplemented',
        example: 'class Shield:\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n\n    def area(self):\n        return self.width * self.height\n\n    def __lt__(self, other):\n        if not isinstance(other, Shield):\n            return NotImplemented\n        return self.area() < other.area()\n\nprint(Shield(1, 1) < 10)\n# TypeError: \'<\' not supported between instances of \'Shield\' and \'int\'',
      },
    ],
  },
  {
    title: '__len__ и __getitem__',
    lead: '`__len__` включает `len(obj)`, а `__getitem__` — обращение `obj[i]`. Объект начинает вести себя как коллекция.',
    sections: [
      {
        title: 'Длина объекта',
        paragraphs: [
          'Своему классу тоже можно разрешить измерять длину. `len(deck)` вызывает `deck.__len__()`.',
          'Внутри обычно просто отдают длину списка, который лежит в объекте. Метод обязан вернуть целое число.',
        ],
        syntax: 'def __len__(self):\n    return len(self._cards)',
        example: 'class Deck:\n    def __init__(self):\n        self._cards = ["2♠", "3♠", "4♠"]\n\n    def __len__(self):\n        return len(self._cards)\n\nprint(len(Deck()))\n# 3',
      },
      {
        title: 'Элемент по индексу',
        paragraphs: [
          'Запись `deck[0]` вызывает `__getitem__` и передаёт туда номер. Отрицательные индексы попадают в тот же метод.',
          'Внутри снова хватает одной строки: отдать элемент внутреннего списка. Всю работу с индексами список делает сам.',
        ],
        syntax: 'def __getitem__(self, position):\n    return self._cards[position]',
        example: 'class Deck:\n    def __init__(self):\n        self._cards = ["2♠", "3♠", "4♠"]\n\n    def __getitem__(self, position):\n        return self._cards[position]\n\ndeck = Deck()\nprint(deck[0])\nprint(deck[-1])\n# 2♠\n# 4♠',
      },
      {
        title: 'Двух методов хватает на многое',
        paragraphs: [
          'С этими двумя методами объект уже можно мерить, брать по индексу, обходить в `for` и передавать в `random.choice`.',
          'При этом класс не наследует список и ничего не копирует. Python просто видит нужные методы и считает объект коллекцией.',
        ],
        syntax: 'for card in deck:\n    ...',
        example: 'class Deck:\n    def __init__(self):\n        self._cards = ["2♠", "3♠", "4♠"]\n\n    def __len__(self):\n        return len(self._cards)\n\n    def __getitem__(self, position):\n        return self._cards[position]\n\nfor card in Deck():\n    print(card)\n# 2♠\n# 3♠\n# 4♠',
      },
    ],
  },
  {
    title: '__enter__ и __exit__ — блок with',
    lead: 'Пара этих методов позволяет писать объект в `with`. Так закрывают файлы и соединения.',
    sections: [
      {
        title: 'Вход в блок with',
        paragraphs: [
          '`__enter__` выполняется в самом начале блока `with`. То, что он вернёт, попадёт в переменную после `as`.',
          'Сюда кладут подготовку: открыть файл, подключиться к серверу, начать замер времени.',
        ],
        syntax: 'def __enter__(self):\n    return self',
        example: 'class GameSession:\n    def __enter__(self):\n        print("Подключаемся к серверу...")\n        return self\n\n    def play(self):\n        print("Играем!")\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        print("Отключаемся.")\n\nwith GameSession() as session:\n    session.play()\n# Подключаемся к серверу...\n# Играем!\n# Отключаемся.',
      },
      {
        title: 'Выход сработает даже при ошибке',
        paragraphs: [
          '`__exit__` вызывается в конце блока всегда: и когда всё прошло хорошо, и когда внутри случилась ошибка. Именно в этом весь смысл `with`.',
          'При ошибке в метод приходят её тип и текст. Строка `return True` говорит Python: ошибку я обработал, дальше её не передавай.',
        ],
        syntax: 'def __exit__(self, exc_type, exc_val, exc_tb):\n    ...\n    return True',
        example: 'class GameSession:\n    def __enter__(self):\n        print("Подключаемся к серверу...")\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        print("Сохраняем прогресс и отключаемся.")\n        if exc_type:\n            print(f"Была ошибка: {exc_val}")\n        return True\n\nwith GameSession():\n    raise RuntimeError("разрыв связи")\n# Подключаемся к серверу...\n# Сохраняем прогресс и отключаемся.\n# Была ошибка: разрыв связи',
        note: '`__exit__` срабатывает всегда, но гасит ошибку только при `return True`. Без него ошибка полетит дальше. Так же устроен `with open(...)`: файл закроется в любом случае.',
      },
    ],
  },
  {
    title: '@staticmethod и @classmethod',
    lead: 'Обычный метод получает объект. Статический — ничего. Метод класса — сам класс.',
    sections: [
      {
        title: '@staticmethod: без self',
        paragraphs: [
          'Иногда функция по смыслу относится к классу, но данные объекта ей не нужны. Сложение двух чисел не зависит от версии калькулятора.',
          'Такой метод помечают `@staticmethod` и пишут без `self`. Вызывать его можно прямо через класс, не создавая объект.',
        ],
        syntax: '@staticmethod\ndef add(a, b):\n    return a + b',
        example: 'class Calculator:\n    @staticmethod\n    def add(a, b):\n        return a + b\n\nprint(Calculator.add(5, 10))\n# 15',
        note: 'Статический метод не видит ни `self`, ни данные объекта. Если методу нужны поля экземпляра — он должен быть обычным.',
      },
      {
        title: '@classmethod: cls вместо self',
        paragraphs: [
          'Метод класса первым аргументом получает не объект, а сам класс. По традиции его называют `cls`: это «я, но чертёж».',
          'Частая задача — второй конструктор. Данные пришли строкой `"Geralt-50"`, метод разбирает её и вызывает `cls(...)`, то есть обычное создание объекта.',
        ],
        syntax: '@classmethod\ndef from_string(cls, text):\n    ...\n    return cls(name, level)',
        example: 'class Hero:\n    def __init__(self, name, level):\n        self.name = name\n        self.level = level\n\n    @classmethod\n    def from_string(cls, text):\n        name, level = text.split("-")\n        return cls(name, int(level))\n\nh = Hero.from_string("Geralt-50")\nprint(h.name, h.level)\n# Geralt 50',
        note: 'Внутри пишут `cls(...)`, а не `Hero(...)`. Тогда наследник вызовет тот же метод и получит объект своего класса.',
      },
    ],
  },
  {
    title: '__slots__',
    lead: 'Жёсткий список разрешённых атрибутов: меньше памяти и никаких случайных полей.',
    sections: [
      {
        title: 'Только разрешённые имена',
        paragraphs: [
          'Обычному объекту можно дописать любой атрибут в любой момент — Python хранит их в скрытом словаре.',
          '`__slots__` перечисляет разрешённые имена и отменяет этот словарь. Лишнее поле теперь даёт ошибку, а каждый объект занимает меньше памяти.',
        ],
        syntax: '__slots__ = ("x", "y", "color")',
        example: 'class Pixel:\n    __slots__ = ("x", "y", "color")\n\n    def __init__(self, x, y, color):\n        self.x = x\n        self.y = y\n        self.color = color\n\np = Pixel(10, 20, "red")\np.z = 30\n# AttributeError: \'Pixel\' object has no attribute \'z\'',
        note: '`__slots__` экономит память, но отнимает возможность дописать поле на ходу. Берут его, когда объектов миллионы, а набор полей известен заранее.',
      },
    ],
  },
  {
    title: 'Dataclass',
    lead: 'Если класс только хранит данные, служебные методы за вас напишет декоратор `@dataclass`.',
    sections: [
      {
        title: 'Класс-мешок вручную',
        paragraphs: [
          'Простому контейнеру данных нужны `__init__`, `__repr__` и `__eq__`. Всё это приходится писать руками.',
          'Кода много, а смысла в нём мало: одни и те же три поля переписаны несколько раз.',
        ],
        syntax: 'def __init__(self, name, weight, price):\n    self.name = name\n    self.weight = weight\n    self.price = price',
        example: 'class InventoryItem:\n    def __init__(self, name, weight, price):\n        self.name = name\n        self.weight = weight\n        self.price = price\n\n    def __repr__(self):\n        return f"InventoryItem({self.name}, {self.weight}, {self.price})"\n\n    def __eq__(self, other):\n        return (self.name, self.weight, self.price) == (other.name, other.weight, other.price)\n\nprint(InventoryItem("Apple", 0.2, 10))\n# InventoryItem(Apple, 0.2, 10)',
      },
      {
        title: 'Тот же класс через @dataclass',
        paragraphs: [
          'Декоратор `@dataclass` читает поля с аннотациями типов и сам генерирует `__init__`, `__repr__` и `__eq__`.',
          'Остаётся перечислить поля. Значение по умолчанию пишут сразу у поля, как `price: int = 0`.',
        ],
        syntax: 'from dataclasses import dataclass\n\n@dataclass\nclass InventoryItem:\n    name: str\n    weight: float\n    price: int = 0',
        example: 'from dataclasses import dataclass\n\n@dataclass\nclass InventoryItem:\n    name: str\n    weight: float\n    price: int = 0\n\nitem1 = InventoryItem("Apple", 0.2, 10)\nitem2 = InventoryItem("Apple", 0.2, 10)\nprint(item1)\nprint(item1 == item2)\n# InventoryItem(name=\'Apple\', weight=0.2, price=10)\n# True',
        note: '`@dataclass` не запрещает писать свои методы. Он только избавляет от рутинных `__init__`, `__repr__` и `__eq__`.',
      },
    ],
  },
  {
    title: 'Композиция',
    lead: 'Наследование — это «является». Композиция — «имеет». Машина не является двигателем, она его содержит.',
    sections: [
      {
        title: 'Наследование ради чужих методов',
        paragraphs: [
          'Хочется взять готовый метод `start()` у двигателя. Кажется удобным написать `class Car(Engine)` — и метод сразу доступен.',
          'Но проверьте фразу: «машина является двигателем». Звучит неправильно, и код это позже подтвердит.',
        ],
        syntax: 'class Car(Engine):\n    ...',
        example: 'class Engine:\n    def start(self):\n        print("Врум-врум!")\n\nclass Car(Engine):\n    def drive(self):\n        self.start()\n        print("Поехали!")\n\nCar().drive()\n# Врум-врум!\n# Поехали!',
        note: 'Так делать не стоит. Понадобится электромотор или лодка с тем же двигателем — и вся иерархия развалится.',
      },
      {
        title: 'Объект внутри объекта',
        paragraphs: [
          'Правильный путь: положить двигатель в атрибут. Машина **имеет** двигатель и просит его завестись.',
          'Теперь деталь можно заменить, даже когда программа уже работает: подставили электромотор — и машина едет тихо. Класс `Car` при этом не меняли.',
        ],
        syntax: 'self.engine = Engine()',
        example: 'class Engine:\n    def start(self):\n        print("Врум-врум!")\n\nclass ElectricEngine:\n    def start(self):\n        print("Тишина... Шшшш...")\n\nclass Car:\n    def __init__(self):\n        self.engine = Engine()\n\n    def drive(self):\n        self.engine.start()\n\nmy_car = Car()\nmy_car.engine = ElectricEngine()\nmy_car.drive()\n# Тишина... Шшшш...',
      },
    ],
  },
  {
    title: 'Одна ответственность',
    lead: 'Класс должен делать что-то одно. Если причин его менять несколько — пора делить.',
    sections: [
      {
        title: 'Слишком много знаний',
        paragraphs: [
          'Этот класс хранит пользователя, пишет его в базу, отправляет письма и рисует отчёт. Четыре разные работы в одном месте.',
          'Значит, у него четыре причины меняться. Сменился почтовый сервис — правят `User`, и рядом легко сломать сохранение в базу.',
        ],
        syntax: 'class User:\n    def save_to_db(self): ...\n    def send_email(self, message): ...\n    def generate_report(self): ...',
        example: 'class User:\n    def __init__(self, name):\n        self.name = name\n\n    def save_to_db(self):\n        print("SQL")\n\n    def send_email(self, message):\n        print("SMTP")\n\n    def generate_report(self):\n        print("PDF")\n\nUser("Neo").save_to_db()\n# SQL',
      },
      {
        title: 'Разделить по ролям',
        paragraphs: [
          'Разложим работы по отдельным классам: данные в одном, сохранение во втором, письма в третьем.',
          'Теперь каждый класс меняют по своей причине, и правка в письмах не задевает базу. Заодно проще искать нужный код: имя класса говорит, что внутри.',
        ],
        syntax: 'class User: ...\nclass UserRepository: ...\nclass EmailService: ...',
        example: 'from dataclasses import dataclass\n\n@dataclass\nclass User:\n    name: str\n\nclass UserRepository:\n    def save(self, user):\n        print(f"Сохраняю {user.name} в базу...")\n\nclass EmailService:\n    def send(self, user, msg):\n        print(f"Письмо для {user.name}: {msg}")\n\nu = User("Neo")\nUserRepository().save(u)\nEmailService().send(u, "Wake up!")\n# Сохраняю Neo в базу...\n# Письмо для Neo: Wake up!',
        note: 'ООП — инструмент против сложности, а не обязанность. Если задачу решает одна функция, класс вокруг неё не нужен.',
      },
    ],
  },
];

const builtins = [
  {
    title: 'len, type и isinstance',
    lead: 'Длина объекта, его тип и проверка типа.',
    sections: [
      {
        title: 'len строки',
        paragraphs: [
          '`len` отвечает, сколько элементов в объекте. Для строки это число символов. У числа `len` нет: будет `TypeError`.',
        ],
        syntax: 'len(строка)',
        example: 'print(len("Python"))        # 6',
      },
      {
        title: 'len списка',
        paragraphs: [
          'Для списка `len` считает элементы, не символы внутри них.',
        ],
        syntax: 'len(список)',
        example: 'print(len([10, 20, 30]))     # 3',
      },
      {
        title: 'len словаря',
        paragraphs: [
          'Для словаря `len` — число ключей. Для множества — число уникальных значений.',
        ],
        syntax: 'len(словарь)',
        example: 'print(len({"a": 1}))         # 1',
      },
      {
        title: 'type',
        paragraphs: [
          '`type(x)` показывает тип объекта. Так смотрят, число перед нами, строка или коллекция.',
        ],
        syntax: 'type(объект)',
        example: 'print(type(25))\n# <class \'int\'>',
      },
      {
        title: 'isinstance',
        paragraphs: [
          '`isinstance(x, тип)` отвечает, является ли объект этим типом. Этот вариант предпочтительнее `type(x) == ...`: он учитывает и наследников. Можно передать несколько типов кортежем.',
        ],
        syntax: 'isinstance(объект, тип)',
        example: 'print(isinstance(25, int))       # True',
        note: '`type(x) == int` работает, но в проверках обычно пишут `isinstance`.',
      },
    ],
  },
  {
    title: 'int, float, str и bool',
    lead: 'Преобразование значения из одного типа в другой.',
    sections: [
      {
        title: 'int',
        paragraphs: [
          '`int` делает целое число из строки или отбрасывает дробную часть у `float`. `int("10")` читает десятичную запись.',
        ],
        syntax: 'int(x)',
        example: 'print(int("10"))        # 10',
        note: '`int("3.9")` нельзя: это не целая запись. Сначала `float("3.9")`, потом `int(...)`.',
      },
      {
        title: 'int из другой системы',
        paragraphs: [
          'Второй аргумент — система счисления. `int("101", 2)` читает двоичную запись.',
        ],
        syntax: 'int("101", 2)',
        example: 'print(int("101", 2))    # 5',
      },
      {
        title: 'float',
        paragraphs: [
          '`float` делает дробное число. Строка должна быть с точкой: `"1.5"`, не `"1,5"`.',
        ],
        syntax: 'float(x)',
        example: 'print(float("1.5"))     # 1.5',
      },
      {
        title: 'str',
        paragraphs: [
          '`str` превращает значение в текст. Так число можно склеить с надписью.',
        ],
        syntax: 'str(x)',
        example: 'print(str(25) + " кг")  # 25 кг',
      },
      {
        title: 'bool: ложное',
        paragraphs: [
          '`bool(x)` отвечает, истинно ли значение в условии. Ложные: `0`, `0.0`, `""`, `[]`, `{}`, `set()`, `None`.',
        ],
        syntax: 'bool(x)',
        example: 'print(bool(0))          # False',
      },
      {
        title: 'bool: строка "0"',
        paragraphs: [
          'Строка `"0"` истинна: она не пустая. То же со строкой `"False"`.',
        ],
        syntax: 'bool("0")',
        example: 'print(bool("0"))        # True',
      },
    ],
  },
  {
    title: 'list, tuple, set и dict',
    lead: 'Конструкторы коллекций: собрать список, кортеж, множество или словарь.',
    sections: [
      {
        title: 'list',
        paragraphs: [
          '`list` собирает список из другой последовательности. Так копируют и превращают `range` или строку в список. Число перебрать нельзя: `list(25)` вызовет ошибку.',
        ],
        syntax: 'list(iterable)',
        example: 'print(list("abc"))                    # ["a", "b", "c"]',
      },
      {
        title: 'tuple',
        paragraphs: [
          '`tuple` собирает кортеж. Исходный список при этом не меняется.',
        ],
        syntax: 'tuple(iterable)',
        example: 'print(tuple([1, 2]))                  # (1, 2)',
      },
      {
        title: 'set',
        paragraphs: [
          '`set` собирает множество: повторы исчезают.',
        ],
        syntax: 'set(iterable)',
        example: 'print(set([1, 1, 2]))                 # {1, 2}',
      },
      {
        title: 'dict',
        paragraphs: [
          '`dict` собирает словарь из пар `(ключ, значение)` или из именованных аргументов. Пустые объекты: `list()`, `tuple()`, `set()`, `dict()`. `{}` — словарь, не множество.',
        ],
        syntax: 'dict(пары)',
        example: 'print(dict([("a", 1), ("b", 2)]))     # {"a": 1, "b": 2}',
      },
    ],
  },
  {
    title: 'abs, round и pow',
    lead: 'Модуль, округление и возведение в степень.',
    sections: [
      {
        title: 'abs',
        paragraphs: [
          '`abs` берёт модуль: отрицательное число становится положительным.',
        ],
        syntax: 'abs(x)',
        example: 'print(abs(-7))             # 7',
      },
      {
        title: 'round',
        paragraphs: [
          '`round` округляет: без второго аргумента к целому, со вторым — до указанного числа знаков.',
        ],
        syntax: 'round(x, n)',
        example: 'print(round(3.14159, 2))   # 3.14',
      },
      {
        title: 'int — не округление',
        paragraphs: [
          '`int(3.9)` всегда отбрасывает дробь к нулю. Это не округление: `3.9` станет `3`, а не `4`.',
        ],
        syntax: 'int(3.9)',
        example: 'print(int(3.9))            # 3',
      },
      {
        title: 'pow',
        paragraphs: [
          '`pow(x, y)` — то же, что `x ** y`.',
        ],
        syntax: 'pow(x, y)',
        example: 'print(pow(2, 10))          # 1024',
      },
      {
        title: 'pow с остатком',
        paragraphs: [
          'Третий аргумент сразу берёт остаток: `pow(x, y, mod)` считают как `(x ** y) % mod`, часто быстрее.',
        ],
        syntax: 'pow(x, y, mod)',
        example: 'print(pow(2, 10, 100))     # 24',
      },
    ],
  },
  {
    title: 'min, max и sum',
    lead: 'Найти крайние значения и сложить числа.',
    sections: [
      {
        title: 'min',
        paragraphs: [
          '`min` находит наименьшее значение. Можно передать коллекцию или перечислить аргументы через запятую.',
        ],
        syntax: 'min(iterable)',
        example: 'nums = [3, 1, 8, 2]\nprint(min(nums))          # 1',
      },
      {
        title: 'max',
        paragraphs: [
          '`max` находит наибольшее значение. Аргументы можно перечислить через запятую без списка.',
        ],
        syntax: 'max(a, b, c)',
        example: 'print(max(3, 1, 8))       # 8',
      },
      {
        title: 'sum',
        paragraphs: [
          '`sum` складывает числа. Пустой `sum([])` даёт `0`. Строки `sum` не склеивает — для этого `"".join(...)`.',
        ],
        syntax: 'sum(iterable)',
        example: 'print(sum([3, 1, 8, 2]))  # 14',
      },
      {
        title: 'sum со стартом',
        paragraphs: [
          'Второй аргумент — начальное значение. Его прибавляют к сумме элементов.',
        ],
        syntax: 'sum(iterable, start)',
        example: 'print(sum([3, 1, 8, 2], 100))     # 114',
      },
      {
        title: 'min с key',
        paragraphs: [
          '`key=` говорит, по чему сравнивать. `min(words, key=len)` найдёт самое короткое слово, а не первое по алфавиту.',
        ],
        syntax: 'min(iterable, key=len)',
        example: 'words = ["я", "питон", "код"]\nprint(min(words, key=len))    # я',
      },
      {
        title: 'max с key',
        paragraphs: [
          'Тот же `key=` для `max` находит элемент с наибольшим признаком: здесь самое длинное слово.',
        ],
        syntax: 'max(iterable, key=len)',
        example: 'words = ["я", "питон", "код"]\nprint(max(words, key=len))    # питон',
      },
    ],
  },
  {
    title: 'sorted и reversed',
    lead: 'Новый отсортированный список и обход в обратном порядке.',
    sections: [
      {
        title: 'sorted',
        paragraphs: [
          '`sorted` всегда возвращает новый список и не меняет исходную коллекцию.',
        ],
        syntax: 'sorted(iterable)',
        example: 'nums = [3, 1, 2]\nprint(sorted(nums))                 # [1, 2, 3]',
      },
      {
        title: 'sorted по убыванию',
        paragraphs: [
          '`reverse=True` сортирует по убыванию. Исходная коллекция по-прежнему не меняется.',
        ],
        syntax: 'sorted(iterable, reverse=True)',
        example: 'print(sorted([3, 1, 2], reverse=True))   # [3, 2, 1]',
      },
      {
        title: 'sorted с key',
        paragraphs: [
          '`key=` задаёт признак сравнения. Здесь слова упорядочены по длине.',
        ],
        syntax: 'sorted(iterable, key=функция)',
        example: 'print(sorted(["я", "питон", "код"], key=len))\n# ["я", "код", "питон"]',
      },
      {
        title: 'sort vs sorted',
        paragraphs: [
          'У списка есть метод `.sort()`: он меняет объект на месте и возвращает `None`. `sorted` возвращает новый список.',
        ],
        syntax: 'lst.sort()',
        example: 'nums = [3, 1, 2]\nnums.sort()\nprint(nums)                         # [1, 2, 3]',
        note: 'Не пишите `lst = lst.sort()`. Метод вернёт `None`, и список пропадёт из переменной.',
      },
      {
        title: 'reversed списка',
        paragraphs: [
          '`reversed` даёт элементы с конца, но это не список, а итератор. Обычно сразу пишут `list(reversed(...))`. Нужна последовательность с индексами.',
        ],
        syntax: 'list(reversed(список))',
        example: 'print(list(reversed([1, 2, 3])))    # [3, 2, 1]',
      },
      {
        title: 'reversed строки',
        paragraphs: [
          'Строку так же разворачивают через `reversed`, затем склеивают обратно. Множество так развернуть нельзя.',
        ],
        syntax: '"".join(reversed(строка))',
        example: 'print("".join(reversed("кот")))     # ток',
      },
    ],
  },
  {
    title: 'range, enumerate и zip',
    lead: 'Числа для цикла, нумерация элементов и пары из двух коллекций.',
    sections: [
      {
        title: 'range',
        paragraphs: [
          '`range` даёт последовательность чисел. Сам по себе он не список: содержимое смотрят через `list(range(...))` или сразу в `for`. Правая граница не входит.',
        ],
        syntax: 'range(stop)',
        example: 'print(list(range(3)))          # [0, 1, 2]',
      },
      {
        title: 'range с шагом',
        paragraphs: [
          'Три аргумента: начало, конец и шаг. Конец по-прежнему не входит.',
        ],
        syntax: 'range(start, stop, step)',
        example: 'print(list(range(1, 8, 2)))    # [1, 3, 5, 7]',
      },
      {
        title: 'enumerate',
        paragraphs: [
          '`enumerate` нумерует элементы и заменяет ручной счётчик `i = 0`. `start` задаёт первый номер, по умолчанию `0`.',
        ],
        syntax: 'enumerate(iterable, start=0)',
        example: 'names = ["Анна", "Борис"]\nfor i, name in enumerate(names, start=1):\n    print(i, name)',
      },
      {
        title: 'zip',
        paragraphs: [
          '`zip` идёт по нескольким коллекциям сразу и берёт пары элементов с одинаковой позицией. Останавливается, когда заканчивается самая короткая. Лишние элементы длинной коллекции в результат не попадут.',
        ],
        syntax: 'zip(a, b, ...)',
        example: 'print(list(zip([1, 2, 3], ["a", "b"])))\n# [(1, "a"), (2, "b")]',
        note: '`range` и `zip` сами по себе не списки. Чтобы увидеть содержимое, пишут `list(...)` или обходят их в `for`.',
      },
    ],
  },
  {
    title: 'map и filter',
    lead: 'Пройти по коллекции: либо преобразовать каждый элемент, либо оставить только подходящие.',
    sections: [
      {
        title: 'Что делает map',
        paragraphs: [
          '`map` берёт функцию и коллекцию. Функцию вызывают для **каждого** элемента, и из ответов собирают новую последовательность.',
          'Сам `map(...)` — ещё не список. Чтобы увидеть результат целиком, его оборачивают в `list(...)`. Функцией может быть `str`, `int`, `lambda` или обычный `def`.',
        ],
        syntax: 'map(функция, коллекция)',
        example: 'print(list(map(str, [1, 2, 3])))\n# ["1", "2", "3"]',
        note: '`map` сам по себе ничего не печатает. Либо пишите `list(map(...))`, либо сразу распаковывайте: `a, b = map(...)`.',
      },
      {
        title: 'map: числа сделать строками',
        paragraphs: [
          'Готовая функция `str` превращает каждый элемент в текст. Список чисел становится списком строк. Это удобно, если потом нужно склеить значения или напечатать их как текст.',
        ],
        syntax: 'list(map(str, коллекция))',
        example: 'print(list(map(str, [10, 20])))\n# ["10", "20"]',
      },
      {
        title: 'map: умножить каждый элемент',
        paragraphs: [
          'Если готовой функции нет, правило пишут через `lambda`. Здесь каждый элемент списка умножается на 10. `x` — текущий элемент, `x * 10` — то, что кладут в ответ.',
        ],
        syntax: 'list(map(lambda x: x * 10, коллекция))',
        example: 'print(list(map(lambda x: x * 10, [1, 2, 3, 4])))\n# [10, 20, 30, 40]',
      },
      {
        title: 'map: ввод двух чисел',
        paragraphs: [
          'Классическая строка ввода: прочитать `"3 7"`, разрезать по пробелам и каждое слово превратить в `int`. `map` здесь не оборачивают в `list`, а сразу распаковывают в две переменные.',
        ],
        syntax: 'a, b = map(int, input().split())',
        example: '# человек ввёл: 3 7\na, b = map(int, input().split())\nprint(a + b)     # 10',
        note: 'Сколько чисел во вводе — столько переменных слева. Для трёх чисел: `a, b, c = map(int, input().split())`.',
      },
      {
        title: 'Что делает filter',
        paragraphs: [
          '`filter` тоже идёт по коллекции, но **не меняет** элементы. Он оставляет только те, для которых функция дала истину. Остальные выбрасывает.',
          'Как и у `map`, результат — не список. Чтобы получить список, пишут `list(filter(...))`.',
        ],
        syntax: 'filter(функция, коллекция)',
        example: 'print(list(filter(lambda x: x > 0, [-2, 0, 5])))\n# [5]',
      },
      {
        title: 'filter: только чётные',
        paragraphs: [
          'Функция `lambda x: x % 2 == 0` для каждого числа отвечает, чётное ли оно. `filter` оставляет те элементы, где ответ `True`. Нечётные в результат не попадут.',
        ],
        syntax: 'list(filter(lambda x: x % 2 == 0, коллекция))',
        example: 'print(list(filter(lambda x: x % 2 == 0, [1, 2, 3, 4])))\n# [2, 4]',
      },
      {
        title: 'Тот же отбор без filter',
        paragraphs: [
          'Тот же смысл можно записать включением: «положи `x`, если условие истинно». Оба варианта правильные. Включение часто читается проще, `filter` — короче, если функция уже есть.',
        ],
        syntax: '[x for x in коллекция if условие]',
        example: 'print([x for x in [1, 2, 3, 4] if x % 2 == 0])\n# [2, 4]',
      },
    ],
  },
  {
    title: 'functools.reduce',
    lead: 'Свернуть коллекцию в одно значение: сумму, произведение, максимум.',
    sections: [
      {
        title: 'Зачем нужен reduce',
        paragraphs: [
          '`reduce` берёт функцию **двух** аргументов и проходит по коллекции слева направо. На каждом шаге в функцию попадают уже накопленный результат и следующий элемент. В конце остаётся одно значение.',
          'Функция лежит в модуле `functools`, поэтому её сначала импортируют. Для обычной суммы проще `sum`, для максимума — `max`. `reduce` нужен, когда готовой функции нет: перемножить все числа, склеить по своему правилу, накопить результат иначе, чем умеют `sum` и `max`.',
        ],
        syntax: 'from functools import reduce\n\nreduce(функция, коллекция)',
        example: 'from functools import reduce\n\nprint(reduce(lambda a, b: a + b, [1, 2, 3, 4]))\n# 10\n# шаг 1: 1 + 2 = 3\n# шаг 2: 3 + 3 = 6\n# шаг 3: 6 + 4 = 10',
      },
      {
        title: 'Сумма',
        paragraphs: [
          'Функция всегда выглядит как `lambda a, b: ...`: `a` — то, что уже накопили, `b` — следующий элемент. Здесь складывают.',
        ],
        syntax: 'reduce(lambda a, b: a + b, коллекция)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [1, 2, 3, 4]))\n# 10',
      },
      {
        title: 'Произведение',
        paragraphs: [
          'Готовой функции «перемножить все» нет. `reduce` с `a * b` как раз для этого.',
        ],
        syntax: 'reduce(lambda a, b: a * b, коллекция)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a * b, [1, 2, 3, 4]))\n# 24',
      },
      {
        title: 'Максимум',
        paragraphs: [
          'Можно накопить наибольшее: на каждом шаге оставить большее из двух. Для обычного случая проще `max`.',
        ],
        syntax: 'reduce(lambda a, b: a if a > b else b, коллекция)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a if a > b else b, [3, 1, 8, 2]))\n# 8',
      },
      {
        title: 'Склейка текста',
        paragraphs: [
          'Строки тоже можно накапливать: `a + b` склеивает куски. Для обычной склейки чаще берут `"".join(...)`.',
        ],
        syntax: 'reduce(lambda a, b: a + b, строки)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, ["Py", "thon"]))\n# Python',
      },
      {
        title: 'Старт',
        paragraphs: [
          'Если передать третье значение, с него начинают, а не с первого элемента. Так добавляют «начальный вклад».',
        ],
        syntax: 'reduce(функция, коллекция, старт)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [1, 2, 3], 10))\n# 16  — начали с 10, затем +1 +2 +3',
      },
      {
        title: 'Пустой список',
        paragraphs: [
          'Со стартом пустой список безопасен: `reduce` вернёт сам старт. Без стартового значения по пустому списку будет `TypeError`.',
        ],
        syntax: 'reduce(функция, [], старт)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [], 0))\n# 0',
        note: 'Без стартового значения `reduce` по пустому списку упадёт с `TypeError`. Со стартом вернёт сам старт.',
      },
      {
        title: 'То же циклом',
        paragraphs: [
          '`reduce` — компактная запись цикла с накопителем. Если цикл читается проще, пишите цикл.',
        ],
        syntax: 'total = 0\nfor x in nums:\n    total = total + x',
        example: 'nums = [1, 2, 3, 4]\ntotal = 0\nfor x in nums:\n    total = total + x\nprint(total)    # 10',
      },
      {
        title: 'То же через reduce',
        paragraphs: [
          'Та же сумма одной строкой. Смысл тот же, что у цикла с накопителем.',
        ],
        syntax: 'reduce(lambda a, b: a + b, nums)',
        example: 'from functools import reduce\nprint(reduce(lambda a, b: a + b, [1, 2, 3, 4]))\n# 10',
      },
      {
        title: 'Готовый sum',
        paragraphs: [
          'Для суммы и максимума обычно берут готовые `sum` и `max`. `reduce` оставляйте для своих правил накопления.',
        ],
        syntax: 'sum(коллекция)',
        example: 'print(sum([1, 2, 3, 4]))    # 10',
        note: '`reduce` оставляйте для своих правил накопления. Суммировать «вручную через reduce» чаще всего незачем.',
      },
    ],
  },
];

const methods = {
  'str — строки': [
    ['.upper()', 'все буквы в верхний регистр', '"кот".upper()  # "КОТ"'],
    ['.lower()', 'все буквы в нижний регистр', '"КОТ".lower()  # "кот"'],
    ['.capitalize()', 'первая буква заглавная', '"привет мир".capitalize()  # "Привет мир"'],
    ['.title()', 'каждое слово с заглавной', '"привет мир".title()  # "Привет Мир"'],
    ['.swapcase()', 'инвертировать регистр', '"Кот".swapcase()  # "кОТ"'],
    ['.count(sub)', 'число вхождений', '"banana".count("a")  # 3'],
    ['.find(sub)', 'индекс первого вхождения или -1', '"python".find("th")  # 2'],
    ['.rfind(sub)', 'индекс последнего вхождения или -1', '"ababa".rfind("ab")  # 2'],
    ['.index(sub)', 'индекс первого вхождения; иначе ValueError', '"python".index("th")  # 2'],
    ['.startswith(prefix)', 'начинается ли строка с текста', '"Python".startswith("Py")  # True'],
    ['.endswith(suffix)', 'заканчивается ли строка текстом', '"file.txt".endswith(".txt")  # True'],
    ['.strip()', 'убрать символы с краёв', '"  кот  ".strip()  # "кот"'],
    ['.lstrip()', 'убрать слева', '"  кот".lstrip()  # "кот"'],
    ['.rstrip()', 'убрать справа', '"кот  ".rstrip()  # "кот"'],
    ['.replace(old, new)', 'заменить подстроку', '"кот".replace("о", "а")  # "кат"'],
    ['.split()', 'строка → список', '"a b c".split()  # ["a", "b", "c"]'],
    ['.join(iterable)', 'склеить строки разделителем', '"-".join(["a", "b"])  # "a-b"'],
    ['.isdigit()', 'только цифры?', '"12".isdigit()  # True'],
    ['.isalpha()', 'только буквы?', '"кот".isalpha()  # True'],
    ['.isalnum()', 'буквы и цифры?', '"a1".isalnum()  # True'],
    ['.islower()', 'все буквы строчные?', '"кот".islower()  # True'],
    ['.isupper()', 'все буквы заглавные?', '"КОТ".isupper()  # True'],
    ['.isspace()', 'только пробелы?', '"  ".isspace()  # True'],
  ],
  'list — списки': [
    ['.append(x)', 'добавить один элемент в конец', 'a = [1, 2]; a.append(3)  # [1, 2, 3]'],
    ['.extend(iterable)', 'добавить элементы iterable', 'a = [1, 2]; a.extend([3, 4])  # [1, 2, 3, 4]'],
    ['.insert(i, x)', 'вставить перед индексом i', 'a = [1, 3]; a.insert(1, 2)  # [1, 2, 3]'],
    ['.remove(x)', 'удалить первое вхождение', 'a = [1, 2, 2]; a.remove(2)  # [1, 2]'],
    ['.pop()', 'удалить и вернуть последний', 'a = [1, 2, 3]; a.pop()  # 3, список [1, 2]'],
    ['.pop(i)', 'удалить и вернуть по индексу', 'a = [1, 2, 3]; a.pop(0)  # 1, список [2, 3]'],
    ['.clear()', 'очистить список', 'a = [1, 2]; a.clear()  # []'],
    ['.index(x)', 'индекс первого x', '[10, 20, 30].index(20)  # 1'],
    ['.count(x)', 'сколько раз есть x', '[1, 2, 2, 3].count(2)  # 2'],
    ['.sort()', 'сортировать на месте', 'a = [3, 1, 2]; a.sort()  # [1, 2, 3]'],
    ['.reverse()', 'развернуть на месте', 'a = [1, 2, 3]; a.reverse()  # [3, 2, 1]'],
    ['.copy()', 'поверхностная копия', 'b = [1, 2].copy()  # [1, 2]'],
  ],
  'tuple — кортежи': [
    ['.count(x)', 'сколько раз встречается x', '(1, 2, 2).count(2)  # 2'],
    ['.index(x)', 'индекс первого x', '(10, 20, 30).index(20)  # 1'],
  ],
  'dict — словари': [
    ['.get(key, default)', 'значение или default', '{"a": 1}.get("b", 0)  # 0'],
    ['.keys()', 'все ключи', 'list({"a": 1, "b": 2}.keys())  # ["a", "b"]'],
    ['.values()', 'все значения', 'list({"a": 1}.values())  # [1]'],
    ['.items()', 'пары ключ, значение', 'list({"a": 1}.items())  # [("a", 1)]'],
    ['.setdefault(key, default)', 'получить или создать ключ', 'd = {}; d.setdefault("a", 1)  # 1, словарь {"a": 1}'],
    ['.update(other)', 'добавить/обновить пары', 'd = {"a": 1}; d.update({"b": 2})  # {"a": 1, "b": 2}'],
    ['.pop(key)', 'удалить ключ, вернуть значение', 'd = {"a": 1}; d.pop("a")  # 1'],
    ['.popitem()', 'удалить последнюю пару', 'd = {"a": 1, "b": 2}; d.popitem()  # ("b", 2)'],
    ['.clear()', 'очистить', 'd = {"a": 1}; d.clear()  # {}'],
    ['.copy()', 'поверхностная копия', '{"a": 1}.copy()  # {"a": 1}'],
  ],
  'set — множества': [
    ['.add(x)', 'добавить элемент', 's = {1}; s.add(2)  # {1, 2}'],
    ['.update(iterable)', 'добавить элементы', 's = {1}; s.update([2, 3])  # {1, 2, 3}'],
    ['.remove(x)', 'удалить; нет x → KeyError', 's = {1, 2}; s.remove(2)  # {1}'],
    ['.discard(x)', 'удалить безопасно', 's = {1}; s.discard(9)  # {1}'],
    ['.pop()', 'удалить произвольный элемент', 's = {1}; s.pop()  # 1, множество set()'],
    ['.clear()', 'очистить', 's = {1, 2}; s.clear()  # set()'],
    ['.copy()', 'копия', '{1, 2}.copy()  # {1, 2}'],
    ['.union(other)', 'объединение', '{1, 2}.union({2, 3})  # {1, 2, 3}'],
    ['.intersection(other)', 'пересечение', '{1, 2}.intersection({2, 3})  # {2}'],
    ['.difference(other)', 'разность', '{1, 2}.difference({2, 3})  # {1}'],
    ['.symmetric_difference(other)', 'не общие элементы', '{1, 2}.symmetric_difference({2, 3})  # {1, 3}'],
    ['.intersection_update(other)', 'пересечение на месте', 's = {1, 2}; s.intersection_update({2, 3})  # {2}'],
    ['.difference_update(other)', 'разность на месте', 's = {1, 2}; s.difference_update({2})  # {1}'],
    ['.isdisjoint(other)', 'нет общих элементов?', '{1}.isdisjoint({2})  # True'],
    ['.issubset(other)', 'является подмножеством?', '{1}.issubset({1, 2})  # True'],
    ['.issuperset(other)', 'содержит другое множество?', '{1, 2}.issuperset({1})  # True'],
  ],
  'frozenset': [
    ['.copy()', 'копия', 'frozenset({1, 2}).copy()  # frozenset({1, 2})'],
    ['.union(other)', 'объединение', 'frozenset({1, 2}).union({2, 3})  # frozenset({1, 2, 3})'],
    ['.intersection(other)', 'пересечение', 'frozenset({1, 2}).intersection({2, 3})  # frozenset({2})'],
    ['.difference(other)', 'разность', 'frozenset({1, 2}).difference({2, 3})  # frozenset({1})'],
    ['.symmetric_difference(other)', 'не общие элементы', 'frozenset({1, 2}).symmetric_difference({2, 3})  # frozenset({1, 3})'],
    ['.isdisjoint(other)', 'нет общих элементов?', 'frozenset({1}).isdisjoint({2})  # True'],
    ['.issubset(other)', 'подмножество?', 'frozenset({1}).issubset({1, 2})  # True'],
    ['.issuperset(other)', 'надмножество?', 'frozenset({1, 2}).issuperset({1})  # True'],
  ],
};

const quick = [
  ['Вывод', 'print(a, b, sep=" ", end="\\n")'],
  ['Ввод числа', 'n = int(input())'],
  ['Два числа', 'a, b = map(int, input().split())'],
  ['Условие', 'if condition:'],
  ['Цикл', 'for x in iterable:'],
  ['Функция', 'def f(x): return result'],
];

const $ = (s) => document.querySelector(s);
const escapeHtml = (value) => String(value).replace(/[&<>'"]/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[c]));
const renderCode = (text) => escapeHtml(text).replace(/`([^`]+)`/g, '<code>$1</code>');
const renderTip = (text) => renderCode(text).replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
const sources = { topics, builtins, oop };
const sourceMeta = {
  topics: { kicker: 'Основная теория', prefix: 'topic' },
  builtins: { kicker: 'Встроенные функции', prefix: 'builtin' },
  oop: { kicker: 'Классы и ООП', prefix: 'oop' },
};
let mode = 'theory';
let modalSource = 'topics';
let modalIndex = 0;

function cardText(topic) {
  const chunks = [topic.title, topic.lead];
  (topic.sections || []).forEach((section) => {
    chunks.push(section.title, ...(section.paragraphs || []), section.syntax || '', section.example || '', section.note || '', ...(section.samples || []).flatMap((sample) => [sample.title, sample.code]));
  });
  return chunks.join(' ');
}

function renderRows(items, allItems, source) {
  return items.map((topic) => {
    const index = allItems.indexOf(topic);
    return `<article class="topic-row" id="${sourceMeta[source].prefix}-${index}" data-open-topic="${source}:${index}"><span class="theory-number">${String(index + 1).padStart(2, '0')}</span><div class="topic-row-text"><h3>${escapeHtml(topic.title)}</h3><p>${renderCode(topic.lead)}</p></div><button type="button" class="open-topic">Открыть</button></article>`;
  }).join('');
}

function renderSection(section, index) {
  const paragraphs = (section.paragraphs || []).map((text) => `<p>${renderTip(text)}</p>`).join('');
  const syntax = section.syntax ? `<figure class="code-panel"><figcaption>КОНСТРУКЦИЯ</figcaption><pre>${escapeHtml(section.syntax)}</pre></figure>` : '';
  const example = section.example ? `<figure class="code-panel is-example"><figcaption>ПРИМЕР</figcaption><pre>${escapeHtml(section.example)}</pre></figure>` : '';
  const grid = syntax || example ? `<div class="code-grid">${syntax}${example}</div>` : '';
  const samples = (section.samples || []).length
    ? `<div class="sample-list">${section.samples.map((sample) => `<article class="sample-card"><h4>${escapeHtml(sample.title)}</h4><pre>${escapeHtml(sample.code)}</pre></article>`).join('')}</div>`
    : '';
  const note = section.note ? `<p class="tip"><strong>Не перепутать:</strong> ${renderTip(section.note)}</p>` : '';
  return `<section class="subtopic" id="sub-${index}"><div class="subtopic-head"><span class="subtopic-num">${String(index + 1).padStart(2, '0')}</span><h3>${escapeHtml(section.title)}</h3></div><div class="prose">${paragraphs}</div>${grid}${samples}${note}</section>`;
}

function openModal(source, index) {
  const list = sources[source];
  const topic = list?.[index];
  if (!topic) return;
  modalSource = source;
  modalIndex = index;
  $('#modal-kicker').textContent = sourceMeta[source].kicker;
  $('#modal-title').textContent = topic.title;
  $('#modal-lead').innerHTML = renderCode(topic.lead || '');
  $('#modal-toc').innerHTML = topic.sections.map((section, i) => `<a href="#sub-${i}">${escapeHtml(section.title)}</a>`).join('');
  $('#modal-body').innerHTML = topic.sections.map(renderSection).join('');
  $('#modal-body').scrollTop = 0;
  $('#modal-prev').disabled = index <= 0;
  $('#modal-next').disabled = index >= list.length - 1;
  $('#topic-modal').hidden = false;
  document.body.classList.add('modal-open');
  history.replaceState(null, '', `#${sourceMeta[source].prefix}-${index}`);
  $('#modal-close').focus();
}

function closeModal() {
  if ($('#topic-modal').hidden) return;
  $('#topic-modal').hidden = true;
  document.body.classList.remove('modal-open');
  history.replaceState(null, '', '#top');
}

function render() {
  const query = $('#search-input').value.trim().toLowerCase();
  const matches = (value) => !query || value.toLowerCase().includes(query);
  const methodNames = Object.keys(methods);
  const activeFilter = document.querySelector('.filter.active')?.dataset.filter || methodNames[0];
  const visibleTopics = topics.filter((topic) => matches(cardText(topic)));
  const visibleOop = oop.filter((topic) => matches(cardText(topic)));
  const visibleBuiltins = builtins.filter((topic) => matches(cardText(topic)));
  const visibleGroups = Object.entries(methods).filter(([name, rows]) => name === activeFilter && matches(`${name} ${rows.flat().join(' ')}`));

  $('#theory-list').innerHTML = renderRows(visibleTopics, topics, 'topics');
  $('#oop-list').innerHTML = renderRows(visibleOop, oop, 'oop');
  $('#builtins-list').innerHTML = renderRows(visibleBuiltins, builtins, 'builtins');
  $('#methods-list').innerHTML = visibleGroups.map(([group, rows]) => {
    const visibleRows = rows.filter((row) => matches(row.join(' ')));
    return `<article class="method-group"><h3>${escapeHtml(group)}</h3>${visibleRows.map(([method, text, example]) => `<div class="method-row"><code>${escapeHtml(method)}</code><span>${escapeHtml(text)}</span><code class="method-example">${escapeHtml(example)}</code></div>`).join('')}</article>`;
  }).join('');

  const visibleCount = mode === 'theory' ? visibleTopics.length : mode === 'oop' ? visibleOop.length : mode === 'builtins' ? visibleBuiltins.length : visibleGroups.length;
  $('#empty-state').hidden = visibleCount > 0;
}

function setMode(nextMode) {
  mode = nextMode;
  document.querySelectorAll('.switch-button').forEach((button) => {
    const active = button.dataset.mode === mode;
    button.classList.toggle('active', active);
    button.setAttribute('aria-selected', String(active));
  });
  $('#quick-section').hidden = mode !== 'theory';
  $('#theory-section').hidden = mode !== 'theory';
  $('#oop-section').hidden = mode !== 'oop';
  $('#builtins-section').hidden = mode !== 'builtins';
  $('#methods-section').hidden = mode !== 'methods';
  render();
}

function parseOpenTarget(value) {
  const [source, index] = String(value || '').split(':');
  if (!sources[source] || Number.isNaN(Number(index))) return null;
  return [source, Number(index)];
}

function init() {
  $('#quick-grid').innerHTML = quick.map(([label, code]) => `<article class="quick-card"><p>${escapeHtml(label)}</p><code>${escapeHtml(code)}</code></article>`).join('');
  $('#method-filters').innerHTML = Object.keys(methods).map((name, i) => `<button class="filter ${i === 0 ? 'active' : ''}" data-filter="${escapeHtml(name)}">${escapeHtml(name)}</button>`).join('');
  $('#topic-count').textContent = topics.length;
  $('#oop-count').textContent = oop.length;
  $('#builtin-count').textContent = builtins.length;
  $('#method-count').textContent = Object.values(methods).flat().length;
  $('#search-input').addEventListener('input', render);
  $('.content-switch').addEventListener('click', (e) => {
    if (e.target.matches('.switch-button')) setMode(e.target.dataset.mode);
  });
  $('#method-filters').addEventListener('click', (e) => {
    if (!e.target.matches('.filter')) return;
    document.querySelectorAll('.filter').forEach((button) => button.classList.remove('active'));
    e.target.classList.add('active');
    render();
  });
  document.addEventListener('click', (e) => {
    const openFrom = e.target.closest('[data-open-topic]');
    if (openFrom) {
      e.preventDefault();
      const target = parseOpenTarget(openFrom.dataset.openTopic);
      if (target) openModal(target[0], target[1]);
      return;
    }
    if (e.target.closest('[data-close-modal]')) closeModal();
  });
  $('#modal-toc').addEventListener('click', (e) => {
    const link = e.target.closest('a[href^="#sub-"]');
    if (!link) return;
    e.preventDefault();
    $('#modal-body').querySelector(link.getAttribute('href'))?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  });
  $('#modal-prev').addEventListener('click', () => openModal(modalSource, modalIndex - 1));
  $('#modal-next').addEventListener('click', () => openModal(modalSource, modalIndex + 1));
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      if (!$('#topic-modal').hidden) {
        closeModal();
        return;
      }
      $('#search-input').value = '';
      $('#search-input').blur();
      render();
    }
  });
  document.querySelectorAll('[data-query]').forEach((button) => button.addEventListener('click', () => {
    $('#search-input').value = button.dataset.query;
    render();
    $('#search-input').focus();
  }));
  const top = $('#to-top');
  window.addEventListener('scroll', () => top.classList.toggle('visible', window.scrollY > 500));
  top.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
  setMode('theory');
  const hash = location.hash.slice(1);
  if (hash.startsWith('topic-')) openModal('topics', Number(hash.slice(6)));
  if (hash.startsWith('oop-')) {
    setMode('oop');
    openModal('oop', Number(hash.slice(4)));
  }
  if (hash.startsWith('builtin-')) {
    setMode('builtins');
    openModal('builtins', Number(hash.slice(8)));
  }
}

init();
